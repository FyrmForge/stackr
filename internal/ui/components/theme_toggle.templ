package components

// ThemeToggle flips light/dark; the choice lives in localStorage only.
templ ThemeToggle() {
	<theme-toggle>
		<button type="button" class="link" aria-label="Toggle light and dark theme">Theme</button>
	</theme-toggle>
}
