package components

// SideDrawer is the one drawer, placed once in the layout outside #main so
// navigation never replaces it. A card click hx-gets a tab into
// #drawer-body; <side-drawer> opens on that swap, and closes itself on
// [data-close] (the X in every DrawerHead), [data-backdrop] or Escape
// (dropping ?drawer and ?tab). A fresh load of ?drawer=&tab= renders it
// open with body inside. The slide and the scrim are CSS (input.css).
templ SideDrawer(body templ.Component, tab string) {
	<side-drawer
		if body != nil {
			open
		}
		if tab != "" {
			tab={ tab }
		}
	>
		<div data-backdrop></div>
		<aside
			role="dialog"
			aria-modal="true"
			aria-label="Details"
			class="fixed top-0 right-0 z-40 h-full w-full max-w-2xl overflow-y-auto border-l border-rw-border bg-rw-surface"
		>
			<div id="drawer-body">
				if body != nil {
					@body
				}
			</div>
		</aside>
	</side-drawer>
}

// DrawerHead is the top of every drawer body (v0 PanelHeader): the title,
// what it is, and the 32px close X.
templ DrawerHead(title, kind string) {
	<div class="flex flex-wrap items-center gap-y-2 justify-between px-4 sm:px-6 py-5 border-b border-rw-border">
		<div class="min-w-0">
			<h2 class="text-lg font-semibold text-rw-text truncate">{ title }</h2>
			if kind != "" {
				<p class="text-xs text-rw-faint truncate font-mono">{ kind }</p>
			}
		</div>
		<button type="button" data-close class="flex h-8 w-8 items-center justify-center rounded-lg text-rw-muted hover:text-rw-text hover:bg-rw-raised transition-colors" aria-label="Close">
			<svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M18 6 6 18M6 6l12 12"></path></svg>
		</button>
	</div>
}

// DrawerLoad is a drawer body that fetches itself: a fresh load of
// ?drawer= for a card whose drawer another handler serves.
templ DrawerLoad(url string) {
	<div hx-get={ url } hx-trigger="load" hx-target="this" hx-swap="outerHTML" hx-push-url="false" aria-busy="true" class="p-6 text-sm text-rw-muted">Loading…</div>
}
