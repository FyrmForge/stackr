# CLI command matrix

Status: historical reference for the shipped CLI rewrite.

The accepted matrix standardized singular command nouns, hidden legacy
aliases, global connection and output flags, interactive confirmation and
machine-readable output. The executable source of truth is
`internal/cli/cmd` and the generated API contract is `docs/openapi.json`.
