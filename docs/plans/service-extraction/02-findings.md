# Service extraction findings

Swept 2026-09-18. Merged from the 14 shard files in `shards/` (10 concept, 4 cross-cutting); round 2 (section 4) merged the six gap shards `shards/g-*.md` (CLI, boot and workers, store, access, small config packages, leftovers) into the rows and the inventory. Read-only record. No fixes, no refactor decisions; those go in `03-refactor.md`.

Paths are under `internal/stackrd/` unless they start with `internal/cli/` or `cmd/`. Short forms: `web/` = `handlers/web/handler/`, `api/` = `handlers/api/v1/`, `stackconf/` = `config/stackconf/`, `orgconf/` = `config/orgconf/`, `envops` = `config/envops/envops.go`, `apply` = `config/stackconf/apply.go`. Gates: `editGate` = config-owned field edit, may stage; `rejectManaged` = structural write, fails closed; `managedErr` / inline `ConfigManaged()` = a third hand-written spelling; `rFU` = `requireFileUnowned` (`web/db/handler.go:62`, RequireUnmanaged with org-scope exception); `RU` = `stackrmw.RequireUnmanaged`.

Corrections to `00-calibration.md`:
- The `Tile → TileConf` serializer exists: `stackconf.TileConfOf` (`stackconf/serialize.go:72`), called from `web/project/handler.go:587` and `web/app/handler.go:1339`. `settingsPatch` (`web/app/handler.go:1366`) does not use it.
- Tile field-list copies are eleven, not four: `repo.Tile` (`store/repo/models.go:342`), `TileConf` (`stackconf/stackconf.go:363`), `tileToConf` (`serialize.go:76-162`), `applyTileConf` (`apply:1681-1770`), plan diff (`stackconf/plan.go:415`, `:732`), `settingsPatch` (`web/app/handler.go:1366-1425`), `patchApp` key switch (`api/apps.go:177-451`), web SaveSettings form (`web/app/handler.go:1428-1577`), web CreateTile form (`web/project/handler.go:471-582`), api createApp (`api/apps.go:60-157`), CLI `tile set` flags (`internal/cli/cmd/tile.go:221`).

Class D hits (docker/swarm above `infra/`): one. `service/admin.go:16` imports `github.com/docker/docker/api/types/swarm`, used at `:156-161`. No hit in `handlers/`, `config/`, `internal/cli`. Inverse layering, not D: `infra/deploy/engine.go:533`, `infra/jobs/jobs.go:421`, `infra/imagewatch/watcher.go:142`, `infra/cigate/cigate.go:117` import `handlers/notify`.

## 1. Operation table

Columns: operation | web | api | cli | config | shared | gate (web / api / config) | logic in handler | side effects | return shape | class.

### 1.1 Tiles

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| List tiles | `web/project/handler.go:628` Graph via `buildGraph` `:1303` | `api/apps.go:23` listApps | `internal/cli/cmd/tile.go:68` | -- | separate | none | api drops `IsManaged`/`IsVolume` rows (`apps.go:32`), per-row `orgAllowed`; web graph includes every kind | store read | api `appOut` (`types.go:92`) vs web graph nodes | |
| Get tile | `web/app/handler.go:583` Detail, `:622` Panel (`load` `:430`, `loadTab` `:521`) | `api/apps.go:169` getApp (`requireTile` `auth.go:313`, slug path `slugpath.go:94`) | `internal/cli/cmd/lifecycle.go:67` | -- | separate | none | web `loadTab`: `placement.NodeOf` `:489`, `px.AccessLog` `:555`, `configMode` `:564` | store read, proxy read (web) | web panel vs `appOut` (no kind, port, limits, replicas, node) | |
| Create tile | `web/project/handler.go:471` CreateTile (UI-managed stages via `stageChange` `:693` → `staging.go:50` → `stackconf/staging.go:118` → `apply:1171`) | `api/apps.go:60` createApp | `internal/cli/cmd/tile.go:93` | `apply:1171` createTile + `:1681` applyTileConf + `:1880` applySource; rules `stackconf/stackconf.go:995` validateTile | three copies | web inline `ConfigManaged()` `:586/:603`; api `managedGuard` `errors.go:14` at `:69` | web: slug rules `:477-529`, kind switch incl. volume `:491-503`, connector org check silently drops `:551-555`, port ≤65535 `:570`, `TimeoutMinutes:30` every kind `:568`. api: `ReservedSlug` `:81`, dup 409 `:84`, source inference `:87-94`, `runpolicy.For` `:103`, port-on-no-ingress `:122`, `checkConnector` 400 `:145`. config: full `validateTile`. cli blanks `schedule`/`command`/`timeout` unless `--cron`/`--function` (`internal/cli/cmd/tile.go:126-129`), api stores them on any kind (`apps.go:150-153`); web kind switch skips `runpolicy`, unknown kind → service (`web/project/handler.go:492-503`); SQL column defaults dead, `CreateTile` writes every column (`001_initial.up.sql:288-316`, `store/repo/sqlite/tiles.go:43-58`), nine callers re-supply them; webhook token `uuid` on api/orgconf (`api/databases.go:237`, `orgconf.go:966`) vs `RandomHex(24)` web `:576` | web: `CreateTile` `:607`, `jobs.LoadSchedules` cron `:611`, `Enqueue` attach target `:616`, staging `:592`. api: store, `LoadSchedules` `:162` (error returned 500). config: store, `ReplaceTileVars` `:1212`, `syncBindings` `:1216`, `syncDomains` `:1222`, deploy `:1227-1238`, cron reload `:1100` | redirect / `appOut` / plan | A |
| Update tile settings | `web/app/handler.go:1428` SaveSettings (+ `settingsPatch` `:1366` staged) | `api/apps.go:177` patchApp; also accepts a managed id (databases) | `internal/cli/cmd/tile.go:221` (`tileSetFlags` `:175`: no replicas, node_group, volumes, run_on_deploy, basic_auth, healthcheck) | `apply:1404` updateTile + `:1681`; `validateTile` | three copies | web `editGate` `:1439`; api `rejectManaged` `:183` (calibration: should be editGate) | limits: web clamps `:157-170`, api raw incl. negatives `:251-253`, config none. timeout: web [1,1440] `:173-182`, api/config refuse <0 only. healthcheck: web clamps `:1507-1510`, api 400 `:376`, config error `:1038`. `source_type` raw on web `:1475`, derived on api `:211-224`. `volumes`: web raw `:1498`, api no key, config `:1732`. `command`: config refuses on non-AllowsCommand `:1018`, web/api accept. cron empty image: web ok `:1454`, api 400 `:445`, config error `:1012`. cli `--cpu` alone sends `limits{cpu, memory_mb:0}` and `--dockerfile` alone clears `build.context` (`internal/cli/cmd/tile.go:247-263`), `patchApp` overwrites both halves (`apps.go:248-253`); "a limit pair is atomic" is written nowhere server-side | web: `UpdateTile` `:1588`, cron reload `:1592`, `syncProxy`→`WriteApp` `:1597`, staging `:1582`. api: store `:453`, cron reload `:458`; no proxy, no redeploy. config: store `:1421`, `ReplaceTileVars` `:1426`, `syncBindings` `:1429`, `syncDomains` `:1441`, `WriteApp` proxy-only `:1491`, deploy on any other field `:1494`, cron rebuild `:1500` | panel / `appOut` / plan | A, B |
| Scale tile (replicas, node_group) | `web/app/handler.go:1540-1554` (`placement.IsPinned` `:1545`) | -- (`appPatch` parses keys, `patchApp` drops them) | -- | `apply:1742-1743`, `validateReplicas` `stackconf.go:1030` | separate | web `editGate` | web: n<1 400, pinned>1 400, `any`→"" | web store only, no redeploy; config deploy `:1494` | -- | A |
| Rename tile | -- | -- | -- | `stackconf/moved.go:255` applyMoves; `:306-311` TearDown, `RemoveApp`, `RenameTile`, `restartTile`, no `WriteApp` after | config only | n/a | `restartTile` (`apply:594-615`) routes managed → `DBs.Deploy`, running service → `a.deploy`, other kinds and stopped tiles → nothing; no branch writes the proxy | proxy route removed and never rewritten (`x-proxy` row 42): the deploy engine never calls `WriteApp` (1.11), so a renamed tile serves nothing until a domain edit or `Resync` | -- | B |
| Delete tile | `web/app/handler.go:1606` Delete | `api/apps.go:463` deleteApp → `teardownTile` `helpers.go:129`; accepts volume and managed ids (`requireTile` has no kind check) | `internal/cli/cmd/tile.go:316` (`rt.Confirm` `:331`) | `apply:1544` deleteTile | three copies | web `editGate` `:1612` (stages a structural op); api `rejectManaged` `:468` | web: attached-volume guard `:1616`, `RequireConfirm` `:1620`, provision detach `:1635-1638`. api: none of these. config: detach `:1559-1564`, no volume guard | web: `envnet.TearDown` `:1632`, `px.RemoveApp` `:1633` (ignored), `Detach` `:1637`, `DeleteTile` `:1640`, no `jobs.LoadSchedules`. api: TearDown, RemoveApp, store, `LoadSchedules` `:475`; no detach, no volume guard. config: `stopContainers` `:1554`, RemoveApp `:1556`, Detach `:1562`, store, cron reload `:1100`. All six tile-delete sites cascade `backups` rows with no backup scheduler reload (`store/db/migrations/001_initial.up.sql:51`); FK cascade covers `domains`, `deployments`, `backups` (`001_initial.up.sql:103,91,51`), `DeleteTile` drops `variables` and `resource_bindings` itself (`store/repo/sqlite/tiles.go:269-282`), `provisions` has no FK (`:199-213`) so api and orgconf leave rows behind; cli adds only `Confirm` (`tile.go:331`) | redirect / 204 | A, B |
| Stop | `web/app/handler.go:330` | `api/lifecycle.go:30` stopApp (accepts managed id) | `internal/cli/cmd/tile.go:43` | -- | separate copies | none | both `ScaleService(ServiceFor(t),0)`, no kind check; web swallows status error `:339`, api returns `:39` | web: cluster, `UpdateTileStatus`, notifier `:341`. api: cluster, store, no notifier. `tiles.status` has 14 writers and one reconciler (`infra/metrics/reconcile.go:24-68`, 1.11); a cron written `stopped` here is skipped by the reconciler for ever (`:26`) and still ticks (`jobs.go:311`) | header / `appOut` | A, B |
| Restart | `web/app/handler.go:352` → `restartTile` `:379` | `api/lifecycle.go:48` restartApp | `internal/cli/cmd/tile.go:47` | `apply:594` restartTile (rename path, redeploys) | separate | none | `ServiceFor` empty → 400, `RestartService(name, Replicas)`; managed id on api gets no `selfHealProvisions` (web `dbs.Start` `managedtiles.go:543`) | web: cluster, status, notifier `:364/:370`. api: no notifier. config: engine enqueue | header / `appOut` | B |
| Toggle cron pause | `web/app/handler.go:398` ToggleCron | `api/lifecycle.go:76` toggleCron | `internal/cli/cmd/tile.go:51` | -- | separate | none | api `Kind!="cron"` 400 `:81`; web no kind check (any tile → `paused`) | web: status, `LoadSchedules` `:410`, notifier `:411`. api: status, reload `:93`, no notifier | header / `appOut` | A, B |
| Run now | `web/app/handler.go:193` RunNow | `api/apps.go:497` runApp | `internal/cli/cmd/tile.go:350` | -- | shared `jobs.StartApp` (`infra/jobs/jobs.go:332`, no kind check inside) | none | api cron/function only `:502`, nil jobs 503 `:505`; web no kind check, `h.jobs` nil deref `:201`; trigger `"manual web"` vs `"manual api"` | run row, workqueue, `notifier.Project` inside `StartApp` `:348` | header / `runAccepted` | A |
| Stop run | `web/app/handler.go:209` | `api/apps.go:520` | `internal/cli/cmd/lifecycle.go:228` | -- | shared `jobs.Stop` `jobs.go:239` | none | both check `run.Ref=="app:"+id` | workqueue cancel | flash / `runStopped` | |
| Tile logs | `web/app/handler.go:97` LogsStream (tail 300) | `api/logs.go:175` getAppLogs (tail ≤5000) | `internal/cli/cmd/deploy.go:19` | -- | resolver duplicated (web `:114-125`, api `:188-212`) | none | service via `envnet.ServiceFor` else `ListByLabel` | cluster read | SSE vs `logsOut` | |
| Run logs | `web/app/handler.go:239` RunsLogsStream (`jobs.RunService` `:272`) | -- | -- | -- | web only | none | replay + follow | cluster read | SSE | |
| Metrics | `web/app/handler.go:145` (`components.MetricRange` `metrics_templ.go:19`) | `api/lifecycle.go:131` appMetrics (`:137-142`) | `internal/cli/cmd/lifecycle.go:183` | -- | range parser duplicated | none | web downsample to 240; cli refuses an unknown `--range` with exit 2 (`internal/cli/cmd/lifecycle.go:191-195`), api coerces it to 1h (`:137-142`) | store read | fragment vs `metricOut` | A |
| Port-forward | display only (`web/org/graph.go:417`, `web/project/handler.go:1386,1431` via `forwards.Sessions()`) | `api/forward.go:33` forwardTile, `:106` forwardPresence | `internal/cli/cmd/deploy.go:224` | -- | one impl | `requireTile(write)` + `ScopeTilesForward` `:37`; no managed gate | `forwardPort` `:184`, running task `:50`, env net `:60`, presence registry + `notify.Project` `:114-138` (only nil-guarded notifier call) | runtime dial, forward registry, notifier | websocket | |
| Start / stop / remove a raw container | `web/container/handler.go:202,210,222` | -- | -- | -- | web only | adminOnly; `ContainerIsSystem` on stop (any state `:212`) and remove (only when running, second inspect `:224-235`); none on start `:202` or terminal (`container/terminal.go:36`) | system guard is handler code; `cluster.StopContainer`/`StopRemove` accept anything (`infra/cluster/cluster.go:171,182`), which `envnet.TearDown` relies on (`envnet.go:214`); roles in `runtime.go:1428-1446` | cluster, `notifier.Containers` `:206,218,239` | redirect keeping `?stopped` `:197-199` | |
| Resolve a tile ref (name or path → id) | -- | `api/slugpath.go:94` (`org:stack:env:tile`) | `internal/cli/cmd/helpers.go:132,159,198` resolveApp / resolveTile / resolveTileID, `backup.go:16` backupTarget | -- | four client-side scanners of `/apps` and `/dbs` beside one server resolver | none | db name matches only inside the linked stack `helpers.go:184`; bare id retries unscoped `:213-221` | api reads | id | |

### 1.2 Volumes + storage

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Create volume tile | `web/project/handler.go:471` CreateTile kind=volume (`:500-503,530-545,586-598`) | `api/volumes.go:39` createVolume | `internal/cli/cmd/tile.go:625` | `apply:1190-1195` + `:1378` resolveAttach + `:1237` redeployAttached; `envops.go:668-686` | five writers of `Kind:"volume"`, the fifth is boot `envops.ConvertLegacyMounts` (`envops.go:642-700` from `main.go:213`: `uniqueSlug` `:700`, no `ConfigManaged` gate, no redeploy of the target; 1.11) | web always stages `:586`, `managedErr` `:603`; api `rejectManaged` `:47` | web: target same env, `service`, `!IsManaged` `:537`, path absolute `:540`, attach optional, no volume_name/max_size. api: app `!IsManaged && !IsVolume` `:44` (cron allowed), attach mandatory, `ValidVolumeName` `:57`, `MaxSizeMB>=0` `:70`. config: any target kind `:1383`. cli defaults the name to the last mount segment or `data` (`tile.go:644-650`), api refuses an empty name (`volumes.go:51`) | web: staging only. api: `CreateTile` `:77`, `Enqueue(app,"volume")` `:80` (error returned). config: store, deploy target only if service `:1397` | redirect / `volumeOut` / plan | A |
| Attach / detach / re-path volume tile | `web/app/handler.go:723` Attach | -- (`patchApp` has no attach key) | -- | `apply:1414-1419` updateTile → resolveAttach, `:1437` redeployAttached | separate | web `editGate` `:744` | web: target rule `:753`, path absolute `:756`, size warn-only `:765-769`, stages `{attach,path}` `:782`. config: slug → id `:1383` | web: `UpdateTile` `:790`, `Enqueue` old and new target `:794-801` (ignored). config: `UpdateTile` `:1421`, deploy new target only `:1393-1399`; old target never redeployed | panel / plan | B |
| Set `max_size_mb` / `volume_name` | `web/app/handler.go:765` (size only) | `api/volumes.go:56,72` create-time only | `tile.go:665-668` | -- (`TileConf` has no field, `apply:1683-1686` never writes) | separate | as above | staged create via `TileConfOf` cannot carry either | store | -- | A |
| Delete volume tile | `web/app/handler.go:1606` Delete | `api/volumes.go:88` deleteVolume; also `api/apps.go:463` deleteApp | `tile.go:711` | `apply` delete of undeclared tile | separate | web `editGate` `:1612`; api `rejectManaged` `:98,:468` | web refuses while attached `:1616-1618`, typed confirm `:1620`. api deleteVolume: no attached guard, `teardownTile` then `Enqueue(app)` `:104-107`. api deleteApp: teardown only, no Enqueue, app keeps bind to a volume with no row | TearDown, RemoveApp, detach, DeleteTile | redirect / 204 | A, B |
| List volumes of a tile | `web/graph/graph.go:641-648`; `web/app/handler.go:698` volumeView | `api/volumes.go:20` (`ListTiles` all rows, filter `:25-33`) | `tile.go:674` | `stackconf/serialize.go:82` attachedSlug | same filter copied (see ownership table) | none / `requireTile(read)` | filter | none | graph `Subtile` vs `volumeOut` | |
| Volume size / inspect | `web/app/handler.go:708` Size (`InspectVolume` no node); `web/db/handler.go:98` VolumePanel → `managedtiles.DataVolume` `managedtiles.go:342` | -- | -- | -- | separate | none | node-less inspect for a moved volume: not checked (`files.go:26` uses `clus.NodeOf`) | none | fragment | |
| Browse / upload / delete files in a volume | `web/app/files.go:15` loadVolumeTile; `web/db/files.go:21` loadVolume | -- | -- | -- | two copies of tile → (node, name) | `load` auth only, no editGate | node + name derivation, `filebrowse.VolumeFS` | cluster file ops | HTML | |
| Move a tile's volumes to another node | `web/server/nodes.go:399` MoveForm, `:447` Move, `:422` moveTargets | -- | -- | `apply:308,663` pendingMoves (refuses apply) | engine `infra/volmove`; target rule in handler `:422-444,459-465`; pending-moves filter twice (`web/project/handler.go:978-989`, `apply:1929-1943`, both `placement.InGroup`); consumer `volmove.go:550,569,675,680` is the fifth "Deploy then status" copy, `:390-425` rolls `home_node` back on boot | adminOnly | candidate nodes = ready, not home, in `placement.For` group | workqueue `:342` | modal | |
| List docker volumes on a node | `web/server/handler.go:158` | -- | -- | -- | -- | adminOnly | none | none | `runtime.VolumeInfo` | C |
| Create hand-made docker volume | `web/server/handler.go:260` | -- | -- | -- | -- | adminOnly | `ValidVolumeName` `:262`, node joined `:244-257` | `clus.CreateVolume`, no row | redirect | C |
| Delete docker volume on a node | `web/server/handler.go:277` | -- | -- | -- | -- | adminOnly | confirm against live list `:292-301`; no ownership check, `stackr-vol-`/`stackr-db-`/`stackr-stor-` volumes with live rows deletable | `clus.RemoveVolume` `:303` | redirect | C |
| Create storage (pool / share) | `web/server/storage.go:68` (`ServerID = :id` `:80`) | `api/storage.go:88` (`ServerID:"local"` hard-coded `:100`; org share `:104-113`) | `internal/cli/cmd/storage.go:59` | `orgconf/storage.go:195` applyStorage (org shares) | separate | web adminOnly; api adminOnly + `orgShareOwner` `:238-249` | `ValidBackend`, export/address rules, slug unique (web `:76` / api `:116-118`); config no probe `:213-217`, row inserted `Status:"unknown"`; orgconf is the only update path (`orgconf/storage.go:223-233`, drops share volumes then rewrites) | row saved before probe (web `:92`, api `:120`); probe via throwaway volume; `UpdateStorage` | redirect / `storageOut` | A |
| Probe storage | `web/server/storage.go:25,128` | `api/lifecycle.go:305` | `internal/cli/cmd/lifecycle.go:690` | -- | two identical copies (web `:29-42`, api `:311-324`) | adminOnly | `NodeOfStorage`, `storagetiles.Probe`, status write | Probe, `RemoveVolume`, `UpdateStorage` | redirect / `storageOut` | |
| Delete storage | `web/server/storage.go:105` (no `OrgID` branch) | `api/storage.go:141` → `:250` deleteOrgShare | `storage.go:106` | `orgconf/storage.go:235-248` | separate | adminOnly; api + `orgShareOwner` `:256` | consumer scan web `:47-65` / api `:153-159`; api org path `OrgStorageRef` `:261-267`; web has no org-ref scan, skips `DropOrgShareVolumes` and managed check | `RemoveVolume` per path, `DeleteStorage`; api org: `DropOrgShareVolumes` `:266` | redirect / `{}` | B |
| Declare storage sub-path | `web/server/storage.go:144` | `api/storage.go:172` | `storage.go:140` | -- | separate | adminOnly; api refuses org share `:179` | web slugify then empty check `:150-152`; api empty check then slugify `:185,189` (punctuation-only name → `Name==""`); row before probe, kept on failure | `CreateStoragePath`, Probe | redirect / `storagePathOut` | A |
| Remove storage sub-path | `web/server/storage.go:180` | `api/storage.go:207` | `storage.go:164` | -- | separate, same logic | adminOnly | consumer scan `:188` / `:213-221` | `RemoveVolume`, `DeleteStoragePath` | redirect / `{}` | |
| Attach storage to a tile | `web/app/storage.go:71` AttachStorage (server storage only, `:91`) | `api/apps.go:297-321` in patchApp (org refs `:304-312`) | `tile.go:203` `--storage` | `stackconf.go:1064-1070` validate (service-only); `apply:1752`; `infra/deploy/engine.go:703-709` | separate | web `RU` `:78` (reject-only); api `rejectManaged`; config service-only | web: `ParseAttachment`, `ValidateAttach` `:95`, dedupe `:98-102`, no kind check. api: per line, outside the `Kind=="service"` block `:332`, cron/function accepted. config refuses non-service | web `UpdateTile`, `Enqueue` if running `:108-110`; api `UpdateTile` (enqueue not checked); config deploy | fragment / `appOut` | A |
| Detach storage from a tile | `web/app/storage.go:115` | same PATCH | same | drop the line | separate | as attach | exact-line filter `:124-131` | `UpdateTile`, conditional `Enqueue` `:135-137` | | |
| List storage | server page; `web/org/settings.go:487` (filter `:496-501`) | `api/storage.go:73` | `storage.go:22` | `orgconf/storage.go:100` orgShares `:105-110` | three copies of "storage rows of org X" | settingsOrg / adminOnly | filter | none | `storageOut` vs `[]repo.Storage` | |
| Resolve the docker volume a tile backs up | `web/backups/handler.go:110,224` | `api/backups.go:198` | via api | `backup:` key | one impl `infra/backup/backup.go:305` VolumeFor | -- | none | none | -- | |
| Clone env: fresh volumes for the copy | -- | -- | -- | `envops.go:94-98` | one impl | n/a | rule lives in envops | store | -- | |

"Which volumes does X own", every implementation (class C prerequisite): `infra/backup/backup.go:305` VolumeFor; `infra/volmove/volmove.go:232` volumesOf; `infra/deploy/engine.go:1265` volumeBinds; `infra/placement/placement.go:78` IsPinned; `api/volumes.go:20`; `web/graph/graph.go:641,648`; `web/graph/graph.go:573` (`"stackr-db-"+id[:8]`); `web/search/handler.go:317,337`; `infra/storagetiles/storagetiles.go:83` DropOrgShareVolumes; `repo.StorageVolume` callers (`api/storage.go:64-68,163,226`, `web/server/storage.go:117,193`); `orgconf/storage.go:116` shareUsers; `web/server/storage.go:47` attachedConsumers + `api/storage.go:153-159,213-221,261-267`; volume tile → owning service hop at `placement.go:48`, `backup.go:421,431`, `volmove.go:751`. Thirteen. Volume name → owner does not exist anywhere; `web/server/handler.go:277` needs it.

### 1.3 Databases (managed instances and slices)

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Create instance | `web/project/handler.go:1659` CreateDB | `api/databases.go:207` createDB | `internal/cli/cmd/infra.go:40` | `apply:1171` (managed branch `:1196-1232`); `orgconf/orgconf.go:914` applyShared (`:962-983`) | separate; all call `managedtiles.NewDB` + `PublishConnection` (`managedtiles.go:305,383`) | web stage when UI-managed, `managedErr` when config-managed `:1708-1718`; api `managedGuard` `:216` | web: empty-slug refuse `:1670`, scope env/stack only `:1700`, stages; `:1719-1727` unreachable. api: no empty check, scope incl. org `:86`, deploys synchronously on request ctx. config: full mapping, vars, bindings, domains, deploy. orgconf `applyShared` (`orgconf.go:962-970`): `Slug: name` raw (no `Slugify`), no `ReservedSlug`, no collision check, `hostEnv` = oldest stack's first env `:1002-1016`, `WebhookToken` `uuid` (`:966`, also `api/databases.go:237`) vs `RandomHex(24)` web | web: staging only. api: store, cluster, vars. config: + bindings, proxy. orgconf: store, cluster, vars | `dbOut`+Path / redirect | A |
| List instances | canvas (not checked) | `api/databases.go:19` | `infra.go:152`; `internal/cli/cmd/helpers.go:77` | -- | separate | none | `IsManaged` + `orgAllowed`, `infraPath` `:43` | store | `[]dbOut` with Path | |
| Get instance | `web/db/handler.go:70,84,146` | `api/databases.go:196` getDB | `infra.go:369` | -- | separate | none | api `IsManaged` 404; getDB omits `Path` that listDBs sets (`:76-80` vs `:35`), CLI prints `path: none` `infra.go:397` | store | differ | A |
| Update instance settings (port, cpu, mem, shm, image, update_policy) | `web/db/handler.go:475` SetPort | `api/databases.go:103` patchDB; also `api/apps.go:177` patchApp on a db id (no `IsManaged` check, no db rules, no redeploy) | `infra.go:301` (no `--update-policy`, `internal/cli/client.go:733`) | `apply:1404` (`:1432-1436` republish, `:1458-1486` redeploy, `needsDBRedeploy` `:1527`); `orgconf.go:929-946` | four rule sets | web `rFU` `:483`; api `rejectManaged` `:112` | web coerces bad port → 0 `:486-489`, mem<6 → 6, bad policy → off `:510-515`, redeploys whenever running `:519`. api 400s `:121-158,178`, redeploy on changed field + running. config redeploy on `needsDBRedeploy` when running or error (incl. node_group/replicas). orgconf compares three fields | store, cluster (Deploy) | `dbOut` / panel | A |
| Set sharing scope | `web/db/handler.go:529` SetScope | `api/databases.go:103` (`:160-170`, `applyScope` `:86`) | `infra.go:301 --scope` | `apply:1704-1711` | separate | web `RU` `:535` (no org exception; `SetPort` in same handler uses `rFU`); api `rejectManaged` | api 400 on unknown scope, web falls to env `:548` | store | `dbOut` | A |
| Delete instance | `web/db/handler.go:444` Delete | `api/databases.go:409` deleteDB; also `api/apps.go:463` deleteApp on a managed id (no force gate) | `infra.go:213` (always `force=true` `:262`; `--force` or `-y` skips the slice-listing prompt `:1532-1537,1551-1559`, so the api default-refuse is unreachable from the CLI) | `apply:1544` deleteTile; `orgconf.go:985-995` | four teardowns | web `rFU` `:452` + `RequireConfirm`; api `rejectManaged` `:418` | web: refuse if provisions `:460`, `dbs.Remove` (RemoveService + `netpool.ReleaseDB` `managedtiles.go:570-576`), `DeleteTile`; no `px.RemoveApp` `:464-467`. api: refuse unless force `:425-434`, `teardownTile` (TearDown + RemoveApp + DeleteTile); no `dbs.Remove`, no netpool release, force leaves provision/resource rows (`store/repo/sqlite/tiles.go:269-282`). config: TearDown, RemoveApp, detach consumers, no held-slices gate, no netpool. orgconf: `dbs.Remove` + DeleteTile only | web: store, cluster, netpool. api: store, cluster, proxy. config: + provisions. orgconf: netpool | -- | B |
| Deploy / redeploy instance | `web/db/handler.go:243` Deploy (goroutine, `context.Background()`) | inside createDB/patchDB; `api/apps.go:480` deployApp accepts a managed id and enqueues the build engine | -- | `apply:1227,1473`, `restartTile :594` | engine `managedtiles.go:420`; status bookkeeping copied six times (`web/db:251-261`, `api/databases.go:249-259`, `apply:1227-1232,1473-1485,603-608`, `orgconf.go:975-982`) plus `volmove.go:675-680`; `imagewatch/watcher.go:152-162` calls `Deploy` on `context.Background()` and writes no status at all (`volmove.go:667-672` documents that the caller must) | none (web) | web async + notifier `:305`; api sync; config sync + warn | cluster, status, notifier (web only), vars (`PublishConnection` self-heal `managedtiles.go:435`) | -- | A |
| Stop instance | `web/db/handler.go:268` | `api/lifecycle.go:30` on a managed id | via resolveTile (not checked) | -- | same cluster call | none | status stopped; web notifier | cluster, store, notifier (web) | header / `appOut` | |
| Start / restart instance | `web/db/handler.go:286` Start | `api/lifecycle.go:48` restartApp | not checked | -- | separate | none | web `managedtiles.go:543` Start (scale 1, `WaitRolled`, `selfHealProvisions`); api `RestartService`, no heal | cluster, store, notifier (web) | | A |
| Publish own connection vars | `web/project/handler.go:1722` (dead) | `api/databases.go:247` | -- | `apply:1220,1435`; `envops.go:104`; `orgconf.go:973` | one impl `managedtiles.go:383`, six hand-sequenced callers | n/a | `updateTile` must re-call after `ReplaceTileVars` `:1432-1436` | store vars | -- | |
| Provision slice for a consumer (+ wire var) | `web/app/handler.go:901` Provision, `:867` wireProvision | `api/databases.go:276` provisionApp, `:374,:397` | -- (two-step via `infra provision` + `tile attach`; cli gates instance vs slice in six `t.Kind !=` checks `infra.go:1389,1426,1466,1618,1682,1722`, `tile.go:415`) | -- (config is slice-first); `envops.go:242` cloneProvisions | separate; engine `managedtiles/provision.go:30` | web `rejectManaged` `:906`; api none (`errors.go:11-13`) | web: `Eligible`, name ""/public false fixed, `envVarName` sanitiser `:966`, single `Ref(DefaultOutput)`, dead config-managed branch `:874-881`. api: `ResolveTarget`, `Eligible`, raw `EnvVar` `:314`, config-managed → publish only `:378`, s3 `AutoInjectAll` whole set `:381-390` | provision + resource rows, consumer `Env`, `Enqueue("provision")`, cluster joinSharedNet | flash / `provisionOut` | A, B |
| Attach consumer to existing slice | `web/app/handler.go:931` | `api/databases.go:322` | `internal/cli/cmd/tile.go:384` | -- | separate; engine `provision.go:107` | web `rejectManaged` `:936`; api none | same-env check duplicated (`:944` / `:336` vs `provision.go:108`); same wiring split as above | as above | | A, B |
| Detach consumer from slice | `web/app/handler.go:989` | `api/lifecycle.go:254` | `internal/cli/cmd/lifecycle.go:335` | `stackconf/slices.go:135` removeSlice; `envops.go:613,525-545` | engine `provision.go:139` | `rejectManaged` both | ownership `p.ConsumerTileID == tile.ID`; no redeploy either side | provision + binding | | |
| Orphan provisions on consumer delete | `web/app/handler.go:1635-1639` | `api/apps.go:463` → `teardownTile` (no lookup) | `tile rm` | `apply:1558-1565`; `envops.go:521` reclaimSlices | separate | web `editGate`; api `rejectManaged` | web/config `Detach` per consumer row; api leaves rows `active` with dangling `ConsumerTileID` | | | B |
| Cut consumer-less slice | -- | `api/resolve.go:81` createInstanceProvision | `infra.go:73` | `stackconf/slices.go:68` createSlice (`CloneSlice` ephemeral `:91-93`), `:121` stampSlicePolicy; `apply:805,1291` | separate; engines `provision.go:49,91` | api none; config n/a | api `ServesEnv` `scope.go:102`, no `Eligible` visible in `:81-155` (not confirmed), no readiness wait, no `on_remove`. config `resolveFrom` `slices.go:21`, `Eligible` `:79`, `WaitReady 90s` `ready.go:42`, adopt-or-create | provision + resource, cluster exec | `sliceOut` | A |
| Drop slice (all consumers) | `web/db/handler.go:352` | `api/resolve.go:156` | `infra.go:213` slice branch | `slices.go:135` (on_remove drop); `envops.go:537,628` | engine `provision.go:152` | web none (no `rFU`, no confirm); api `requireTile(inst, write)` `:162`, no `rejectManaged` | `db_name` from form | store, cluster exec | | |
| Fork slice | `web/db/handler.go:375` | `api/resolve.go:128` (`ScopeDBsFork`) | `infra.go:111` | -- | engine `fork.go:34` | none | web `src.InstanceTileID == d.ID`, no name input; api name/slug from body | store, cluster exec | `sliceOut` | |
| Set slice public | `web/db/handler.go:398` | `api/lifecycle.go:274` | `infra.go:409` | `slices.go:103` (`:111-114`) | engine `provision_s3.go:212` per row | none | web loops every row sharing `DBName` `:425-433`, notifier; api `provisionRows` loop `:292-297`, `p.Public` in memory only, no notifier; config flips the one `findSlice` row `:154-180` | store, s3 policy, notifier (web) | | B |
| List slices of an instance | `web/db/handler.go:311` (groups by `DBName`, first row wins `:333`) | `api/resolve.go:56`, `toSliceOut :174` | `infra.go:152 <path>` | -- | separate | none | grouping by hand | store | `dbGroup` vs `sliceOut` | |
| List a consumer's provisions / resources | `web/app/handler.go:811` | `api/databases.go:354`; `api/variables.go:397` | `tile.go:450` | -- | separate | none | web builds `Ref` per row | store | `provisionRow` vs `provisionOut`/`resourceOut` | |
| Resolve infra address | `web/app/handler.go:851` (build only) | `api/resolve.go:23` resolvePath | `infra.go:36` | `slices.go:21` resolveFrom | two resolvers: `ResolveTarget` `resolve.go:86` (colon, org-scoped), `ResolveInfraPath` `infrapath.go:28` (dotted) | api `requireEnvAccess` | two grammars, same rows | store | `resolveOut` | |
| Domains on an instance (also domains) | `web/db/handler.go:572,617,641` | `api/domains.go:35` accepts a managed id | not checked | -- (`syncDomains` skips non-service) | separate | web `rFU`; api `rejectManaged` | web `SpeaksHTTP` `:581`, port = engine port `:601`, https ⇒ force. api no engine check, port from body or `t.ContainerPort` (0), https/force default true | store, proxy `WriteApp` | | A |
| Backups on an instance (also backups) | `web/backups/handler.go:107,217` | `api/backups.go:182` | `stackr backup` | `backup:` | engine `backup.go:305,317,698` | not checked | kind default managed → dump | store | | |
| Data / file browser, data volume panel | `web/db/handler.go:98`; `web/db/data.go`, `files.go` | -- | -- | -- | engine `sqlbrowse.go`, `s3browse.go`, `managedtiles.go:342` | none | UI-native | cluster exec | | |
| Instance logs / metrics | `web/db/handler.go:184,232` | `api/logs.go:16` on a db id (not checked) | not checked | -- | | none | pre-swarm container fallback `:201-211` | cluster | | |
| Env clone / teardown of instances and slices | -- | -- | -- | `envops.go:48` CloneTiles (`:79-87` new creds, `:103-105` publish, no Deploy), `:242` cloneProvisions, `:511` Teardown (`:525-545`), `:613` reclaimSlices | direct engine calls | n/a | ephemeral vs persistent decides drop vs detach | store, cluster, vars | | |
| Org-shared instances (also orgs) | -- | `api/databases.go:207` scope org | `infra create --scope org` | `orgconf.go:914` applyShared | separate from stackconf branch | none in orgconf | compares image/shm/port `:932`; engine change = Remove + DeleteTile + recreate `:948-955`; undeclared = Remove + DeleteTile `:985-995`, no provisions gate, no TearDown, no RemoveApp | store, cluster, netpool | | B |

### 1.4 Environments + variables + secrets

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Create environment (empty) | `web/project/handler.go:305` CreateEnvironment | `api/stacks.go:89` createEnv | `internal/cli/cmd/stack.go:187` | `apply:906-926` | separate; PR hook is a fourth creator (`web/prhook/handler.go:501-510`, no checks) | web `managedErr` `:315`; api `managedGuard` `:98` | web: reserved `settings`/`list` `:322-325`, no empty or duplicate check. api: empty check `:106` only, Type hard `static`. config: skips existing `:907`, no reserved rule. Home env is a real row with slug `stack` (`store/repo/models.go:192-195`) hidden by `ListEnvironmentsByStack` (`sqlite/stacks.go:302-306`); api reserves `HomeSlug` (`envs.go:132`), web does not, so a web env named "Stack" hits `UNIQUE (stack_id, slug)` (`001_initial.up.sql:126`) as a raw 500; no layer maps constraint errors | store; config + env settings loop `:940-968` | redirect / `envOut{id,name,slug}` | A |
| Clone environment | `web/project/handler.go:305` with `base_env_id` (`:337-353`) | `api/envs.go:118` copyEnv | `internal/cli/cmd/promote.go:289` | -- | `envops.CloneTiles` `envops.go:48` | web `managedErr`; api `rejectManaged` `:124` | web: base in stack `:342-344`, no dup check, `LoadSchedules` only on clone `:352`. api: reserved incl. `HomeSlug` + empty `:132`, dup 409 `:135`, reload always `:150` | env row, tiles, vars, provisions, layout; `PublishConnection`; `EnsureAutoDomain` → proxy `envops.go:109-116`; scheduler reload | redirect / `envOut` | A |
| List environments | `web/project/handler.go:2089` | `api/stacks.go:73` | `stack.go:145` | -- | separate | none | web tile counts + colours | none | full rows vs `envOut` (no type, colour, position, policy, branch) | |
| Delete environment | `web/project/handler.go:363` | `api/envs.go:58` deleteEnv | `promote.go:219` | `apply:1088-1096` → `teardownEnv` `:1571` | `envops.Teardown` `envops.go:511` | web `managedErr` `:376`; api `rejectManaged` `:64`; config protected flag `apply:741-749` | web last-env `:382`, `RequireConfirm` `:385`. api last-env `:71`, `force` when running `:76`. config last-env: not checked. cli sends `force = --force || -y` (`internal/cli/cmd/promote.go:239-240`), so a scripted `-y` tears down running tiles; web has no running check at all `:386-389`; three force rules | store, `envnet.TearDown`, netpool release, `RemoveApp` (ignored), managed detach/drop, `LoadSchedules` (web `:392`, api `:84`, config `:1098`). Left behind on every surface: `staged_changes` (no FK, `001_initial.up.sql:271-281`) and env `node_positions` (`sqlite/stacks.go:378-381`; stack delete cleans them `:276-282`) | redirect / 204 | A, B |
| Reset config-managed environment | `web/project/handler.go:409` | `api/envs.go:91` resetEnv | `promote.go:255` (`force || rt.Yes` `:274`, same footgun as delete) | -- | `envops.Teardown` | both require `ConfigManaged()` (inverse of delete, same Teardown) | web `RequireConfirm`, Teardown, reload, `planner().RunAll` `:436`. api: Teardown, reload, no replan | store, cluster, proxy, scheduler, plan run (web) | redirect / 204 | A, B |
| Rename environment | -- | -- | -- | `stackconf/moved.go:280` TearDown, `:283` RenameEnvironment, then `restartTile` | config only | n/a | not checked | cluster, store | -- | |
| Set env colour | `web/project/handler.go:2112` | `api/envs.go:162` patchEnv | `promote.go:321 --color` | `apply:958-962` (file value written every apply) | separate; `envcolor.Valid` both | web `managedErr` `:2126`; api none | api accepts a write the next apply overwrites | store | redirect / `envOut` | A |
| Set env apply policy | `web/project/handler.go:841` SaveEnvConfig | `api/envs.go:162` | `promote.go:321 --apply-policy` | `apply:958-962` unconditional | separate | web requires `ConfigManaged()` `:853`; api none | web coerces bad value to "" `:857-860`, also writes `config_branch`, `RunEnv` when branch set `:866`. api 400 `:182`, no plan. config reverts panel/CLI value to "" when file undeclared | store, plan run (web) | redirect / `envOut` | A |
| Set env config branch | `web/project/handler.go:841` | -- | -- | -- (read `apply:698`) | web only | requires ConfigManaged | trim, store, `RunEnv` | store, plan run | redirect | |
| Mark env difference intended | `web/project/envcompare.go:98` | -- | -- | `apply:1145` syncDeclared (key-only rows, wipes declared each apply) | separate shapes (`Intended{Key,Value}` vs `{Key}`) | none | `compareStack` `:20` | store | widget | |
| Copy one tile from reference env | `web/project/envcompare.go:118` → `applier.CopyTile` `:142` | -- | -- | -- | web only | `managedErr` `:124` | `Planner.Snapshot`, `StateToResolved`, keep target Domains | store, deploy, `LoadSchedules` `:146` | widget | |
| Compare environments | `web/project/envcompare.go:46` | -- | -- | n/a | `envcompare.Compare` | none | static envs only | none | HTML | |
| Set stack variable(s) | `web/project/handler.go:2285` SaveStackVar | `api/variables.go:245` → `writeVars` `:116` (replace set) | `internal/cli/cmd/vars.go:95` (`client.go:365` GET, merge, PUT) | `apply:1830` applyVars | separate | none (deliberate `variables.go:257`, `handler.go:2281`) | web `secretNameRe` `:2293`, generate = 32 chars overwriting. api name non-empty only `:120`, masked guard `:123`, generate skips existing `:153`, deletes omitted `:187-195`. config plain only, skips secret names `:1849`, never deletes | web: store, audit, `ClearWaiting`, `replanAsync` `:2302`. api: store, audit, ClearWaiting, no replan. config: store, ClearWaiting, no audit | HTML / `varsOut` | A, B |
| Delete stack variable | `web/project/handler.go:2313` | `writeVars` omit | no `vars unset` verb | -- (`apply:1826`) | separate | none | web audit + `replanAsync`; api audit only | | | B |
| Set env variable(s) | `web/project/handler.go:2578` | `api/variables.go:296` | `vars.go:95 --env` | `apply:1862-1872` | separate | none | as stack row; `ClearWaitingEnv` all three | web + `replanAsync` `:2599`; api none | | A, B |
| Delete env variable | `web/project/handler.go:2613` | `writeVars` omit | -- | -- | separate | none | web `replanAsync`; api none | | | B |
| Set org variable(s) | `web/org/settings.go:414` | `api/variables.go:350` | `vars.go:95 --org` | `orgconf.go:707,723` (no audit; `orgconf.Parse` `:160-241` never validates `vars:` keys) | separate; `varNameRe` `:410` second copy of `secretNameRe` | `RequireOrgWrite` `:421` / `requireOrgWrite` | web regex, api non-empty; `ClearWaitingOrg` both (`:437`, `:180`); no replan either | store, audit (handlers only) | | A |
| Delete org variable | `web/org/settings.go:451` | `writeVars` omit | -- | -- | separate | RequireOrgWrite | audit both | store | | |
| Set plan input (missing stack secret) | `web/project/handler.go:915` SetPlanInput (`:923-930`, `RunAll` `:933`) | -- | -- | -- | web only | loadStack, no managed gate | name regex, value required, `ClearWaiting`, replan | store, audit, plan run | redirect | |
| Set org plan input (missing org secret) | `web/org/config.go:156` SetPlanInput (`:173-184`) | -- | -- | -- | web only | owner | name regex `settings.go:411`, `ClearWaitingOrg`, `orgcfg.Plan` | store, audit, plan | redirect | |
| Set tile plain variables (env blob) | `web/app/handler.go:1161` SaveEnv | `api/variables.go:84` putVars | `vars.go:95 --tile` | `ReplaceTileVars` from `apply:1212,1426` only (no handler maintains that table); boot `envops/backfill.go:23` re-projects env lines with no `ClearWaiting*` `:38-45`; store `projectEnvVars` runs inside every `CreateTile`/`UpdateTile` (`sqlite/tiles.go:62,216`), additive only, so `managedtiles/resources.go:174-181` deletes rows by hand | separate | web `editGate` `:1172` (stages); api `rejectManaged` `:91` | web staged patch `{env,build_args}` or `UpdateTile` then `Enqueue(a,"variables")` if running `:1204`, no upper-env guard. api rewrites blob from non-secret entries `:130-141`, no redeploy (`:109`) | web store/staging + deploy; api store | panel / `varsOut` | A, B |
| Set tile secret variable | `web/app/handler.go:1089` SaveSecretVar | `api/variables.go:84` (`secret:true`) | `--secret` | -- | separate | web none `:1089-1108`; api `rejectManaged` | web name non-empty only, generate overwrites; api generate skips | store, audit; no redeploy | | A |
| Delete tile variable | `web/app/handler.go:1110` DeleteVar | `writeVars` omit | -- | -- | separate | web `editGate` only for blob names `:1136`; api `rejectManaged` | web consults staged patch `currentDesiredEnv` `:1295` | store/staging, audit | | A |
| Read variables (masked / unmasked) | `web/project/handler.go:1857,2211`, `web/org/settings.go:280`, `web/app/handler.go:1029` | `api/variables.go:67,229,280,334` | `vars.go:191` | -- | separate | web unmask on `CanWriteOrg` `:1865`; api scope `secrets:read` `:26` | `blankSecrets`, `audit.PanelViews`, unset-declared-secrets `:1997`; api `toVarEntries`, `auditSecretReads`; cli `vars get` re-masks even for a `secrets:read` key (`internal/cli/cmd/vars.go:1105-1110`, deliberate), `vars pull` `:1129` is the exporter | audit rows | HTML / `varsOut` | |
| Reveal / copy one value | `web/project/handler.go:1917,2178`, `web/org/settings.go:337`, `web/app/handler.go:1064` | folded into get | -- | -- | `audit.ServeValue` | `CanWriteOrg` 403 | none | audit | plaintext | |
| Resolved environment for a tile | `web/project/handler.go:1537` tileRefs (read) | `api/variables.go:203` | `vars.go:245` | -- | `varref.Resolver` | scope | api 422 on unresolvable | audit | `varsOut` | |
| Reference catalogue | `web/app/handler.go:1029` | `api/variables.go:373` | -- | -- | `varref.Catalogue` | none | none | none | HTML / `refSourceOut` | |
| Generate a secret value | `components.VarValue` (`components/varsedit.templ`, 32 chars) | `writeVars` `:148-162` | `--generate [--length]` | `apply:1777` ensureSecrets (skip-if-set, file length/charset; no audit, no ClearWaiting `:1814`). Unaudited secret writers overall: `apply:1802-1816`, `orgconf.go:723-727`, `envops.go:219-231,274-281` (clone), `:548-553` (teardown delete), `managedtiles.go:383-389`, `provision_s3.go:177-178`; every web/api path calls `audit.Record` (`store/audit/audit.go:185-190`) | `secrets.Generate` shared; policy per surface | n/a | web always overwrites a live value; api/config never overwrite | store | | A, B |
| Mint share / drop link | `web/project/handler.go:2337` | -- | -- | -- | `sharelink.Mint` `config/sharelink/sharelink.go:46` | `RequireOrgWrite` `:2348` | kind, fields regex, ttl 1..720h, window 0..60m, passphrase, all in the handler `:2351-2390` not the package; links outlive minter and org move (`web/org/handler.go:148` never touches `secret_links`) | store | one-time URL | |
| Revoke share link | `web/project/handler.go:2415` | -- | -- | -- | web only | RequireOrgWrite | scope check `:2425` | store | | |
| Public drop-link submit | `web/sharepub/handler.go:41` → `sharelink.Submit` `:117` | -- | -- | -- | web only, unauthenticated | token | all fields required, `BurnDropLink` tx | store, audit `:154`; no `ClearWaiting`, no replan (`:129-131`) | HTML | B |
| Public share-link reveal | `web/sharepub/handler.go:41` → `sharelink.Reveal` `:164` | -- | -- | -- | web only | none | window vs burn | store, audit | HTML | |
| Env network claim / release | -- | -- | -- | -- | claimed by `infra/deploy`, released by `envops.Teardown` `envops.go:517,568-583` | n/a | 13 handler sites reach `envnet` directly for names/reads (`api/logs.go:29`, `api/lifecycle.go:36,54`, `api/forward.go:50,60,64`, `api/apps.go:485`, `api/helpers.go:130`, `web/components/panel.go:74,80`, `web/app/handler.go:114,335,380,852,1632`, `web/deployment/handler.go:81`, `web/project/handler.go:1200`, `web/project/placement.go:48`) | cluster | | |

### 1.5 Stacks + plans + staging + deploys + releases

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| List stacks | org canvas | `api/stacks.go:13` | `internal/cli/cmd/stack.go:40` | -- | separate | `orgAllowed` `:20` | filter | store | `stackOut` vs `stackRow` (drops description) | |
| Create stack | `web/project/handler.go:142` | `api/stacks.go:28` | `stack.go:79` | `orgconf.go:854` applyStack | three copies hand-build `repo.Stack` + production env (web `:161-183`, api `:39-48`, orgconf `:862-870`); store also inserts `HomeEnv` (`store/repo/sqlite/stacks.go:146-162`) | web `RequireOrgWrite` on cookie org `:149-158`; api `orgForCreate` `helpers.go:27`; config plan | nobody checks empty slug or duplicate slug (`UNIQUE (org_id, slug)` `001_initial.up.sql:269` surfaces as 500; renames pre-check it `api/lifecycle.go:359`, `runner.go:424`); `CreateStack` already inserts the home env (`sqlite/stacks.go:144-166`) and all three add `production` by hand; orgconf idempotent `:856-861`, binding `:896-901`, `RunAll` `:904` | store ×2; config planner | redirect / 201 `stackOut` | A |
| Rename stack | -- | `api/lifecycle.go:337` patchStack → `Applier.RenameStack` `apply:536` then second `UpdateStack` `:375` | `internal/cli/cmd/lifecycle.go:574` | `orgconf/moved.go:61` (`:77-78` writes Name, Slug only) | two impls | api `rejectManaged` `:346` | api: trim, empty 400 `:355-358`, dup 409 `:359-361`. moved: skip if target exists `:72-74`, no teardown, no restart, display name becomes slug; `apply:530-535` names the bug this avoids | api: store, teardown, deploy; config: store only | `stackOut` / plan | B |
| Delete stack | `web/project/handler.go:445` | `api/stacks.go:52` | `stack.go:106` | `orgconf.go:761-782` | three copies of `TeardownStack` + `DeleteStack` | web `RequireConfirm` `:453`, managed allowed `:446-448`; api `requireOrgWrite`, no managed guard | typed confirm web only | store, teardown (RemoveApp ignored), netpool; no `jobs.LoadSchedules` on either surface (`envops.go:598`) | redirect / 204 | B |
| Move stack to another org | `web/org/handler.go:148` MoveStack | -- | -- | -- | web only | `RequireOrgWrite` both orgs `:160,170` | dup slug in target `:179` | `SetStackOrg`; whether service names embed org slug: not checked | redirect | |
| Bind / unbind config repo | `web/project/handler.go:702` SaveConfigBinding | -- | -- | `orgconf.go:895-902` | two writers | loadStack | web: connector in org `:718-721`, repo normalised `:727`, unbind clears four fields `:710`, drops staged rows every env `:736-742`, plan `:743`. orgconf: binding + `OrgDeclared`, no staged drop, connector written as given (`refBinding` `:621-630`, no org check), no repo normalisation so `https://github.com/o/r.git` and `o/r` are different bindings, plan `:904` | store, staging delete (web), planner | redirect / plan | B |
| Set stack `ui_edits` mode | -- | -- | -- | `apply:474` applyUIEdits | config only | n/a | none | `UpdateStack` | -- | |
| Plan now | `web/project/handler.go:748` → `runPlan` `:762` | `api/config.go:100` planStack | `internal/cli/cmd/plan.go:212` | n/a | `Planner.RunAll` | web loadStack, no bound check; api `requireConfigStack(write)` `config.go:38`: 409 unbound, 422 no file | web `ErrNotBound` → generic flash `:768-769`, reports `plans[0]` only `:770-773`; api returns every plan `:121-127` | plan rows; no canvas nudge either surface | redirect / 201 | A |
| Approve plan | `web/project/handler.go:780` | `api/config.go:169` | `plan.go:299` (destructive confirm `:314-318`) | n/a | `stackconf.EnqueueApply(force=true)` `stackconf/job.go:112` | web `loadPlan` `:827` (ownership only); api `requirePlan(write)` → 409 when unbound `:49-51` | pending check web 400 `:787`, api 409 `:174`; cli reads the plan and confirms only when `Destructive` (`plan.go:671-680`) | workqueue; no nudge | redirect / 202 | A |
| Reject plan | `web/project/handler.go:808` | `api/config.go:189` | `plan.go:328` | n/a | direct `SetConfigPlanStatus("rejected")` (plan status written from 7 sites, no owner: `apply:328,376`, `job.go:86`, `api/config.go:198`, `web/project/handler.go:817`, `api/orgconfig.go:140`, `web/org/config.go:230`, `web/org/setup.go:430`) | same gate split | pending check | store | | |
| Preview plan | -- | `api/config.go:130` | `plan.go:168` (cli `readBundle` `:433-465`: include relative, one level, 1 MB raw cap) | n/a | single | `requireConfigStack(write)` `config.go:39-52` while the route is registered under `config:read` (`v1.go:244,281`; org twin `orgconfig.go:55`): a viewer's `config:read` key gets 403 where the catalogue promises read | 2 MB body cap `:140`; includes looked up only in the posted map (`runner.go:337-342`) | none | `planDetailOut` | A |
| List / show plans | `web/project/handler.go:945` PlanView, `web/project/releases.go:107` | `api/config.go:69,90` | `plan.go:244,280` | n/a | separate view builders (`stackPlanCfg :878` vs `toPlanDetail config.go:235`) | loadStack / read | none | store | `PlanViewCfg` vs `planDetailOut` | |
| Export stack config | `web/project/releases.go:347` | `api/config.go:261` | `internal/cli/cmd/defaults.go:187` | n/a | identical copies of Snapshot → StateToResolved → EnvOrder → ExportYAML (`releases.go:353-370` = `config.go:268-288`), ad-hoc `Planner{Store}` | `resolveStackSlugs` / `requireStackAccess` | none | none | yaml | |
| Staging review / apply / discard | `web/project/staging.go:42` StagingReview (`applier.StagedPlan`), `:62` StagingApply (`ApplyStaged(force=true)`), `:81,99` Discard (direct `DeleteStagedByEnv`/`DeleteStagedChange`) | -- | no CLI verb | `stackconf/staging.go:118` ApplyStaged | web only | apply: `managedErr` `:59`; review: loadStagingEnv; discard: none | none | apply job; no canvas nudge | flash | |
| Trigger deploy | `web/deployment/handler.go:73` | `api/apps.go:480` deployApp | `internal/cli/cmd/deploy.go:92` (+ poll `:162`) | `apply:77` deploy (`isUpper`/`ownRepo` `:88-96`); push `web/prhook/handler.go:399-406`; imagewatch `infra/imagewatch/watcher.go:175` | `engine.Enqueue` `infra/deploy/engine.go:211` (refuses volume `:212`) | web `RequireStackAccess` write; api `requireTile(write)` | web refuses cron `:78-80` + upper env `:81-83`; api upper env only `:485-487`; engine builds non-KeepAlive kinds `engine.go:518,655`; two upper-env rules (`apply:59` isUpper vs `envnet.UpperEnv` `envnet.go:110`); trigger strings "manual"/"api"/"config". imagewatch `watcher.go:175` enqueues with no `UpperEnv` check: an upper-env tile with `update_policy=auto` is the one tile rebuilt outside a promotion (`apply:141` is the sanctioned path) | deployment row, workqueue | redirect / 202 | A |
| Rollback | `web/deployment/handler.go:92` | `api/lifecycle.go:102` | `lifecycle.go:99` | by design | `engine.EnqueueRollback` `:235` | write both | tag required; api trims `:108-111`, web raw `:97-101`; neither checks `UpperEnv` or kind | store, workqueue | redirect / 202 | A |
| Cancel deployment | `web/deployment/handler.go:131` | `api/lifecycle.go:119` | `lifecycle.go:429` | n/a | `engine.Cancel` `:301` | web `loadDeployment` → `RequireStackAccess` (write on POST, `middleware/orgctx.go:258-264`); api `loadDeployment` `api/apps.go:570-579` → `requireTile(c, id, false)`, no write role | read-only member with `ScopeAppsDeploy` key can cancel; web skips tenancy when the tile row is gone (`web/deployment/handler.go:44-52` `if app != nil`), so Detail/Status/Stream/Cancel run on the deployment id alone (deployments cascade with tiles `001_initial.up.sql:91`, so the branch is likely dead, not verified) | engine cancel | redirect / `deploymentOut` | A |
| Deployment status / detail / list / logs | `web/deployment/handler.go:109,118,158`; `web/app/handler.go:542,575` | `api/apps.go:540,561`; `api/logs.go:116` | `lifecycle.go:141,378,404`; `deploy.go:162` | n/a | separate | read | live-state set: web `queued`/`running` only `:123,198` (so `waiting_ci` ends poll and SSE); cli terminal `done`/`error`/`cancelled` `deploy.go:205`; engine cancels `waiting_ci` `engine.go:310`; api releases counts it building `releases.go:86`. Four copies. Boot `SweepStaleRuns` (`sqlite/deployments.go:55-69`, `main.go:315`) writes queued/running → `error` and `building` tiles → `stopped`; a user cancel writes `cancelled` (`engine.go:313`); `waiting_ci` is not swept; deployments have no atomic claim unlike work items (`sqlite/workitems.go:48-76` vs `engine.go:124,310,456`) | none | page / `deploymentOut` / `deployResult` | A |
| List releases | `web/project/releases.go:140` → `commitlog.go:284,443` (git log overlay, only importer of `infra/gitlog`) | `api/releases.go:33` (`:50-94`) | `internal/cli/cmd/promote.go:17` | n/a | separate derivations, same rules | read | window 5, static envs, newest `done` sha | store; web GitHub fetch `commitlog.go:146` | `releaseRow` vs `releaseOut` | |
| Promote commit to upper env | `web/project/releases.go:292` PromoteCommit (dialogue `:193`) | `api/releases.go:127` promote | `promote.go:63` | by design; job half `stackconf/job.go:96-105` | plain: `Applier.Promote` `apply:155` inline on request ctx. With plan: both hand-build `ApplyJob` and `work.Enqueue(..., stackID, ...)` (web `:322`, api `:158`), bypassing `EnqueueApply` (dedupe `cp.ID` `job.go:112-115`). Rung rule ×3 (`releases.go:171-190` = `api/releases.go:170-189`, `apply:163-179`) | loadStack write / `requireStackAccess` + `requireOrgWrite` | web `Force:true` `:323`, no `work==nil` check; api `in.Force` default false `:159`, 503 no queue `:155-157`; neither checks `cp.Status=="pending"`. cli `pickRelease` (`promote.go:131-196`) is the only resolver of `head`, `latest`, `--from <env>`, short sha with an ambiguity rule; api and `Applier.Promote` take any non-empty string and never check the commit is built (`releases.go:137-144`, `apply:156-183`); three force policies (web true, api body, cli `--force` `:124`) | workqueue or engine | redirect / 202 `promoteOut` | A |
| Park a push deploy behind CI, release it | `web/prhook/handler.go:399-404` Park; janitor `infra/cigate/cigate.go:39` | -- | -- | `wait_for_ci` tile key | single | webhook HMAC | park when `WaitForCI && ConnectorID != "" && After != ""`; cigate 30m timeout `:58`, no-connector fail `:64-67` | deployment `waiting_ci`, `Release`/`FailWaiting`, `notifier.Push` `:117` | -- | |
| Image watch and auto-update | `web/settings/handler.go:117` SaveImageWatch (`interval >= 0` `:118-123`); watcher `infra/imagewatch/watcher.go:53` | -- | -- | -- | single | adminOnly | `watchable` `:100-112`, `auto`: managed → `DB.Deploy`, cron/function → `PullImage`, service → `Enqueue("image-update")` `:152-179`; `image_digest` has three writers with three triggers (`main.go:420-424` OnFinish image+done, `managedtiles.go:497-500` every managed Deploy, `watcher.go:118-127,183-187`); check history rides `cron_runs` under `watch:<tile>` `:218`, pruned only when some cron finishes (`jobs.go:291`) | setting + digests, `notifier.Push`, engine | redirect | A |
| Apply run policy (kind registry) | `web/project/handler.go:1186` (KeepAlive only) | `api/apps.go:103,113,243,444` | via api | `stackconf.go:1011` | `config/runpolicy/runpolicy.go:29`; also `engine.go:518,655`, `infra/metrics/reconcile.go:26` | n/a | web tile settings does not consult it | none | | |

### 1.6 Domains

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| List a tile's domains | `web/app/handler.go:539,561` | `api/domains.go:18` | `internal/cli/cmd/tile.go:546` | export `stackconf/serialize.go` | separate reads | none | none | store | `domainOut` (`types.go:210`) drops Auto, Rule, Priority, Middlewares, Position, cert; cli `Domain` (`client.go:405`) drops force_https, redirect_to | |
| Add a custom domain to a tile | `web/app/handler.go:1648` CreateDomain; `web/db/handler.go:572` (managed) | `api/domains.go:35` createDomain (accepts managed id) | `tile.go:498` (host, --port, --no-https only) | `apply:1582` syncDomains; plan `stackconf/plan.go:812,332,892`; `stackconf/middlewares.go:158` | three rule sets | web app `editGate` `:1654` (stages); web db `rFU` `:62`; api `rejectManaged` `:40` | web app: rule/priority/middlewares + `checkMiddlewares` `:1668`, redirect port 80 `:1681`, wildcard needs `dns_provider` `:1688`, same-tile rule exception `:1704`, `CheckOrgSquat` `:1714`, HTTPS + ForceHTTPS follow checkbox `:1741-1746`. web db: `SpeaksHTTP` `:582`, engine port `:600`. api: redirect port left 0 `:47-53` (rendered `http://alias:0` `infra/proxy/proxy.go:447`), https/force default true `:54-63`, no rule/priority/middlewares, no squat, no wildcard, no kind check. config: squat `plan.go:348`, middleware refs, redirect 80 `apply:1639`, recreate-on-update `:1625-1628`, no wildcard check. CLI `--no-https` gives https=false, force=true. SQL `https`/`force_https DEFAULT 1` dead, `CreateDomain` writes both (`001_initial.up.sql:107,114`, `sqlite/deployments.go:83-90`); store uniqueness is `(host, path, rule)` `:370`: web app mirrors it `:1700`, api `:64-71` and web db `:593` refuse any existing host+path | store, proxy write (`syncProxy` `app:1918` / `db:641` / `domains.go:83` / `apply:1674`), staging (web app) | panel / `domainOut` | A |
| Generate the auto domain | `web/app/handler.go:1762` | `api/lifecycle.go:159` | `internal/cli/cmd/lifecycle.go:452` | `plan.go:334,338` AutoHost then syncDomains | `envops.EnsureAutoDomain` `envops.go:399` (web, api); five `envops.Ops{}` literals (`api/lifecycle.go:175`, `api/envs.go:194`, `web/app/handler.go:1778`, `web/project/handler.go:2641`, `web/server.go:612`) | `rejectManaged` both `:1769`/`:165` (bypasses staging where CreateDomain stages) | kind==service && port | store, proxy `envops.go:462` | flash / `domainOut` | |
| Flip HTTPS / force_https | `web/app/handler.go:1800` ToggleDomainHTTPS (https only) | `api/lifecycle.go:194` patchDomain | `lifecycle.go:482 --https` | `stackconf.go:521-527` via syncDomains `apply:1615` | separate | web `editGate` `:1806` (stages); api `rejectManaged` `:205` | web: no `d.TileID == a.ID` check `:1810-1813`, rewrites whatever `:domainID` names then `syncProxy(a)`. api resolves tile from domain `:201`. force_https unreachable from web and CLI | store, proxy, staging | panel / `domainOut` | A |
| Set / clear custom certificate | `web/app/handler.go:1846` SetDomainCert | `api/lifecycle.go:194` (cert_pem/key_pem) | `lifecycle.go:482 --cert/--key/--clear-cert` | -- | same `tls.X509KeyPair` check `:1856`/`:221` | web none (`middleware/managed.go:11`); api `rejectManaged` | web checks ownership `:1852` | store, proxy | flash / `domainOut` | A |
| Remove a tile domain | `web/app/handler.go:1877`; `web/db/handler.go:617` | `api/domains.go:90` | `tile.go:583` | `apply:1663` | separate | web app `editGate` `:1883`; web db `RU`; api `rejectManaged` `:100` | web app live branch `DeleteDomain(c.Param("domainID"))` with no ownership check `:1908` (staged branch checks `:1892`); web db checks `:626`; api `GetDomain` then `requireTile(d.TileID, write)` `:92-99` | store, proxy, staging | -- | A |
| List domain resources | org `web/org/settings.go:473` (`:505`); stack `web/project/handler.go:2443` (`envops.VisibleDomainResources`); server `web/server/handler.go:104` | `api/domainresources.go:140` | `internal/cli/cmd/domain.go:28` | export `orgconf/export.go:77`, `serialize.go:37` | separate filters | none | three web filters; api tenancy | store | `domainResourceOut` drops Declared; cli drops ACMEEmail, Declared | |
| Create a domain resource | org `web/org/settings.go:522` (+ wizard `web/server.go:243`); stack `web/project/handler.go:2460`; server `web/server/handler.go:196`; wizard default `web/org/setup.go:210`; seed `cmd/stackrd/seed.go:28` | `api/domainresources.go:214` | `domain.go:52` | stack `apply:402` (plan `plan.go:533`); org `orgconf.go:799` (plan `:443-486`) | `ValidateResourceHost` `envops.go:287`, `HostTaken` `:302` shared by web+api, not config | web org `RequireOrgWrite` + `ConfigManaged` 409 `:530-535`; web stack `managedErr` `:2470`; web server adminOnly only; api tenancy `:168` + `rejectManagedOwner` `:280` | squat only web org `:545` (orgconf `:461-482,811-823` none, creates `Declared=true` with the file's acme `:815-819`); api accepts `acme_email` and drops it `:244`; web has no ACME field; instance owner id = `sv.ID` (web `:215`) vs `GetServer("local").ID` (seed `:37`) vs caller string (api `:178`), latent C | store only | flash / `domainResourceOut` | A |
| Set a domain resource's ACME account | -- | `api/domainresources.go:306` patchDomainResource | -- | org `orgconf.go:826-830`; stack `apply:439-445` copies IncludeEnvOnDefault + Declared only, `plan.go:550-559` never diffs acme_email | separate | api tenancy only, no `rejectManagedOwner` (siblings have it `:241,:267`) | api lower+trim, `EnsureTraefik` synchronous, error ignored `:338`; stack file acme change is a silent no-op; `orgconf.go:826-833` writes the row and calls nothing though its plan note promises a traefik restart (`:477`) | store, proxy static (api) | `domainResourceOut` | B |
| Set include_env_on_default | -- (create-time only) | -- | -- | stack `apply:439-442`; org `orgconf.go:826-829` | config only | n/a | | store | | |
| Delete a domain resource | org `web/org/settings.go:567` (own rows `:585`); stack `web/project/handler.go:2497` (`:2512`); server `web/server/handler.go:226` (deletes any id, no level/owner/managed check) | `api/domainresources.go:252` | `domain.go:94` | stack `apply:449-455` (Declared only); org `orgconf.go:834-840` | separate | web org write + 409 `:577`; web stack `managedErr` `:2504`; web server adminOnly only; api tenancy + `rejectManagedOwner` `:264-269` | api re-runs create rules | store; generated hostnames left in place | -- | A |
| Basic-auth URL protection (cascade `protect*`, tile `basic_auth_*`) | server `web/server/handler.go:312` (Resync `:336`); org `web/org/registry.go:257` (`:284`); stack `web/project/handler.go:1022` (`:1041`); env `:2542` (`:2568`) | `api/settings.go:114` (Resync `:160`); tile patch: calibration B | not checked | `stackconf.go:195-196`, `apply:970`; `orgconf/export.go:121-123`; tile `apply:1737-1738,1446-1491` | rule once in `infra/proxy/protect.go:35`; every writer must remember Resync | per level | writers persist + Resync | store, proxy resync | -- | |
| Declare stack `proxy.middlewares` | -- (web validates refs only `web/app/handler.go:1928`) | -- | -- | `middlewares.go:205` → `proxy.WriteStackMiddlewares` (only writer besides `Resync` `proxy.go:614`) | config only; ref check twice (`app/handler.go:1928`, `middlewares.go:158`) | n/a | union-then-final write | store, proxy | -- | |
| Root domain bootstrap | wizard prefill `web/org/setup.go:446` | -- | installer `internal/installer/steps.go:152` | `cmd/stackrd/seed.go:28` | one path | none | three grammars: installer `CheckRoot` (`internal/installer/answers.go:518-549`) refuses IP, `localhost`, single label, numeric TLD, labels over 63 chars; seed only `ToLower` + `TrimSpace` + `HostTaken` (`seed.go:33-46`); `envops.ValidateResourceHost` `:287-299` (no scheme, slash, port, space); `proxy.routablePanel` `infra/proxy/proxy.go:797-802` refuses IP and localhost for the panel host only. `localhost` or `intranet` is refused by the installer and accepted by the panel | store | -- | A |
| Remove a tile's route on tile delete | `web/app/handler.go:1633` | `helpers.go:128` | via api | `apply:1556`, `moved.go:308`, `envops.go:519` | five sites, same one-liner, error ignored everywhere | tiles | domain-row cascade not checked | proxy | -- | |

### 1.7 Backups

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| List backup destinations | `web/settings/handler.go:168` (org page `web/org/settings.go:244`, admin `:183`); tile tab `web/backups/handler.go:117-128` | `api/backups.go:30` | `internal/cli/cmd/backup.go:396` | -- | three visibility rules | org write / adminOnly; api `ScopeBackupsRead` + `orgAllowed` | web org: own rows `settings/handler.go:175`; tile tab `VisibleTo` `:125`; api own + shared globals, admin all `:37-43` | store | `[]repo.BackupDestination` vs `destinationOut` | |
| Create backup destination | `web/settings/handler.go:296` | `api/backups.go:49` | `backup.go:423` | -- | probe shared (`infra/backup/backup.go:128`) | web `destOrg` `:145` (org route incl. `RequireOrgSetup`) or adminOnly; api admin for global `:64-67`, `requireOrgWrite` `:70`, no setup-done check | web trims `:304-308`, api stores raw `:58-61` (`ResolveNamed` `authz.go:85` misses a name with a stray space); S3 probe before insert both | store, S3 probe | redirect / `destinationOut` | A |
| Delete backup destination | `web/settings/handler.go:336` | `api/backups.go:84` | `backup.go:481` | -- | separate | scope checks `:348-350` / `:91-98` | `backups.destination_id ON DELETE CASCADE` (`001_initial.up.sql:52`) drops every schedule; neither surface reloads the backup scheduler (`:351-357`, `:101-104`); orphaned cron entries fire `Start` on missing rows every tick (`backup.go:100-102`, `work.go:109-112`) | store cascade | redirect / 204 | B |
| Share / unshare server-wide destination | `web/settings/handler.go:680` | `api/backups.go:416` | `backup.go:511` | -- | `destinationUsers` duplicated verbatim (`:712-738` vs `:451-480`) | adminOnly / `isAdmin` | `Global()` check; refuse unshare while in use | store | | |
| List tile backup schedules | `web/backups/handler.go:180,104` | `api/backups.go:138` | `backup.go:59` | plan diff `stackconf/backups.go:81` | separate | web `RequireStackAccess` (no setup gate); api `requireTile(false)` incl. `orgReady` | web derives kind, `VolumeFor` text, visible dests, slices, per-row runs | store | rich view vs `[]backupOut` | |
| Create tile backup schedule | `web/backups/handler.go:189` | `api/backups.go:154` | `backup.go:105` | `stackconf/backups.go:178` (`:217-230`); parse `stackconf.go:934` validateBackup | `backup.Validate` (`authz.go:49`) web+api; config does not call it; dest resolution `authz.go:19,77` | web `RequireStackAccess`; api `requireTile(true)`; no `editGate`/`rejectManaged` on either though `backup:` is file-owned (`backups.go:3-8`); web comment stale `:314-317` | kind: web dump on managed unless volume `:217-219`, api any body kind `:181-187` (accepts dump on non-managed, fails at run `backup.go:318-320`), config derives `:39`. `VolumeFor`: web `:223-228`, api `:197-201`, config none. keep default web 7 `:213`, others 0 (`keep_latest DEFAULT 7` `001_initial.up.sql:57` dead, `CreateBackup` writes it `sqlite/backups.go:71-75`). tz rule twice (`stackconf.go:950` vs `models.go:915`) | store; reload web `:236`, api `:208`, config `apply:988` | fragment / `backupOut` | A |
| Update tile backup schedule | `web/backups/handler.go:242` | `api/backups.go:230` | `backup.go:172` | `stackconf/backups.go:232-235` (`cur[0]` only) | as create | as create; api `requireBackup(true)` admits panel row to admins `:124-130` | web overwrites cron/tz from form `:262-263`; api patches non-empty only `:263-277` (tz cannot be cleared); config plans and applies `cur[0]` only (`:119,232`), extra panel schedules invisible and deleted on block removal `:209-215` | store; reload | fragment / `backupOut` | A |
| Delete tile backup schedule | `web/backups/handler.go:280` | `api/backups.go:288` | `backup.go:229` | `stackconf/backups.go:209-215` | separate | `RequireStackAccess` / `requireBackup(true)`; no managed gate | implicit deletes via tile cascade (`001_initial.up.sql:51`) from `api/helpers.go:134`, `web/app/handler.go:1640`, `web/db/handler.go:467`, `apply:1566`, `orgconf.go:952,992`: none reload the backup scheduler | store; reload on explicit paths only (`:288`, `:297`, `apply:988`) | fragment / 204 | B |
| Run backup now | `web/backups/handler.go:294` | `api/backups.go:329` | `backup.go:288` | -- | one impl `infra/backup/work.go:105` | as above | api 503 on nil service `:334`; web nil deref `:299`; cron entry `backup.go:98-103` uses the same `Start`. Run consumer `quiesce` (`backup.go:404-417`) scales the service to 0 and back with no `tiles.status` write, so the reconciler (1.11) flips `stopped` then `running` per backup | run row, workqueue `backup.run` | fragment / 202 | B |
| List backup runs | `web/backups/handler.go:163` (10) | `api/backups.go:313` (50) | `backup.go:255` | -- | separate | as list | limit differs | store | | |
| Restore from a run | `web/backups/handler.go:309` | `api/backups.go:348` | `backup.go:311` | -- | one impl `work.go:128` → `backup.go:639` | `ScopeBackupsRestore` (api); no managed gate (deliberate) | api re-checks `run.BackupID` `:359-361` that `restorable` does `:641`; cli polls terminal `done`/`error`/`cancelled`/`superseded` (`backup.go:337-356`, = `WorkItem.Done` `models.go:741`). Boot recovery `backup/work.go:165-187` interrupted at `stepRestoring` leaves the service stopped with no status write (web stop `app/handler.go:335-341` writes `stopped` and nudges) | workqueue `backup.restore` | fragment / `restoreOut` | B |
| Read restore status | via `configView.Restore` | `api/backups.go:375,386` | `backup.go:350` | -- | one impl `work.go:144` | | none | store | | |
| Panel self-backup save / run / delete | `web/settings/handler.go:216,261,276` | create refused (`api/backups.go:188`); patch/run/delete by id if known `:124-130`; nothing lists it | -- | -- | web only for create | adminOnly | singleton scan `:198-210`, dest must be `Global()` `:222-225`, keep 0 `:237-239`, `Validate` `:241` | store; reload `:253-254,285-286`; `backup.go:471` writePanelArchive | redirect | |
| Retention (keep count) | field | field | `--keep` | `keep:` | one impl `backup.go:602` prune | n/a | default drift: web tile 7, everything else 0; DB default 7 unused | S3 prune | | |

### 1.8 Orgs + members + auth

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Create org (draft) | `web/org/handler.go:55` | -- | -- | -- | web only | adminOnly `web/server.go:310` + inline `IsAdmin` `:68` | mode ∈ {config,ui}; reuse unfinished draft `unfinishedDraft :108`; placeholder name `setup.go:398`; creator owner | `CreateOrg` + `UpsertOrgMember` `:90-97`; cookie `:190` | | |
| Pick setup branch | `web/org/setup.go:415` | -- | -- | -- | web only | owner | ui mode rejects pending org plan, clears binding `:428-435` | store | | |
| Wizard step gates | owner: `web/org/setup.go:93,171,416`, `web/org/config.go:51`, `web/org/members.go:68,235`; member: `/setup/domain` (`web/org/settings.go:529`), `/setup/connector` (`web/settings/handler.go:441`) | -- | -- | -- | web only | `setupOpen` (`setupgate.go:403-405`) waives the draft gate for the whole `/orgs/:slug/setup` prefix, each handler picks its own role | a member of a draft org cannot see the wizard yet can post its domain and connector steps | store | | A |
| Finish setup (activate) | `web/org/setup.go:170` | -- | -- | -- | web only (only writer of `setup_done_at`) | owner | placeholder refusal `:179`; `ensureDefaultDomain` `:208` (direct `CreateDomainResource` `:223`) | `UpdateOrg`, domain resource | | |
| Discard / delete org | `web/org/members.go:164` | -- | -- | -- | web only | owner `:45`; typed confirm for finished orgs `:179-183` | refuse if stacks `:184-197`, last org unless draft `:205`, `deploy.BuilderFor` `:214` | `DeleteOrg`; `rt.RemoveBuilder`; `backup_destinations.org_id` cascade with no scheduler reload; FK cascade covers stacks, connectors, invites, members, destinations, org storage, registry creds (`001_initial.up.sql:259,75,130,177,29,448,531`), `DeleteOrg` adds `node_positions` (`sqlite/stacks.go:107-133`); left behind: org `variables`, org `domain_resources`, `org_config_plans` (`:473-484`), `annotations`, `graph_groups`, `secret_links` | | |
| Rename org | `web/org/members.go:66` (+ wizard `web/server.go:235`) | -- | -- | `orgconf.go:424-443` plan, `:660-673` apply | separate | owner / plan approval | web: slug rules `:90-96`, anti-squat over other orgs' resources `:101-116`, registry-images guard `:122-129`, logo `:136-147`. config: collision + registry only `:430,440,664`, no anti-squat | `UpdateOrg` | | A |
| Switch active org | `web/org/handler.go:138` | -- | -- | -- | web only | `InOrg` | cookie | cookie | | |
| List orgs | `middleware/orgctx.go:58` | `api/slugpath.go:109` listOrgs | `internal/cli/cmd/lifecycle.go:542` | -- | separate | none | web prefers finished org `:81-104`; api returns drafts while `orgAllowed` `auth.go:230` drops them elsewhere and `requireOrg` 409s (`orgReady` `:257`) | none | | |
| Invite member (by email) | `web/org/members.go:234` (+ wizard `web/server.go:244`) | `api/members.go:61` addMember | `internal/cli/cmd/members.go:57` | -- | separate `newInvite` (web `:259`, api `:210`) | owner both (`ownedOrg` / `requireOrgOwner` `:26`) | web: role coerce `:241`, expiry 1..365 else 14 (`settings.go:598`), no member-exists check. api: 409 if member `:77-86`, expiry ≤0 → 7 `:211`, no upper bound | web: `CreateInvite` + `mailInvite` `:285` (`MailFailed` in memory only `:297-298`). api: store only; `*API` has no Mailer (`v1.go:57`) | flash / `inviteOut` | A, B |
| Mint invite link | `web/org/members.go:391` | `api/members.go:175` | `members.go:215` | -- | separate | owner | as above minus member check | web store + mail; api store | | B |
| Change member role | `web/org/members.go:318` | `api/members.go:94` | `members.go:97` | -- | separate incl. last-owner guard (`:377` / `:145`) | owner | web bad role 400 `:327`; api silently `member` `:108` (`validRole` `:232-237`); cli checks `--role` non-empty only (`internal/cli/cmd/members.go:358`), so `--role admin` prints "is now member" `:376` | `UpsertOrgMember` | | A |
| Remove member | `web/org/members.go:347` | `api/members.go:121` | `members.go:132` | -- | separate | owner | web deletes without 404 `:355-360`; api 404 `:128` | `DeleteOrgMember` | | A |
| List members / invites | `web/org/settings.go:161` | `api/members.go:41,159` | `members.go:22,175` | -- | separate | member; invites owner | web merges into `peopleRows`, expiry state `:502-505`; api `Used` only | none | | |
| Revoke invite | `web/org/members.go:422` | `api/members.go:192` | `members.go:256` | -- | separate | owner; both check `inv.OrgID` | none | store | | |
| Resend / re-invite | `web/org/members.go:436,460` | -- | -- | -- | web only | owner | mailer present `:441`, not expired `:444`; new row before old delete `:465-471` | store, mail | | |
| Accept invite / join | `web/auth/invite/handler.go:47,61,118` | -- | -- | -- | web only | rate limit `web/server.go:662` | unused+unexpired `:36`, address match `:79,120`, password len≥8 + confirm `:91-96`, `AuthService.Register` `:100`, membership upsert only if absent `:124` | `UpsertOrgMember`, `MarkInviteUsed`, session | redirect | A |
| Register (first boot) | `web/auth/register/handler.go:62` | -- | -- | -- | `service/auth.go:34` Register | `firstBootOnly` `web/server.go:654-656` | `validate.PasswordStrength` `:44`; service: first user admin `:47-50` | `CreateUser`, session | redirect | A |
| Login | `web/auth/login/handler.go:53` | `api/auth.go:149` KeyAuth | `internal/cli/login.go:26,37` | -- | web `AuthService.Authenticate` `service/auth.go:104`; api own key lookup | rate limit | web lowercases `:58`; `KeyAuth` `api/auth.go:163-166` checks only that the user row exists; `Active` is checked only in `Authenticate` (`service/auth.go:117`), the session loader `web/server.go:162-164` returns the row regardless (confirmed round 2, see Enable / disable user) | session | | |
| Logout | `web/auth/login/handler.go:92` | -- | `internal/cli/cmd/auth.go:45` | -- | different things | -- | delete session `:98-104` | session | | |
| Change password | `web/account/handler.go:114` | -- | -- | -- | `AuthService.ChangePassword` `service/auth.go:76` | self | len≥8 + confirm `:121-128` in handler; service has no strength rule | `UpdateUser` | | A |
| Edit profile | `web/account/handler.go:72` | -- | -- | -- | web only | self | email trimmed, not lowercased `:78`; login/register/invite lowercase; `GetUserByEmail` exact (`store/repo/sqlite/users.go:29`); collision `:83-87`; `users.email UNIQUE` is case-sensitive (`001_initial.up.sql:341`) and the store never folds (`users.go:559-578`); lowercased at eight handler sites (`register/handler.go:67`, `login/handler.go:58`, `invite/handler.go:74,83`, `api/members.go:70,184`, `web/org/members.go:244,400`) | `UpdateUser` | | A |
| Create API key | `web/account/handler.go:194`; CLI approve/exchange `web/cli/handler.go:86,139` | -- | `internal/cli/login.go:107` | -- | two copies: scope filter (`account:201-209` vs `cli:96-104`), `CanGrantWrite` (`account:260` exported, `cli:55,96` does not call it), token mint (`account:214` vs `cli:153`) | `ReadOnlyGuard` exempt `orgctx.go:281`; write scope by active-org cookie | drop unauthorised scopes silently; cli one-time code TTL 2m `:28`; `Exchange` `web/cli/handler.go:139-162` is unauthenticated behind `authLimit` (`server.go:186-189`) and a third minting site `:153-161`. `CanGrantWrite`/`grantable` read `CanWrite` of the active cookie org (`orgctx.go:148-154`) while keys are user-scoped with no org column (`repo.APIKey`, `web/cli/handler.go:153-160`): member of A, viewer of B mints write scopes on A and uses them on B; only the per-request `requireOrgWrite` stops it and `cancelDeployment` forgot (1.5). Demotion leaves scopes in place. No api or cli key list or revoke | `CreateAPIKey` | page / `{key}` | A |
| Revoke / list API keys | `web/account/handler.go:232,141` | -- | -- | -- | web only | owner or admin `:234` | none | store | | |
| Grant / revoke server admin | `web/settings/handler.go:528` | -- | -- | -- | web only | adminOnly | last-admin guard `:534-546` | `UpdateUser` (bypasses AuthService) | | |
| Enable / disable user | `web/settings/handler.go:563` | -- | -- | -- | web only | adminOnly | admin cannot be disabled `:569`; flips `Active` and nothing else `:563-580`: existing sessions (`web/server.go:162-164`; hamr `Load` clears the cookie only on a nil subject) and API keys (`api/auth.go:163-166`) keep working until expiry; no revocation called | `UpdateUser` | | A |
| Theme / graph prefs | `web/account/handler.go:320,353` | -- | -- | -- | web only | self | theme whitelist `:326`; prefs ≤4KB `:358-364` | `UpdateUser` | | |
| Org access / write / owner check | `middleware/orgctx.go:179,220,206`, `setupgate.go:113`, `web/org/members.go:31` | `api/variables.go:319` requireOrg, `auth.go:217,257,300`, `api/members.go:26` | via api | -- | six functions, same rules | -- | 404 not member; draft → SetupPending (web) / 409 (api); write = owner/member; admin bypass. Web owner/admin gates answer 404 (`members.go:56`, `settings.go:225`, `server.go:197`), api 403 (`api/members.go:37`, `auth.go:210`); draft gate in five places (`orgctx.go:183-186`, `setupgate.go:328,395`, `variables.go:328`, `auth.go:292`, `helpers.go:52-66`); api `requireStackAccess` `:276` and `requireEnvAccess` `resolve.go:197` are tenancy-only, every write must add `requireOrgWrite` by hand | none | | |
| Bind org config repo | `web/org/config.go:50` (+ wizard `web/server.go:238`) | -- | -- | -- | web only | owner | connector in org `:61-64`, repo normalised `:65`, plans immediately `:80` | `UpdateOrg`, plan row | | |
| Plan org config | `web/org/config.go:50` (`plan_only`) | `api/orgconfig.go:22` planOrg | `internal/cli/cmd/org.go:56` | -- | `orgconf.Runner.Plan` `orgconf.go:314`; runner constructed inline twice (`api/orgconfig.go:19`, `web/prhook/handler.go:275`) | web owner (`ownedSettingsOrg` `web/org/settings.go:217`); api `requireOrgWrite` = member `:28` | error mapping | plan row | redirect / `planOut` | A |
| Preview org plan | -- | `api/orgconfig.go:50` | `org.go:22` | -- | `Runner.PreviewBundle` `:345` | member write + bound `:58` | 2MB cap | none | `planDetailOut` | |
| List / view org plans | `web/org/plans.go:36,77` | `api/orgconfig.go:85` (no read-one) | `org.go:79`; approve confirms blind `:110-116` | -- | separate | web owner `:38`; api any member `:86` | web folds stack plans `:54-71` | none | | A |
| Approve org plan (apply) | `web/org/config.go:125,203` (+ wizard `setup.go:253`) | `api/orgconfig.go:117` | `org.go:106` | `Runner.Apply` `orgconf.go:635` | apply shared; runs inline on request ctx on both (`:214`, `:125`), the failure `stackconf/job.go:241-248` fixed for stacks | web owner; api member write (`requireOrgPlan` `:111`) | pending check both; web apply error → flash 200 `:217`; api 422 `:124`; cli confirms blind (`org.go:647-652`) because there is no `GET` one-plan route (`v1.go:242-246`) | `UpdateOrg`, proxy `Resync` `:700-704`, domain rows `:799`, stacks, storage, vars (no audit `:707,723`) | redirect / `planOut` | A, B |
| Reject org plan | `web/org/config.go:140,222`; `setup.go:430` | `api/orgconfig.go:132` | `org.go:135` | -- | direct store write ×3 | owner / member write | pending | `SetOrgConfigPlanStatus` | | A |
| Export org config | `web/org/registry.go:293` | `api/config.go:293` | `internal/cli/cmd/defaults.go:206` | `orgconf.ExportYAML` `export.go:129` | shared | member both | none | none | yaml | |
| Move stack between orgs | see stacks | | | | | | | | | |

### 1.9 Registry + GitHub + PR hooks

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| List registries | `web/settings/handler.go:86` | `api/registry.go:267` | `internal/cli/cmd/registry.go:292` | -- | separate | adminOnly (`web/server.go:475` / `v1.go:184`) | none | store | web passes `Password` to template `settings.templ:93`; api strips `:345` | |
| Add external registry | `web/settings/handler.go:636` | `api/registry.go:281` | `registry.go:325` | -- | separate, identical | adminOnly | trim, require both | store only; no consumer in `infra/` (pulls use ghcr connector or managed registry, `infra/deploy/engine.go:573-579,920-930`) | flash / `registryOut` | |
| Delete registry | `web/settings/handler.go:656` | `api/registry.go:328` | `registry.go:395` | -- | separate | adminOnly | api 404 / 409 if `Managed` `:334-338`; web `DeleteRegistry(c.Param("id"))` no load, no managed guard `:657` (template hides button `settings.templ:126`); boot recreates managed row with new password (`infra/registry/registry.go:41-55`), breaking every org's system credential until `EnsureSystemCredential` (`token.go:285-309`) | store | | A |
| Set managed registry domain | `web/settings/handler.go:603` | `api/registry.go:298` patchRegistry | `registry.go:363 --domain` | -- | separate | adminOnly | web: managed required `:609`, `registry.EnsureManaged` when set `:616-622`, `px.WriteRegistry` `:624`. api: any registry by id, patch domain/username/password, `WriteRegistry` only when managed `:320-324`, no `EnsureManaged`; unused `Domain` stored on external rows | store, proxy, runtime (web). Boot `EnsureManaged` (`main.go:286`, `infra/registry/registry.go:36-65`) creates row and service but never `WriteRegistry`, and `Resync` (`proxy.go:577-624`) does not render `registry.yml` (`:715`): a wiped data dir loses the registry route until the domain is re-saved; boot goroutine vs handler `EnsureManaged` unguarded (`main.go:285`) | flash / `registryOut` | B |
| Update registry username/password | -- | `api/registry.go:311-316` | -- | -- | api only | adminOnly | writes creds on any registry incl. managed; fallout for token endpoint (`web/registrytoken.go:49-50`), agent pull secret (`token.go:331`), org system creds not checked; `registries.password` stored plaintext (`sqlite/infra.go:31-51`), as are `Tile.DBPassword`, `BasicAuthPassword`, `WebhookToken`, `Settings.ProtectPassword`; `secrets.Encrypt` is applied only in `store/repo/sqlite` (env, variables, provisions, backup keys, storage passwords, cert keys, drop links) | store | `registryOut` | |
| List org registry credentials | `web/org/registry.go:33,44` | `api/registry.go:46` | `registry.go:173` | -- | separate | member / `requireOrg` | web loads managed registry for push host `:51-62` | store | `registryView` vs `registryCredentialOut` | |
| Create org registry credential | `web/org/registry.go:165` | `api/registry.go:65` | `registry.go:216` | -- | separate | web `ownedSettingsOrg`; api `requireOrgRegistry(write)` + 503 when no managed registry `:26-44` | trim, refuse `SystemCredentialName`, `RandomHex(24)`, hash, prefix; web mints without registry (deliberate `:28-31`). Gate drift [SEC]: web owner (`web/org/registry.go:166,193,218` for create, delete, tag delete), api write = any member (`api/registry.go:26-34,101,200`): a member with a `stacks:write` key mints a long-lived push/pull credential and deletes tags | store | plaintext once | A |
| Delete org registry credential | `web/org/registry.go:192` | `api/registry.go:88` | `registry.go:249` | -- | separate, identical | owner / org match + write | 404 unless org matches; 409 if `System` | store | | |
| List org images / tags | `web/org/registry.go:71,107` | `api/registry.go:123,168` | `registry.go:44,79` | -- | separate; `registry.NewClient(...).Images/Tags` | member / `requireOrgRegistry(read)` | web joins `liveTags` `:113-116` (walks stacks/tiles/deployments `:130-159`); api `orgImage` resolves + `ValidRepoName` `:147-166` | registry read | | |
| Delete image tag | `web/org/registry.go:217` | `api/registry.go:199` | `registry.go:113` | -- | separate | owner / write | web: namespaced form required `:224`, no `ValidRepoName`/`ValidTag`, in-use via `liveTags` keyed after first `/` `:152`. api: `ValidRepoName`, `ValidTag` `:209-211`, `tagInUse` suffix match `:252`. A stored `ImageTag` without host is protected by api, deletable by web | registry `DeleteTag` | | A |
| Mint registry token | `web/registrytoken.go:32` (`GET /v2/token`) | -- | -- | -- | single | basic auth | three identities: managed root `:49-57`, agent pull `:62-71`, org credential `:72-91`; `TouchOrgRegistryCredential` | store | JSON | |
| Start GitHub connector (manifest) | `web/settings/handler.go:424` | -- | -- | -- | single | `RequireOrgWrite` `:441` | `gh.Begin` creates pending row (`infra/githubapp/githubapp.go:82-92`), cookie `:451-457` | store, GitHub | form | |
| Complete GitHub connector | `web/settings/handler.go:464` | -- | -- | -- | single | state nonce (`githubapp.go:139-153`) | `gh.Complete` → `UpdateConnector` | store, GitHub | redirect | |
| Delete connector | `web/settings/handler.go:498` | -- | -- | -- | single | `RequireOrgWrite(cn.OrgID)` `:512` | no reference check: `tile.ConnectorID`, `stack.ConfigConnectorID`, `org.ConfigConnectorID` keep the dead id; `cigate.go:69-73` fails parked deploys, `stackconf/runner.go:47-49` errors planner | store | | |
| List org connectors | `web/org/settings.go:229` → `web/settings/handler.go:406`; inline copies `web/app/handler.go:68`, `web/org/config.go:32`, `web/project/handler.go:119,1825` | -- | -- | -- | four inline copies of `Provider=="github" && Connected()` | member | filter | store | | |
| List repos / branches / file exists | `web/app/handler.go:77`, `web/project/handler.go:105`, `web/org/setup.go:342`; `web/org/config.go:260,275` via `pickerConnector :240` | -- | -- | -- | three repo aggregators | tile/stack/org load; picker owner + org + github `:249-252` | dedup differs per copy | GitHub read | | |
| Set tile connector | `web/app/handler.go:1481-1494` (silent drop) | `api/apps.go:145,237` via `checkConnector` `helpers.go:114` (400) | `tile.go:162,184` | `apply:1888-1897` applySource (raw id, falls back to `stack.ConfigConnectorID`, no org check); plan `plan.go:884-889` only when `len(OrgConnectors) > 0` | four copies of "connector must belong to org": web, api, planner, engine `githubapp.go:318-331` (silent nil → unauthenticated clone) | per calibration | two of four fail open | store | | A |
| Get PR env settings | `web/project/handler.go:2265` | `api/lifecycle.go:384` | `internal/cli/cmd/lifecycle.go:608` | -- | `envops.LoadPRConfig` `envops.go:729` | `RequireOrgAccess` / `requireStackAccess` | web decides whether to show hook URL (`stacksettings.templ:584-590`) | store | page / `prEnvOut` | |
| Set PR env settings (enable, comment, status) | `web/project/handler.go:2647` | `api/lifecycle.go:397` | `pr-envs --enable/--comment/--status` | `pr_envs.enabled/comment/status` `stackconf.go:129-136`; not applied by `apply.go`; `comment`/`status` written into `PRConfig` at PR open (`web/prhook/handler.go:488-500`); `enabled` read-through `:476-481`, never persisted | `envops.SavePRConfig`; three writers | `RequireOrgWrite` / `requireOrgWrite`; no `editGate`/`rejectManaged` | web unchecked = off `:2653-2658`, mint secret if empty; api nil = unchanged, `RotateSecret` `:410-419` | store (`prenv.<stackID>`) | flash / `prEnvOut` | A |
| Rotate PR webhook secret | `web/project/handler.go:2670` | `api/lifecycle.go:417` | `--rotate` | -- | shared Save | as above | `RandomHex(24)` | store | | |
| Receive PR webhook, stack-scoped | `web/prhook/handler.go:82` Hook (`POST /hooks/github/:stack`) | -- | -- | -- | shares `dispatch` `:427` | HMAC on `cfg.Secret` `:99` | 404 when `!cfg.Enabled` `:92`; no `stackTracksRepo`, no `updatePlanComment` (only in `HookConnector` `:157-161`): a manual webhook can open `pr-N` on a stack tracking a different repo | dispatch | JSON | A |
| Receive connector webhook | `web/prhook/handler.go:120` HookConnector (`:141-176`) | -- | -- | -- | single | HMAC on connector `WebhookSecret` `:129-138` | `ListStacks` by org, `updatePlanComment` `:157-159`, `stackTracksRepo` `:443`, per-stack dispatch | dispatch, PR comment | JSON | |
| PR opened → create PR env | `web/prhook/handler.go:462` openPR | -- | -- | `pr_envs.tiles` template (`stackconf.go:858-873`) | single | none | writes file `comment`/`status` into `PRConfig` `:488-500`; creates env row directly `:501-511` (no rules); `ApplyResolved` `:520-526`; `CopyLayout` `:531-533`; `LoadSchedules` `:534` | store, apply, scheduler, deploys | -- | |
| PR synchronize → redeploy | `web/prhook/handler.go:667` syncPR | -- | -- | -- | single | none | `Enqueue(t,"webhook")` for git service/cron `:684-686`; no `WaitForCI` park unlike `autoDeploy` `:399` | engine | -- | |
| PR closed → tear down | `web/prhook/handler.go:691` closePR | -- | -- | -- | `envops.Teardown` (Ops from `web/server.go:612`) | none | `Teardown`, `LoadSchedules` `:702` | cluster, proxy, scheduler | -- | |
| Push → re-plan config-managed stacks / org | `web/prhook/handler.go:250` planConfigs (from `:186`) | plan-now `api/config.go:100` (no auto-apply) | `stackr plan` | -- | separate: webhook `RunEnv`/`Run` + `maybeAutoApply` → `EnqueueApply(force=false)` `:334-347` | HMAC / `requireConfigStack` | org branch `:268-273`, env/stack branch match, `notifier.Project` `:303,319` (only planner path that nudges) | plan rows, workqueue, notifier | -- | |
| Push → auto-deploy tracking tiles | `web/prhook/handler.go:353` autoDeploy | -- | -- | `wait_for_ci` `stackconf.go:390,1047,1106`, `apply:1759` | single | none | `configOnlyPush` `:226`, first static env `:418`, `sameRepo` `:708`, `watchMatch` `watch.go:13`, park `:399-404` else `Enqueue` | engine Park/Enqueue | -- | |
| CI gate release / fail | `infra/cigate/cigate.go:39` (janitor, `cmd/stackrd/main.go:439`) | -- | -- | -- | single | -- | 30m timeout, grace 3m, `Release`/`FailWaiting`, `Push` `:117` ("Deploy blocked" payload vs engine "Deploy failed" `engine.go:534`) | store, engine, notifier | -- | |
| Deploy finished → PR status + comment | `infra/githubapp/feedback.go:73` via `engine.OnFinish` (`main.go:335-337`) | -- | -- | `PRConfig` comment/status | single | -- | reads `LoadPRConfig` `:89`, posts status + sticky comment | GitHub | -- | |
| PR plan preview comment | `web/prhook/handler.go:541` updatePlanComment | -- | -- | -- | single | -- | `Preview`, `planMarkdown` `:598`, stores under `githubapp.PlanKey` `:591`, `RefreshPRComment` | store, GitHub | -- | |

### 1.10 Settings + proxy admin + nodes + notifications

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Read defaults cascade | server `web/server/handler.go:66`; org `web/org/registry.go:244`; stack/env `web/project/stacksettings_templ.go:305,2683` | `api/settings.go:97` | `internal/cli/cmd/defaults.go:17` | export only | `settings.Levels`/`Resolve` (`config/settings/settings.go:186,242,365`) | web org owner; api server admin `:39`, org/stack member, env access | parse | none | own vs `Above` / `settingsOut` (`:202`, 14 keys, password masked `:238`) | |
| Set server defaults (+ rename server) | `web/server/handler.go:312` SaveSettings | `api/settings.go:114` ("server") | `defaults.go:135` | -- | `settings.Merge`+`Check` | web adminOnly `web/server.go:345`, any `:id`; api `isAdmin`, hard-coded `"local"` `:42` | web sets `sv.Name` unvalidated `:322` (empty allowed); api never touches name; `ProtectPassword` stored plaintext inside the settings JSON at every level (`config/settings/settings.go:28`), api masks it to `"set"` `:238-246` | `UpdateServer`; `px.Resync` web `:336`, api `:160` | flash / `settingsOut` | A |
| Set org defaults | `web/org/registry.go:257` SaveDefaults | `api/settings.go:114` ("org") | `defaults.go:83` | `orgconf.go:687-700` (whole blob `:689`, own `Check` `:184`) | Merge+Check | web owner + `ConfigManaged` 409 `:262`; api `requireOrgWrite` = member `:53`, no managed check | api write is overwritten by the next apply `:687-691` when file declares `defaults:`, sticks otherwise | `UpdateOrg`; `Resync` web `:284`, api `:160`, config `:700` | | A |
| Set stack defaults | `web/project/handler.go:1022` | `api/settings.go:114` ("stack") | `defaults.go:95` | `apply:932-937` (whole blob `:933`) | Merge+Check | `loadStack` / `requireOrgWrite` `:64`; neither checks `ConfigManaged` | none | `UpdateStack`; `Resync` web `:1041`, api `:160`, config `:970-971` | | |
| Set env defaults | `web/project/handler.go:2542` SaveEnvSettings | `api/settings.go:114` ("env") | `defaults.go:104` | `apply:955-964` | Merge+Check | `loadStack` / `requireEnvWrite` `:70`; no managed gate | web `Merge(Parse(env.Settings), form)`, flash on error; api null → clear, 400 | `UpdateEnvironment`; `Resync` web `:2568` via `resyncProxy :1049`, api `:157`, config `:971`. All three resync; the `x-plan-staging` shard's claim that `:2565` skips it is wrong, re-read and dropped | | |
| Set org env-colour defaults | `web/org/settings.go:123` | -- | -- | `orgconf.go:173,680` | `envcolor.Valid` | owner + 409 managed `:129` | slug, delete-on-empty `:132-150` | `UpdateOrg` | | |
| Set org `ui_edits` default | -- | -- | -- | `orgconf.go:178,676` | config only | n/a | `docs/surface-parity.md` marks web/API ✅; no setter in `handlers/` | `UpdateOrg` | | |
| Read proxy config | `web/settings/proxy.go:51` | `api/proxycfg.go:64` | `internal/cli/cmd/proxy.go:20,106` | -- | byte-identical helper pairs `settings/proxy.go:25,34,45` / `proxycfg.go:39,48,59` | adminOnly | decode `proxy_custom_dynamic`, `CurrentStatic()` | none | web adds trusted/cloudflare | |
| Set / clear static traefik override | `web/settings/proxy.go:109` | `api/proxycfg.go:75` | `proxy.go:62` | -- | two copies | adminOnly | YAML gate, blank → clear | `SetSetting`; `go px.EnsureTraefik` web `:122` (logged), api `:93` (error dropped) | | |
| Write / delete custom dynamic entry | `web/settings/proxy.go:136,156` | `api/proxycfg.go:97,118` | `proxy.go:142,166` | -- | two copies | adminOnly | `Slugify` on write, raw name on delete (both) | `SetSetting` + `SyncCustomDynamic` | | |
| Set trusted proxies / Cloudflare | `web/settings/proxy.go:67` | -- | -- | boot seed `cmd/stackrd/seed.go:52-70` (second copy) | web only | adminOnly | `netaddr.ParseTrusted` `:75` rejects the save on one bad CIDR `:74-77`; seed skips bad lines with a warning (`seed.go:54-57`) and leaves Cloudflare off on fetch failure `:67` where the handler falls back to the `cloudflare_cidrs` cache `:83-89`; installer `CheckProxies` `internal/installer/answers.go:565` is a third caller of the parser; no api writer | 2× `SetSetting`; `go EnsureTraefik` `:98`; read by `infra/proxy/trusted.go:96-98` | | A |
| Set DNS-01 provider + env | `web/settings/handler.go:371` | -- | -- | -- | web only | adminOnly | stored unvalidated `:373-376`; consumer `infra/proxy/proxy.go:88-89`; used by web CreateDomain wildcard rule `web/app/handler.go:1688` | `SetSetting`; `go EnsureTraefik` `:380` | | |
| Toggle nightly docker cleanup | `web/settings/handler.go:583` | -- | -- | -- | web only | adminOnly | "0"/"1"; the worker `main.go:376-402` is a 24h `time.Sleep` loop (first run 24h after boot, not nightly) that truncates access.log over 50MB then `clus.Prune` + `registry.GarbageCollect`; no "prune now" handler | `SetSetting("cleanup_enabled")`; read `cmd/stackrd/main.go:385` | | |
| Read audit log | `web/settings/handler.go:748` | -- | -- | -- | web only | adminOnly | actor dedupe in handler `:757-770` | none | | |
| Check / run panel upgrade | `web/settings/upgrade.go:195,209` | -- | -- | -- | `service/admin.go:68,120` AdminService | adminOnly | render only; `CheckUpgrade` also runs at boot and every 24h (`main.go:486-493`). `Upgrade` retags the relay `:135-146` and `UpdateServiceImage` sets only `STACKR_IMAGE` `:156`; the panel service spec (env list, mounts, constraints) lives only in the installer `internal/installer/steps.go:128-169` with a hand copy in `scripts/dev/deploy-test.sh:118-131`, so a new env var reaches fresh installs and never upgraded ones | `SetSetting` upgrade keys `admin.go:96-99,169`; swarm service update (`swarm.UpdateConfig` `:156`) | | A, D |
| View notification centre (marks all read) | `web/notification/handler.go:258` | -- | -- | -- | web only | RequireAuth | write on GET `:265`, error dropped | store | | |
| Mark all read / clear / unread count | `web/notification/handler.go:271,279,287` | -- | -- | -- | web only | RequireAuth | count error dropped `:288` | store | | |
| Set my notification preferences | `web/account/handler.go:280` | -- | -- | -- | web only | self | loop `notify.Kinds` `handlers/notify/notify.go:42`, JSON blob `:289-290` | `UpdateUser` | | |
| Fan out a notification (`Push`) | `handlers/notify/notify.go:102` | -- | -- | -- | one impl; callers all `infra/`: `engine.go:533,536`, `cigate.go:117`, `jobs.go:421`, `watcher.go:142,145,204` | n/a | recipient enumeration + `EnabledForUser` `:113`, 30-day prune `:133` | `CreateNotification`, ws `:134`; errors swallowed `:125` | | A |
| Canvas layout (positions, annotations, groups; stack, env, org) | `web/project/stackgraph.go:95-226`, `web/project/handler.go:1083,1128`, `web/org/graph.go:117-323` | -- | -- | -- | web only (UI-native) | loadStack / loadOrg | none | store; `notifier.Project`/`Org` after each write | | |
| Mail (SMTP) settings | -- | -- | -- | -- | `infra/mail/mail.go:37` FromEnv only; no store key, no page | n/a | | | | |
| Global search | `web/search/handler.go:44` | -- | -- | -- | web only | RequireAuth; `middleware.Orgs` `:52` | in-memory join across 12 listers, `tileTarget` `:299` | none | | |
| Server detail / host info / ping history | `web/server/handler.go:66,143` | -- | -- | -- | web only | adminOnly | metric bucketing `:355`, disk units `:78`, resource/storage filtering `:104-123` | none | | |
| About | `web/about/handler.go:309` | -- | -- | -- | static | none | none | none | | |
| List swarm nodes | `web/server/nodes.go:30` | -- | -- | -- | `nodes.Service.Sync` `infra/nodes/nodes.go:75` | adminOnly | none; the same `Sync` runs every 30s from boot (`main.go:495-500`), adopts unknown swarm nodes with `CreateServer` (`nodes.go:135`) and mirrors with `UpdateServer` `:216`, so a GET writes `servers` rows | Sync mirrors swarm → `servers`; `Ping` every 30s → `InsertMetric` `nodes.go:246` | | |
| Add node | `web/server/nodes.go:64` | -- | -- | -- | `nodes.AddNode` `nodes.go:331` owns validation | adminOnly | `agent.Ensure` orchestration + soft-fail text in handler `:71-88`; boot runs `agent.Ensure` only when `len(nodes) >= 2`, retried 10 times every 15s (`main.go:463-484`), the handler runs it once unconditionally: a first-node AddNode whose Ensure fails (registry not up yet, the case the boot retry exists for `:469-470`) gets no retry until a boot with 2+ nodes | `CreateServer` + join key; swarm agent service | | A |
| Mint join key | `web/server/nodes.go:93` | -- | -- | -- | `nodes.IssueKey` `:382` | adminOnly | none | store | | |
| Redeem join key / claim node id | `web/server/nodes.go:108,150` | -- | -- | -- | `nodes.Redeem`/`Claim` `:495,422` | one-time key + XFF `:183` | join-script assembly (`rt.JoinToken` `:120`, `registry.PullAddr` `:126`) in handler | store | shell script | |
| Drain node | `web/server/nodes.go:263` | -- | -- | -- | web only | adminOnly | pinned-tile refusal `refusePinned :279` (`tasksOn :237`) in handler | `rt.SetNodeAvailability` `:319` | | |
| Activate node | `web/server/nodes.go:309` | -- | -- | -- | web only | adminOnly | none | `rt.SetNodeAvailability` | | |
| Remove node | `web/server/nodes.go:329` | -- | -- | -- | web only | adminOnly | manager refusal `:335`; typed confirm `:342`; drain → `rt.RemoveNode` → `BurnServerJoinKeys` → `DeleteServer` `:346-363`, all in handler | swarm, store ×2 | | |
| Set node group label | `web/server/nodes.go:375` | -- | -- | -- | web only | adminOnly | "not joined" guard `:382` | `rt.SetNodeGroup` | | |

### 1.11 Boot, workers and raw containers

Call sites that run outside a request: `cmd/stackrd/main.go` in boot order, then tickers, janitor entries and queue consumers under `infra/` (`shards/g-boot-workers.md` B1-B23, W1-W21), plus the raw-container pages (`shards/g-leftovers.md`). "svc" in the class column = no drift, but store + infra together, so a service candidate.

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Convert legacy mounts to volume tiles (boot) | -- | -- | -- | -- | `envops.ConvertLegacyMounts` `envops.go:642` from `main.go:213`, once flag `:643` | none | service, unmanaged, `name:path` lines `:654-664`; `uniqueSlug` `:700` is not the handlers' slug rule; no `ConfigManaged` gate, no `Enqueue(target,"volume")` where web `:603-616` and api `volumes.go:55-81` both redeploy | `CreateTile`, `UpdateTile`, `SetSetting volumes_migrated` | -- | A |
| Backfill variables from env blobs (boot) | -- | -- | -- | -- | `envops/backfill.go:23` from `main.go:220` | none | skips tiles with rows `:38`; no `ClearWaiting*`, no replan (api `variables.go:167`, web `project/handler.go:2302` do both) | `UpsertVariable` | -- | B |
| Network pool ensure / sweep / backfill (boot) | -- | -- | -- | -- | `infra/netpool/netpool.go:48,229,212` from `main.go:257-268`; same `ClaimEnv` as deploy and envops | none | `Sweep` removes orphan services on unclaimed nets `:250-254`; `Backfill` → `SetEnvironmentNetwork` `:124`; zero handler callers of `netpool` | store, runtime | -- | svc |
| Ensure managed registry (boot goroutine) | `web/settings/handler.go:620` (only when `Domain != ""`) | `api/registry.go:319` never calls it | -- | -- | `infra/registry/registry.go:36-65` from `main.go:275,286` | signer nil → refuse `:65` | `CreateRegistry` row if none `:43-53` (`RandomHex(16)` password), `PullImage`, `StopRemove` legacy containers, `EnsureService`; no `WriteRegistry` (1.9) | store, runtime | -- | B |
| Sweep stale runs (boot) | -- | -- | -- | -- | `store/repo/sqlite/deployments.go:55-69` from `main.go:315`, before `srv.Start` | none | queued/running → `error` + `InterruptedMsg`, `building` tiles → `stopped`; `waiting_ci` untouched (cigate re-adopts, `cigate.go:40`); a user cancel writes `cancelled` (`engine.go:313`), engine finish writes tile `stopped` on cancel and `error` on failure `:497-510` | store | -- | A |
| Seed install (boot) | -- | -- | -- | -- | `cmd/stackrd/seed.go:28` from `main.go:354`, once flag `:29` | none | see 1.6 "Root domain bootstrap" and 1.10 "Set trusted proxies" | instance domain resource, `trusted_proxies`, `trust_cloudflare`, `install_seeded` | -- | |
| Nightly prune + registry GC | flag only (1.10) | -- | -- | -- | `main.go:376-402` | `cleanup_enabled=="1"` read per tick | 24h `Sleep` loop; access.log truncate over 50MB; `clus.Prune` has no other caller | runtime, registry | -- | |
| Deploy finished hooks (`engine.OnFinish`) | -- | -- | -- | -- | `main.go:335-338` `gh.DeployFinished`; `:409-426` `jobs.StartApp(TriggerDeploy)` for `function && RunOnDeploy && done`, `SetTileImageDigest` for `done && SourceType=="image"` | kind gate inline | third `image_digest` writer (1.5 image-watch row); run-now kind gate here and in api `apps.go:512`, absent on web `app/handler.go:201` | run row, digest | -- | A |
| Work queue recover (boot) + poll | stop via `jobs.Stop` `jobs.go:239` | same | `internal/cli/cmd/lifecycle.go:228` | -- | `infra/workqueue/workqueue.go:254` recover, `:303-372` poll, `:413-422` finish; per-kind `OnRestart` (`stackconf/job.go:47` requeue; `jobs.go:213-233`, `backup/work.go:150-187`, `volmove.go:390` cleanup) | none | restart closes a cron run `error`/`interrupted` (`jobs.go:227`), a user stop closes it `stopped` (`:249-252`); `ClaimWorkItem` atomic by rows-affected (`sqlite/workitems.go:48-76`); concurrency per kind from settings | store | -- | A |
| Tile status reconcile (30s tick) | writers `web/app/handler.go:338,361,367`, `web/db/handler.go:252-294` | writers `api/lifecycle.go:39,61,66`, `api/databases.go:250,255` | -- | writers `apply:605-607,1228-1231,1474-1484`, `orgconf.go:977-980` | `infra/metrics/reconcile.go:107` (`reconcileStatus` `:24-68`); other writers `engine.go:476,509,521`, `volmove.go:569,675,680`, `deploy/waiting.go:77,98` | none | 14 writers write intent, one observer writes observation 30s later; the reconciler alone consults `runpolicy.For(kind).KeepAlive` and skips run-to-completion kinds and volumes `:26`; local container beats swarm task list (`mergeRemote` `:126`) | `UpdateTileStatus` | -- | A |
| Metrics sampler (tick) + agent samples | -- | `handlers/api/server.go:100` → `nodes.StoreSample` `nodes.go:288` | -- | -- | one impl `StoreHostSample` `metrics.go:200`; ticks `metrics.go:110-148,171,178-179,201-204,246,402` | agent shared bearer key `infra/agent/server.go:100-108` | retention from `settings.ForServer`; `PruneMetrics`; `notifier.Server()` | `InsertMetric` | -- | svc |
| Cron run consumer | run-now 1.1 | run-now 1.1 | -- | -- | `infra/jobs/jobs.go:316-321` entry (`Kind=="cron" && Cron!="" && Status!="paused"` `:311`) → `StartApp` `:332`; `runApp` `:356-425` | none | `skipReason` `:147` dependency gating, overlap `:81`, limits from `settings.ForTile`; only writer of `tiles.last_status`/`last_run_at` (`RecordTileRun`; readers `graph.go:692,704`); `PruneCronRuns` `:291` | cron run rows, workqueue, `Push` on ok→error edge | -- | svc |
| Deploy engine status writer | -- | -- | -- | -- | `infra/deploy/engine.go:424-541` | none | single writer of `building`/`idle`/`waiting:<name>`: `KeepAlive` → `idle` `:517`, `varref.Unset` → `waiting:` `:503-507`, cancelled → `stopped` `:500`; never calls `px.WriteApp` (routes exist only if a handler wrote them: `api/domains.go:83,110`, `api/lifecycle.go:238`, `web/db/handler.go:646`, `web/app/handler.go:1923`, `envops.go:462`, `apply:1491,1674`) | deployment + tile status, `Push`, `OnFinish` | -- | svc |
| List / inspect raw containers | `web/container/handler.go:93` List, `:127` NodeList, `:147` Detail, `:171` inspect | -- (no container route in `v1.go`) | -- | -- | web only | adminOnly `web/server.go:549-550` | `?node=` else `clus.Self` `:38-43`; single-node shortcut `:97`; `applyFilter` drops non-running unless `?stopped=1` `:76-90`; error taxonomy by message match ("No such container" 404 `:179`, `net.Error` renders `down` `:182-191`, else 500); stats only when running `:155-157` | cluster read | `ctRow` / `ContainerDetail` | |
| Raw container logs (SSE) | `web/container/handler.go:244,255` | -- | -- | -- | web only | adminOnly | tail fixed 300 `:259`; SSE framing in the handler `:265-281`, same shape as tile logs (1.1) | cluster stream | event-stream | |
| Raw container terminal (exec TTY) | `web/container/terminal.go:25,36` | -- | -- | -- | web only; the only `wsterm.Serve` site | adminOnly, no system guard | `nodeExec` adapter `:43-50`; default shell (nil cmd) `:37` | cluster `ExecTTY` | websocket | |

### 1.12 Installer and relay

| operation | web | api | cli | config | shared | gate | logic in handler | side effects | return shape | class |
|---|---|---|---|---|---|---|---|---|---|---|
| Install server (fresh host) | -- | -- | `cmd/stackr-install` → `installer.Main` `internal/installer/flags.go` | -- | panel service spec `steps.go:128-169` (env list `BASE_URL`, `ACME_EMAIL`, `STACKR_TLS`, `ROOT_DOMAIN`, `TRUST_CLOUDFLARE`, `TRUSTED_PROXY_CIDRS`, `DATA_DIR`, `DATABASE_PATH`, ports, `HOST_PROC`, `STACKR_IMAGE`; mounts; constraints) only here; hand copy `scripts/dev/deploy-test.sh:118-131`; `AdminService.Upgrade` sets `STACKR_IMAGE` only (`service/admin.go:156`) | root, docker present, not installed `steps.go:369-394` | swarm init + pool pick `:178-214`; `daemon.json` registry trust `:277-322`; network `stkr` `:331-349` (= `runtime.go:46`); relay pull/tag `:95-111`; CLI wrapper `:356-365` execs `/app/stackr`, present in `cmd/stackrd/Dockerfile:48` not `Dockerfile.runtime:6-10` (dead on a runtime-image rig). Constants agree today (hostname `stkr-panel`, relay tag, registry port 5000, service name `stackr`, ports 80/443, `STACKR_TLS`, `TRUST_CLOUDFLARE` "1"); `DATA_DIR` default `/var/lib/stackr` (`flags.go:77`) vs server `./data` (`main.go:79`); server also reads `TRUSTED_PROXIES` (`main.go:78,143`), a name the installer never sets | host: swarm, daemon.json, docker restart, service, `/usr/local/bin/stackr` | text | A |
| Validate install answers | -- | -- | `installer.Answers.Check` `answers.go:473`: `CheckRoot` `:518`, `CheckHost` `:552`, `CheckProxies` `:565`, `CheckEmail` `:582`, `CheckPort` `:605`, `CheckDataDir` `:631` | -- | `CheckProxies` shares `netaddr.ParseTrusted` (`internal/netaddr/netaddr.go:11`, refuses /0, bare IP → /32 or /128) with `seed.go:52` and `web/settings/proxy.go:75`; `CheckRoot` has no server twin (1.6 root domain row) | n/a | full DNS-label grammar `:518-549`; DNS warnings `form.go:190-212` | none | | A |
| Port-forward relay process | -- | -- | -- | -- | `cmd/proxyrelay/main.go:107-109` defaults `-listen :15000`, `-idle 60s`; the server passes only `-target` (`runtime.go:360`) and dials `ProxyRelayPort = 15000` (`runtime.go:83`) | n/a | port defined twice, nothing ties them; idle 60s is relay-only and the server comment relies on it (`runtime.go:388-390`) | container | | |

Settings observations, no class: `settings.Merge` folds server-only knobs (`build_node`, four concurrency keys) at org/stack/env (`settings.go:192-195,228-234`), `settingKeys` exposes them on every level, `Resolve` reads them at server only (`:37-46`), and `DefaultsConf` omits them so an apply wipes a stray override (`apply:933`). Plan-status nudges: web `PlanNow`/`ApprovePlan`/`RejectPlan` and api `approvePlan`/`rejectPlan` fire no `notifier.Project`; only the webhook planner does.

## 2. Service inventory

Ranked by flagged rows, then call sites. Each entry: rows it owns, sketched methods (from the "logic in handler" column), call sites (from the lens 2 tables), services it calls, then the drift findings as annotations. Class in brackets.

### 2.1 ManagedInstanceService + SliceService (databases)

Rows: create / get / list instance, update settings, set scope, delete, deploy, stop, start, publish connection, provision, attach, detach, orphan on consumer delete, cut slice, drop, fork, set public, list slices, resolve infra address, org-shared instances. 15 flagged rows (9 A, 6 B).

Methods, `ManagedInstanceService` (grows `managedtiles.Service`): `Create(ctx, stack, env, InstanceSpec) (*Tile, error)` (slug rules, scope derivation via `applyScope`, `NewDB`, `CreateTile`, `PublishConnection`, `Deploy`, status); `Update(ctx, t, prev *Tile) error` (one `needsRedeploy(prev, t)` predicate replacing `api/databases.go:119-158`, `web/db/handler.go:519`, `apply:1527`, `orgconf.go:932`; republish vars); `Delete(ctx, t, force bool) error` (held-slices gate, `envnet.TearDown`, `Proxy.DropTile`, `netpool.ReleaseDB`, drop or orphan slices, `DeleteTile`); `Deploy/Start/Stop(ctx, t)` (own ctx, status write, notifier).
Methods, `SliceService`: `Provision(ctx, inst, consumer, name, public, WireOpts)`; `Attach`; `Detach`; `DetachAllOf(ctx, consumerID)` (called by TileService.Delete on every surface); `Cut(ctx, inst, env, slug, name, public, onRemove)` with `WaitReady`; `Drop`; `Fork`; `SetPublic(ctx, inst, dbName, public)` looping all rows and persisting `Public`. `InfraResolver.Resolve(ctx, path, scope)` accepting colon (`api/resolve.go:86`) and dotted (`stackconf/slices.go:21`, `infrapath.go:28`) grammars.
Call sites: `managedtiles` reached from 38 sites (web 17, api 12, cfg 9); `NewService` constructed per request at `api/envs.go:195`, `web/project/handler.go:2641`, `web/app/handler.go:1010`.
Calls: ProxyService (route rewrite/remove), DeployService (consumer redeploy), VariableService (`PublishConnection`, inject), SchedulerService, NotificationService, netpool, cluster.

- [B] Delete instance is four teardowns, none complete. Web `web/db/handler.go:464-467` releases netpool, never `px.RemoveApp` (s3 instance with a domain keeps its route until `Resync` prunes, `infra/proxy/proxy.go:717`). API `databases.go:437` → `teardownTile` `helpers.go:129-135` removes the route, never `netpool.ReleaseDB`, and with `force=true` leaves provision/resource rows (`store/repo/sqlite/tiles.go:269-282`). CLI always `force=true` (`infra.go:262`). Config `apply:1544-1567` no held-slices gate, no netpool. Orgconf `orgconf.go:985-995` netpool only. `DELETE /apps/:id` accepts a managed id with no force gate.
- [B] API `deleteApp` never detaches consumer provisions (`api/apps.go:463-478`, `helpers.go:129-135`); web `web/app/handler.go:1635-1639` and config `apply:1558-1565` do. Rows stay `active` with a dangling `ConsumerTileID`.
- [B] s3 wiring: API injects the whole `AutoInjectVars` set (`api/databases.go:381-390`); web injects only `S3_ENDPOINT` (`web/app/handler.go:868,885`).
- [B] Set slice public: web `web/db/handler.go:425-433` and API `api/lifecycle.go:292-297` flip every row sharing `DBName`; config `slices.go:107-114,154-180` flips one representative row. API sets `p.Public` in memory only (`:297`).
- [B] Org-shared instances: `orgconf.go:948-955,985-995` delete with `dbs.Remove` + `DeleteTile`, no held-slices check, no `TearDown`, no `RemoveApp`.
- [A] Create: API accepts empty slug (`api/databases.go:227-233`), web refuses (`web/project/handler.go:1670-1672`); web scope env/stack only (`:1700`), API/config allow org. Web `CreateDB` `:1719-1727` unreachable; web can only stage, API creates and deploys inline, never stages.
- [A] Update settings: gate `rFU` (web `web/db/handler.go:62-67,483`) vs `rejectManaged` (API `databases.go:112`); web coerces bad port/policy (`:486-489,510-515`), API 400s (`:121,178`); redeploy rule in four copies; `PATCH /apps/:id` on a db id applies none of the db rules and no redeploy; CLI cannot set `update_policy` (`infra.go:358-365`).
- [A] Set scope: unknown value web → env (`web/db/handler.go:548-549`), API 400 (`databases.go:94-95`); two gates in one handler (`RU` `:535` vs `rFU` `:483`).
- [A] Deploy/restart: web `Start` heals provisions (`managedtiles.go:543-561`), API `restartApp` on the same id does not (`api/lifecycle.go:48-71`); `POST /apps/:id/deploy` enqueues the build engine for a managed tile; web `Deploy` detached on `context.Background()` (`web/db/handler.go:249-262`).
- [A] Provision/attach: gate web `rejectManaged` (`web/app/handler.go:906,936`) vs API none (`databases.go:276-351`); var name sanitised on web (`:966-980`), raw on API (`:314,401`); web dead config-managed branch `:874-881`.
- [A] Cut slice: config `WaitReady 90s`, `Eligible`, clone on ephemeral, `on_remove` (`slices.go:79-131`); API `createInstanceProvision` uses `ServesEnv` (`api/resolve.go:106`), none of the rest; `Eligible` not visible in `:81-155` (not confirmed).
- [A] Domains on an instance: web `SpeaksHTTP` + engine port (`web/db/handler.go:581,601`); API `createDomain` accepts a managed id with no engine check and port 0 (`domains.go:35-75`).
- [A] Get: `getDB` omits `Path` that `listDBs` sets (`api/databases.go:76-80` vs `:35`).
- Duplication: `NewDB` + `PublishConnection` hand-sequenced at six sites; status bookkeeping after `Deploy` copied six times.
- Round 2: [A] CLI `infra rm` sends `force=true` on every path (`internal/cli/cmd/infra.go:1559`), `-y`/`--force` skip its own prompt (`:1532-1537`), so the api held-slices refusal is unreachable (g-cli). [A] orgconf `applyShared` writes `Slug: name` unslugified with no reserved or collision check (`orgconf.go:962-970`), `WebhookToken` via `uuid` (`:966`, `api/databases.go:237`) (g-store, g-config-small). [B] `imagewatch/watcher.go:152-162` is a sixth `Deploy` caller and the only one writing no status; `volmove.go:675-680` is the fifth "Deploy then status" copy (g-boot-workers). Instance-vs-slice kind gates live in six CLI checks (`infra.go:1389,1426,1466,1618,1682,1722`); `Delete(instance, force)` must keep the held-slices refusal and take the user's flag, not a constant.

### 2.2 EnvironmentService + VariableService (+ ShareLink, EnvCompare)

Rows: create, clone, list, delete, reset, rename, colour, apply policy, config branch, mark intended, copy tile, compare, stack/env/org/tile variables set and delete, plan inputs, read/reveal/resolve, generate, share links, drop-link submit, env network. 16 flagged rows (10 A, 6 B).

Methods, `EnvironmentService` (seed `envops.Ops`; one constructor instead of five literals): `Create(ctx, stack, CreateEnv{Name, Type, BaseEnvID})` (one slug/reserved/duplicate rule, managed gate, clone, reload; replaces `web/project/handler.go:305`, `api/stacks.go:89`, `api/envs.go:118`, `web/prhook/handler.go:501`); `Delete(ctx, stack, env, force)` (last-env, running, Teardown, reload); `Reset(ctx, stack, env, force)` (managed-only, Teardown, reload, replan); `Update(ctx, env, EnvPatch{Color, ApplyPolicy, ConfigBranch})`; `EnsureAutoDomain`; `PRConfig get/set`.
Methods, `VariableService`: `Set(ctx, Owner{Kind,ID}, []VarWrite{Name, Value, Secret, Generate, Length}, actor)` (one name regex, masked guard, generate policy, audit, `ClearWaiting*` by owner, replan when config-managed, tile redeploy when running); `Unset`; `Replace` (PUT semantics); `List(ctx, Owner, reveal)`; `Resolve`. `ShareLinkService.Submit` must call `VariableService.Set`. `EnvCompareService.Compare/MarkIntended/CopyTile`. `PromotionService` folded into ReleaseService (2.4).
Call sites: `envops` from 15 sites; `envnet` from 11 (2 `TearDown`, rest reads); `deploy.ClearWaiting*` from 9 (D22-D29 in `shards/x-deploy-orchestration.md`).
Calls: ProxyService (`DropTile`, `Resync`, auto domain), SchedulerService, PlanService (replan), SliceService (detach/drop), DeployService (redeploy running tile), StagingService, AuditService, netpool.

- [B] Variables over the API never replan or redeploy: `api/variables.go:116-197` vs web `replanAsync` `web/project/handler.go:2302,2325,2599,2629` and `engine.Enqueue` `web/app/handler.go:1204`. `stackr vars set` on a running tile or managed stack changes nothing visible.
- [B] Reset env: web replans (`web/project/handler.go:436`), API `resetEnv` (`api/envs.go:91-113`) does not; stale pending plan.
- [B] Drop-link submit: `sharelink.go:117-158` writes vars with no `ClearWaiting` and no replan (`:129-131` admits it).
- [B] Config apply mints generated secrets and writes vars with no `audit.Record` and (secrets) no `ClearWaiting` (`apply:1777,1814,1846`; `orgconf.go:707,723`).
- [A] Env create has four rule sets: web reserved only (`web/project/handler.go:322`), API createEnv empty only (`api/stacks.go:106`), API copyEnv all three (`api/envs.go:132-137`), config none (`apply:907-919`), PR hook none (`web/prhook/handler.go:501-510`).
- [A] Apply policy: web only on managed stacks and coerces (`web/project/handler.go:853-860`), API any stack and 400s (`api/envs.go:177-183`), config overwrites `ApplyPolicy`/`Color` every apply even when undeclared (`apply:958-962`).
- [A] Env colour: web refuses on managed (`web/project/handler.go:2126`), API accepts (`api/envs.go:162-189`).
- [A] Variable name rule: web `secretNameRe` (`web/project/handler.go:2293,2591`, `web/org/settings.go:410`), API any non-empty (`api/variables.go:120`).
- [A] Generate: web overwrites a live value (`components/varsedit.templ`), API and config skip if set (`variables.go:149-153`, `apply:1799-1809`).
- [A] Tile plain vars: web `editGate` stages (`web/app/handler.go:1172-1191`), API `rejectManaged` 409 (`variables.go:91`); tile secret: web no gate and no regex (`:1089-1108`), API refuses the whole write on a managed stack.
- [A] Upper-env rule twice (`apply:59` isUpper vs `envnet.UpperEnv` `envnet.go:110`); web `SaveEnv` redeploys with neither (`web/app/handler.go:1202`).
- Duplication: `promoteEnv` byte-identical (`web/project/releases.go:171-190`, `api/releases.go:170-189`); API `envOut` drops type, colour, position, policy, branch.
- Round 2: [A] home env slug `stack` reserved by api only; a web env named "Stack" is a raw UNIQUE 500 (`store/repo/models.go:192-195`, `001_initial.up.sql:126`, `web/project/handler.go:322`) (g-store). [B] env delete leaves `staged_changes` and env `node_positions` on every surface (`sql:271-281`, `sqlite/stacks.go:378-381`); `Delete` must clear them itself. [A] CLI `-y` doubles as `force` on env rm/reset (`promote.go:239-240,274`); web has no running check (g-cli). [B] boot `BackfillVariables` upserts with no `ClearWaiting*` (`envops/backfill.go:23-45`) (g-boot-workers). [B] unaudited secret writers beside apply/orgconf: `envops.go:219-231,274-281,548-553`, `managedtiles.go:383-389`, `provision_s3.go:177-178` (g-store). [A] org `vars:` keys never validated by `orgconf.Parse` (`orgconf.go:160-241,706-712`) (g-config-small). Share-link mint rules sit in the handler (`web/project/handler.go:2351-2390`); links survive `MoveStack` (`web/org/handler.go:148`) (g-access).

### 2.3 TileService + TileLifecycleService + TileTelemetry

Rows: list, get, create, update settings, scale, rename, delete, stop, restart, toggle cron, run now, stop run, logs, run logs, metrics, port-forward, raw container ops. 9 flagged rows (7 A, 5 B, overlapping).

Methods, `TileService`: `Create(ctx, stack, env, stackconf.TileConf, actor) (*Tile, error)` (`validateTile` is the widest rule set; stage-vs-write from `ConfigManaged()`/`UIEdits()`); `Update(ctx, t *Tile /*full*/, changed, actor) error` (limits/timeout/healthcheck/source/command/kind rules once; side effects from `apply:1404`: store, cron reload, `Proxy.SyncTile` when proxy-only, `Deploy.RedeployIfRunning` otherwise); `Delete(ctx, t, actor) error` (attached-volume guard, `TearDown`, `Proxy.DropTile`, `Slice.DetachAllOf`, `Backup.DropForTile`, store, `Scheduler.ReloadCron`; replaces web `:1632-1640`, api `teardownTile`, config `deleteTile`); `Rename(ctx, t, newSlug) error` (TearDown, `RenameTile`, `Proxy.SyncTile`, redeploy; today `moved.go:306-311` skips the proxy write); `Gate(ctx, stackID, GateKind) (stage bool, err)` (editGate vs rejectManaged as one enum).
Methods, `TileLifecycleService`: `Stop`, `Restart`, `ToggleCron` (cron only), `RunNow` (cron/function only), `StopRun`; every status change notifies. `TileTelemetryService.Logs` (one resolver, today `web/app/handler.go:114-125` and `api/logs.go:188-212`), `Metrics` (one range parser).
Call sites: `deploy.Engine.Enqueue` from 14 handler sites; `jobs.LoadSchedules` from 18; tile field-list copies 11 (header).
Calls: ProxyService, StagingService, SchedulerService, DeployService, SliceService, VariableService, BackupScheduleService, VolumeService (guard), ConnectorService (org check), NotificationService, cluster.

- [B] Config apply redeploys a service on any non-proxy field (`apply:1487-1495`) and rebuilds a cron on source change (`:1499-1500`); web `SaveSettings` (`web/app/handler.go:1588-1602`) and API `patchApp` (`api/apps.go:453-460`) only write the row. Port, limits, healthcheck, command, replicas edits take effect on the next manual deploy. API also skips the proxy rewrite (calibration).
- [B] Delete tile: API skips provision detach and volume guard; web skips `jobs.LoadSchedules` (`web/app/handler.go:1632-1644`); all six delete sites skip the backup scheduler reload.
- [B] Rename tile (`moved.go:306-311`, config only) removes the proxy route and never rewrites it; `restartTile` (`apply:594-615`) redeploys or does nothing, and the engine never calls `WriteApp` (1.11). `Rename(ctx, t, newSlug)` owns TearDown, `RenameTile`, `Proxy.SyncTile`, redeploy, in that order (2.6).
- [B] Stop/Restart/ToggleCron: web notifies (`web/app/handler.go:341,364,370,411`), API never (`api/lifecycle.go:30-97`).
- [A] Gate on delete: web `editGate` stages a structural op (`web/app/handler.go:1612`), API `rejectManaged` (`api/apps.go:468`). Gate on patch: API `rejectManaged` `:183` should be `editGate` (calibration).
- [A] Kind checks one-sided: web `Deploy` refuses cron (`web/deployment/handler.go:78`), API does not (`api/apps.go:480`); API `toggleCron`/`runApp` require cron/function (`api/lifecycle.go:81`, `api/apps.go:502`), web `ToggleCron`/`RunNow` accept any tile (`web/app/handler.go:398,193`) and `RunNow` nil-derefs `h.jobs` `:201`.
- [A] Create: kind set (web accepts volume `web/project/handler.go:500-503`, API service/cron/function `api/apps.go:103`, config six kinds `stackconf.go:1009-1129`); port on non-ingress kind stored on web `:570`; connector from another org silently blanked on web `:551-555`, 400 on API, unchecked in config `apply:1890,1897`; timeout default 30 for every kind on web `:568`; webhook token `RandomHex(24)` vs uuid; empty slug accepted by API; third managed-gate spelling `:586,603`.
- [A] Update settings: limits, timeout, healthcheck, `source_type`, `volumes`, `storage`, `command`, cron empty image all differ (row 1.1). Scale is web-only over HTTP and config; API drops `replicas`/`node_group` silently (`types.go:81`, `tile.go:175-206`).
- [A] Rollback: API trims tag, web raw; neither applies `UpperEnv`.
- [A] API `createApp` returns 500 when the cron reload fails after commit (`api/apps.go:162-164`); 14 other sites discard.
- Round 2: [A] the CLI is the only surface that strips `schedule`/`command`/`timeout` off a non-cron tile (`internal/cli/cmd/tile.go:126-129`); api stores them on any kind (`apps.go:150-153`); web create hand-switches on three kind literals and skips `runpolicy` (`web/project/handler.go:492-503,570`) (g-cli, g-config-small). [A] `--cpu` alone zeroes memory, `--dockerfile` alone clears context (`tile.go:247-263`, `apps.go:248-253`); "limits and build are atomic pairs" is written nowhere server-side. [A] SQL tile defaults are dead, `CreateTile` writes every column (`sql:288-316`, `sqlite/tiles.go:43-58`), nine callers re-supply them differently (g-store). [A] `tiles.status` has 14 writers and one observer (`reconcile.go:24-68`); web `Stop` on a cron parks it `stopped` for ever (1.11, 2.15) (g-boot-workers). [A] boot `SweepStaleRuns` says `error`, user cancel says `cancelled` (`sqlite/deployments.go:55-69`, `engine.go:313`). Four CLI name resolvers beside `slugpath.go:94` (`helpers.go:132-223`, `backup.go:16-35`). `UpdateTile` never writes status, slug, home_node, digests (`tiles.go:160-217`); slug moves only via `RenameTile`.

### 2.4 DeployService + ReleaseService + PlanService + StagingService + ApplyEngine (stacks, plans, deploys)

Rows: list/create/rename/delete/move stack, bind config, ui_edits, plan now, approve, reject, preview, list plans, export, staging, deploy, rollback, cancel, deployment status, releases, promote, CI park, image watch, run policy. 11 flagged rows (9 A, 3 B, overlapping).

Methods, `StackService`: `Create(ctx, orgID, name, desc)` (non-empty slug, unique per org, production env); `Rename` (wraps `Applier.RenameStack`; `moved:` path calls it too); `Delete` (`TeardownStack` + `DeleteStack` + `Scheduler.ReloadCron`); `Move`; `BindConfig`/`Unbind` (connector-in-org, staged-row drop, plan run).
Methods, `DeployService`: `Deploy(ctx, tile, trigger)` (volume, kind, upper-env, own-repo rules once; web, api, prhook, imagewatch, stackconf route through it); `Rollback`; `Cancel`; `Park`/`Release`; `IsLive(status)` (one live/terminal set); `RedeployIfRunning`; `ClearWaiting(ctx, ownerKind, ownerID, name)`.
Methods, `ReleaseService`: `List`; `PromoteTarget` (rung rule once); `Promote(ctx, stack, env, commit, planID, force)` (one enqueue via `ApplyQueue`, plan-id dedupe, pending check).
Methods, `PlanService`: `Plan`, `PlanEnv`, `PreviewBundle`, `Approve → enqueue`, `Reject`, `Get`, `List`, `Export`, `SetInput`, `RunApplyJob`; owns every `SetConfigPlanStatus/Error` write (today 7 sites). `StagingService`: `Stage`, `DesiredEnv`, `DesiredDomains`, `Review`, `Apply`, `Discard`, `DiscardOne`; owns the editGate decision (`web/app/handler.go:1234`). `ApplyEngine.Reconcile(stack, resolved, opts, force)`: each `createTile/updateTile/deleteTile/syncDomains/applyDomainRes/applyVars/ensureSecrets/applyBackups/env write/RenameStack/applyMoves/applyMiddlewares/createSlice` becomes a call into the owning concept service. `ApplyQueue.EnqueueApply(ctx, stack, plan, force, promote)` single dedupe key.
Call sites: `stackconf`/`staging`/`envops`/`orgconf` from 84 handler sites (api 25, web 47, prhook 12); `service/` has zero; `workqueue` from 8; `infra/deploy` from 31.
Calls: ApplyEngine → TileService, DomainService, DomainResourceService, EnvironmentService, VariableService, BackupScheduleService, SliceService, ProxyService, DeployService, SchedulerService. PlanService → ApplyQueue, NotificationService. StackService → EnvironmentService, PlanService, StagingService, ConnectorService.

- [A] Cancel deployment: API `loadDeployment` `api/apps.go:570-579` uses `requireTile(c, id, false)`, so a read-only member with a `ScopeAppsDeploy` key can cancel; web requires write role (`middleware/orgctx.go:258-264`).
- [A] Promote with plan: web `Force:true` (`web/project/releases.go:323`), API body force (`api/releases.go:159`); both hand-build `ApplyJob` under the stack id, bypassing `EnqueueApply`'s plan-id dedupe (`stackconf/job.go:112-115`); neither checks pending. Plain promote runs `Applier.Promote` inline on request ctx (`releases.go:335`, api `:146`), the failure `job.go:52-57` exists to avoid.
- [A] Plan now / approve: API requires binding (`api/config.go:38-53,49-51`), web does not (`web/project/handler.go:768-773,827-838`); web reports `plans[0]` only; pending check 400 vs 409.
- [A] Create stack: neither web (`web/project/handler.go:165`) nor API (`api/stacks.go:40`) checks empty or duplicate slug; raw UNIQUE error.
- [A] Deployment live-state set copied four times (`web/deployment/handler.go:123,198`, `internal/cli/cmd/deploy.go:205`, `engine.go:310`, `api/releases.go:86`); `waiting_ci` ends the web poll and SSE.
- [A] Trigger deploy: web refuses cron, API does not; trigger strings differ.
- [B] Rename stack via `moved:` (`orgconf/moved.go:77-78`) writes Name/Slug only; `Applier.RenameStack` (`apply:552-576`) stops and redeploys because service names embed the slug (`apply:530-535`). Display name becomes the slug.
- [B] Bind config: web drops staged rows in every env (`web/project/handler.go:733-742`); `orgconf.applyStack` (`orgconf.go:895-902`) binds a UI-managed stack and drops nothing.
- [B] Stack delete tears down every env with no cron reload on either surface (`web/project/handler.go:460`, `api/stacks.go:64`, `envops.go:598`).
- Staging discard has no gate (`web/project/staging.go:75-107`); `replanAsync` drops errors on `context.Background()` (`web/project/handler.go:2525-2538`); stack export copy-pasted (`releases.go:353-370` = `config.go:268-288`).
- Round 2: [A] CLI `pickRelease` (`promote.go:131-196`) is the only resolver of `head`/`latest`/`--from`/short sha; api and `Applier.Promote` accept any string and never check the commit is built; three force policies (g-cli). [A] plan-preview registered `config:read` (`v1.go:244,281`) but gated write (`config.go:39-52`, `orgconfig.go:55`) (g-access). [A] web `loadDeployment` skips tenancy when the tile row is gone (`web/deployment/handler.go:44-52`) (g-access). [A] image auto-update bypasses `UpperEnv` (`watcher.go:175`) (g-boot-workers). [A] stack create never pre-checks `UNIQUE (org_id, slug)` (`sql:269`); three copies insert `production` though `CreateStack` inserts the home (`sqlite/stacks.go:144-166`) (g-store). [A, B] orgconf binding skips the connector check, repo normalisation and staged drop (`orgconf.go:895-902,621-630`) (g-config-small). Pending-moves filter twice (`web/project/handler.go:978-989`, `apply:1929-1943`); deployments have no atomic claim (`engine.go:124,310,456` vs `sqlite/workitems.go:48-76`); no `GET` one org plan, so the CLI approves blind (`v1.go:242-246`, `org.go:647-652`). `DeployService` needs an exported terminal predicate (CLI hard-codes `deploy.go:919`) and `RecoverInterrupted` in place of `SweepStaleRuns`.

### 2.5 OrgService + MemberService + OrgPlanService + AccessService + AuthService + APIKeyService (orgs)

Rows: 35 org/member/auth rows (1.8). 12 flagged rows (10 A, 2 B, one A+B).

Methods, `MemberService`: `Invite(ctx, org, actor, email, role, days)` (one role whitelist, one expiry default and cap, member-exists check, mails when configured); `Resend`; `Reinvite`; `Revoke`; `SetRole` (strict, last-owner guard); `Remove` (404, last-owner guard); `Accept(ctx, inv, user)`. `OrgService`: `CreateDraft`; `Rename` (slug, collision, anti-squat, registry-images guard; used by web and `orgconf.Apply`); `Delete`; `FinishSetup` (calls DomainResourceService); `SetDefaults` (Merge/Check, managed refusal, `Proxy.Resync`); `SetEnvColor`; `Bind`. `OrgPlanService`: `Plan`, `Preview`, `Approve → ApplyQueue` (today inline), `Reject`, `List`, `Get`, `Export`; one authorisation rule; one `orgconf.Runner`. `AccessService`: `IsMember`, `CanWrite`, `IsOwner`, `Ready(org)` replacing six functions (`middleware/orgctx.go:206,220`, `setupgate.go:113`, `web/org/members.go:31`, `api/auth.go:217,257,300`, `api/members.go:26`). `AuthService` (exists, `service/auth.go`): add `NormalizeEmail`, `ValidatePassword`, `UpdateProfile`, `SetAdmin`/`SetActive` (last-admin, admin-not-disabled rules from `web/settings/handler.go:534-546,569`); `KeyAuth` to share the active-user rule. `APIKeyService.Mint/Revoke/ListForUser` (one scope filter, one token format).
Call sites: `orgconf` runner built inline at `api/orgconfig.go:19` and `web/prhook/handler.go:275`; `AuthService` bypassed by `account.SaveProfile` (`:106`), `settings.ToggleUserAdmin/Active` (`:555,575`), `config/sharelink/sharelink.go:53,94`.
Calls: MailService, ProxyService, DomainResourceService, OrgRegistryService (`OrgHasImages`), runtime (builder), `orgconf.Runner` → StackService, DomainResourceService, VariableService, StorageService, ManagedInstanceService.

- [A] Org plan authorisation: web owner-only (`web/org/settings.go:217`, used at `web/org/config.go:52,112`, `web/org/plans.go:38`); API `planOrg` `api/orgconfig.go:28`, `requireOrgPlan` `:111`, `previewOrgPlan` `:55` use `requireOrgWrite` (owner or member), `listOrgPlans` `:86` any member. A member can apply an org plan (rename org, create/delete stacks) from the CLI.
- [A] Org defaults: web owner + 409 on `ConfigManaged` (`web/org/registry.go:262`); API member, no managed check (`api/settings.go:47-57`); next apply overwrites (`orgconf.go:687-694`).
- [B] API invite never mails: `api/members.go:210-219` vs web `mailInvite` `web/org/members.go:285`; `*API` has no Mailer (`v1.go:57`). CLI text says the link is the credential (`cmd/members.go:68`), web flash promises email (`:311`).
- [A] Invite bounds: web 1..365 else 14 (`web/org/settings.go:598`), API ≤0 → 7, no upper bound (`api/members.go:211-213`); member-exists check API-only (`:77-86`).
- [A] Role change: web bad role 400 (`web/org/members.go:327`), API silently `member` (`api/members.go:108`). Remove: web deletes without 404 (`:355-360`), API 404 (`:128`).
- [A] Rename anti-squat only on web (`web/org/members.go:101-116`); `orgconf.go:424-443,660-673` skip it.
- [A] Password rule in three handlers, none in the service: `web/auth/register/handler.go:44` `PasswordStrength`, `web/auth/invite/handler.go:91` len≥8, `web/account/handler.go:121` len≥8; `service/auth.go:34,76` hash anything.
- [A] Email case: `web/account/handler.go:78` trims only; login/register/invite lowercase; `GetUserByEmail` exact (`store/repo/sqlite/users.go:29`). Profile save with capitals can lock the user out. Not runtime-verified.
- [B] Org plan apply runs inline on the request context (`api/orgconfig.go:125`, `web/org/config.go:214`).
- [D] `service/admin.go:16` swarm import.
- Duplication: API-key minting (`web/account/handler.go:201-222` vs `web/cli/handler.go:96-108,153-162`); last-owner guard; six gate functions; API `listOrgs` returns drafts (`api/slugpath.go:109-131`).
- Round 2: [A, SEC] `ToggleUserActive` (`web/settings/handler.go:563-580`) revokes nothing: sessions (`web/server.go:162-164`) and keys (`api/auth.go:163-166`) run on; `Active` is checked only at `service/auth.go:117` (g-access). [A] key write scopes are granted against the active cookie org (`web/account/handler.go:201-205,260`, `web/cli/handler.go:55,96`, `orgctx.go:148-154`) while keys have no org column; demotion leaves scopes; only per-request `requireOrgWrite` holds, and `requireStackAccess`/`requireEnvAccess` are tenancy-only (`auth.go:276`, `resolve.go:197`) (g-access). [A] CLI `Exchange` is a third minting site, unauthenticated behind a rate limit (`web/cli/handler.go:139-162`); no api/cli key list or revoke (g-leftovers). [A] wizard steps mix owner and member (`web/org/setup.go:93,171,416` vs `web/org/settings.go:529`, `web/settings/handler.go:441`; `setupgate.go:403-405`) (g-access). [A] the store never folds email case (`sqlite/users.go:559-578`, `sql:341`); eight handler sites lowercase (g-store). [A] `--role admin` becomes `member` silently (`api/members.go:232-237`, `cli/cmd/members.go:358,376`) (g-cli). Web owner/admin gates answer 404, api 403 (`members.go:56`, `settings.go:225`, `server.go:197` vs `api/members.go:37`, `auth.go:210`); draft gate in five places (`orgctx.go:183-186`, `setupgate.go:328,395`, `variables.go:328`, `auth.go:292`, `helpers.go:52-66`).

`AccessService` brief (from `shards/g-access.md` §3). Owns "may this principal do this verb on this resource" so no handler on any surface computes a role again. (1) One principal type for session and key: user, admin flag, org roles loaded once (today `OrgContext` `orgctx.go:58` and `KeyAuth` `auth.go:169-179` separately), scopes for keys; `Active` checked at resolution, not only at login. (2) Resource-rooted checks: every method takes the resource (org, stack, env, tile, deployment, backup, provision, domain, plan, link) and walks to its org as `requireTile` `auth.go:313` and `RequireStackAccess` `orgctx.go:258` do; `ReadOnlyGuard` `:268`, `CanWrite` `:148` and `IsOwner` at `web/project/handler.go:1782` shrink to UI hints. (3) Levels as one ladder: Read (member), Write (owner or member), Owner, Admin, plus Draft as an org flag replacing five copies; the wizard allow-list `setupOpenRoutes` becomes a per-operation attribute. (4) Per-operation table: `registry.credential.create = Owner`, `deployment.cancel = Write`, `orgplan.approve = Owner`, `orgdefaults.set = Owner`; both surfaces call `Require(ctx, principal, verb, resource)`; the drift rows in g-access and 1.8 are exactly where the surfaces picked different levels for one verb. (5) Scopes enforced in the same call, validated against current roles in the target org at use time; `secrets:read` applied by VariableService, not `variables.go:28`. (6) Revocation reacts to deactivate, remove, demote, move stack (sessions, keys, share links, ws rooms); today nothing reacts. (7) Websocket `JoinRoom` (`main.go:307`) asks `CanRead(principal, room)` with a subject func wired to the cookie. (8) One error policy: 404 not yours, 403 not at this level, 409 draft or managed, decided once and translated per surface (`SetupPending` `setupgate.go:380` sketches it). Call sites that stop deciding: every helper in the g-access §0 table, `orgForCreate`, `requireConfigStack`, `requireBackup`, `resolveResourceTenancy`, web `loadStack`/`load`/`loadTile`/`loadDeployment`/`loadBackup`/`settingsOrg`/`ownedOrg`/`ownedSettingsOrg`/`loadOrg`, and both scope-grant copies.

### 2.6 ProxyService (cross-cutting)

Rows: every proxy write in 1.1, 1.3, 1.4, 1.5, 1.6, 1.9, 1.10. 53 call sites: 44 writes (web 23, api 12, config 8, boot 1), 4 reads, 5 `forward.Registry`. Seven rule sets around one write surface (`shards/x-proxy.md` §2): R1 `editGate` (web app rows), R2 `rejectManaged` (api + web auto domain), R3 `rFU` (web db), R4 inline `ConfigManaged()` 409 (web org defaults, web env delete), R5 no managed gate (web cert, web server/stack/env settings, api settings all levels, admin pages), R6 config apply, R7 boot/webhook. "Add domain" crosses four sets. `RemoveApp` errors ignored at every site; `Resync` logged everywhere; `EnsureTraefik` fire-and-forget in five of six sites. `infra/deploy` never calls `WriteApp`: routes are written at domain/settings time and by `Resync` only.

Methods: `SyncTile(ctx, tile)` (`ListDomainsByTile` + `WriteApp`, skip cron/function/managed-non-HTTP); `DropTile(ctx, tileID)` (`RemoveApp`, logged); `SyncStackMiddlewares`; `Resync`; `SetCustomEntries`; `SetStaticOverride`; `SetTrustedProxies`; `SetDNS`; `SetResourceACME`; `SetRegistryDomain` (`UpdateRegistry`, `registry.EnsureManaged`, `WriteRegistry`); `AccessLog`; `CurrentStatic`; `Forwards`/`AddForward`. Nothing above it holds `*proxy.Proxy`; `EnsureAutoDomain` takes the service.
Called by: TileService, DomainService, DomainResourceService, EnvironmentService, StackService, ManagedInstanceService, SettingsService, RegistryAdminService, PREnvService, OrgService.

- [B] Web managed-db delete never `RemoveApp` (2.1). [B] API `patchRegistry` writes the route without `EnsureManaged` (2.8). [A] Web `SetDomainCert` has no managed gate, API has (2.7). [A] Web `ToggleDomainHTTPS`/`DeleteDomain` rewrite the wrong tile's route (2.7). [A] Settings patch on a managed org/stack resyncs from API and web stack/env pages, web org page refuses (2.9).
- [B] `moved.go:308` removes a renamed tile's route and never rewrites it. `restartTile` (`apply:594-615`) is the only thing that runs after: managed → `DBs.Deploy`, running service → `a.deploy`, everything else returns; no branch touches the proxy, and the deploy engine never calls `WriteApp` (1.11). A `moved:` rename drops the tile's traefik route until a domain edit or `Resync` puts it back. `TileService.Rename` must call `ProxyService.SyncTile` after `RenameTile`. `EnsureTraefik` after ACME patch is synchronous with the error discarded (`api/domainresources.go:224`).
- Round 2: [B] boot `EnsureManaged` never renders `registry.yml` and `Resync` (`proxy.go:577-624`) skips it (`:715`); only `web/settings/handler.go:624` and `api/registry.go:319` call `WriteRegistry` (g-boot-workers). [A] trusted-proxy CIDRs: seed skips a bad line (`seed.go:54-57`), handler 400s (`web/settings/proxy.go:74-77`); Cloudflare fallback differs (`:67` vs `:83-89`); installer `answers.go:565` is a third `netaddr.ParseTrusted` caller: `SetTrustedProxies(cidrs, trustCF)` with one policy. Recorded: the deploy engine never calls `WriteApp` (1.11), so ProxyService is called by Tile and Domain services, never by the engine; `Resync` should render the registry route or boot calls `EnsureRegistryRoute`.

### 2.7 DomainService + DomainResourceService

Rows: 1.6. 7 flagged rows (5 A, 2 B).

Methods, `DomainService`: `Attach(ctx, tile, DomainSpec{Host, Path, Port, HTTPS, ForceHTTPS *bool, RedirectTo, Rule, Priority, Middlewares})` (host+path ownership with same-tile rule exception, squat, wildcard-needs-DNS, middleware refs, managed `SpeaksHTTP` + engine port, redirect port default, one `Proxy.SyncTile`); `EnsureAuto`; `SetTLS(ctx, domainID, actor, TLSPatch)` (domain-belongs-to-tile check); `Detach`; `ListByTile`. `DomainResourceService`: `Create(ctx, level, ownerID, host, opts{IncludeEnvOnDefault, ACMEEmail})` (`ValidateResourceHost`, `HostTaken`, squat once for every level, instance owner canonicalised, managed-owner refusal); `SetACME` (+ `Proxy.RewriteStatic`); `SetIncludeEnv`; `Delete`; `Visible`; `AutoHost`.
Call sites: web app ×5, web db ×2, api ×5, `syncDomains`, `EnsureAutoDomain`; resources web ×7, api ×4, `applyDomainRes`, `orgconf.applyDomains`, seed.
Calls: ProxyService, StagingService, SettingsService (dns_provider), OrgService/StackService (owner lookups).

- [A] Web `ToggleDomainHTTPS` (`web/app/handler.go:1810`) and live-branch `DeleteDomain` (`:1908`) act on `:domainID` without `d.TileID == a.ID`; `h.load` `:430` only proves write on tile `a`'s stack. Cross-tenant flip or delete by id. API resolves the tile from the domain row (`api/domains.go:92-99`, `api/lifecycle.go:197-201`).
- [A] Attach domain, three rule sets: API skips squat, wildcard-DNS, `checkMiddlewares`, kind/`SpeaksHTTP`, cannot set rule/priority/middlewares (`domainIn` `types.go:200`), leaves redirect port 0 (`http://alias:0`, `proxy.go:447`); HTTPS defaults differ (web checkbox off = both off; api/config nil = both on; CLI `--no-https` = https off, force on).
- [A] Cert/HTTPS gates: web cert none (`:1846`), web https `editGate`, API `rejectManaged` for both; `force_https` API-only.
- [A] Domain resources: squat only on web org (`web/org/settings.go:545`); API accepts and drops `acme_email` (`api/domainresources.go:244`); web has no ACME field; web server delete removes any id (`web/server/handler.go:226`); instance owner id differs per writer (latent C).
- [B] Stack-file `acme_email` applied on create only (`stackconf/plan.go:533`, `apply:439-445`); org file diffs and applies (`orgconf.go:476,826-830`); API patch skips `rejectManagedOwner` and ignores the `EnsureTraefik` error (`:338`).
- [B] Registry TLS domain: see 2.8.
- Duplication: middleware ref check twice; trusted-proxy parse twice; five `envops.Ops{}` literals.
- Round 2: [A] "is this a domain we can serve" has three grammars: installer `CheckRoot` (`internal/installer/answers.go:518-549`), seed `ToLower` + `HostTaken` (`seed.go:33-46`), `envops.ValidateResourceHost` (`:287-299`), plus `proxy.routablePanel` (`proxy.go:797-802`); `localhost` is refused by the installer and accepted by the panel (g-leftovers). [A, B] orgconf domains: no squat (`orgconf.go:461-482,811-823`), `Declared=true` with acme, acme update writes the row and never `EnsureTraefik` though `:477` promises a restart (g-config-small). [A] SQL `https`/`force_https DEFAULT 1` dead (`sql:107,114`, `sqlite/deployments.go:83-90`); uniqueness `(host, path, rule)` `sql:370` mirrored by web app only (g-store). CLI fills the stack-level owner (`domain.go:611-616`) and resolves host to id (`:647-660`) that the api refuses to guess: `Create(level, owner?)`, `Resolve(hostOrID)` (g-cli).

### 2.8 RegistryAdminService + OrgRegistryService + ConnectorService + PREnvService

Rows: 1.9. 7 flagged rows (6 A, 1 B).

Methods, `RegistryAdmin`: `List`; `AddExternal`; `Delete` (refuses `Managed`); `SetManagedDomain` (`UpdateRegistry` + `EnsureManaged` + `Proxy.SetRegistryDomain`); `PatchCredentials` (fallout decided, not guessed). `OrgRegistry`: `Credentials`; `CreateCredential` (one "managed registry required" rule); `DeleteCredential`; `Images`; `Tags`; `DeleteTag` (one name resolver `orgImage`, one in-use matcher); `MintToken` (the three identities). `ConnectorService`: `Begin`; `Complete`; `Delete` (reference check over tiles/stacks/org); `Connected(ctx, orgID)` (replaces four inline filters); `ForTile`/`CheckOwned` (replaces web/api/planner/engine copies); `Repos`, `Branches`, `FileExists`. `PREnvService`: `Get`; `Set` (editGate when the file owns `comment`/`status`); `RotateSecret`; `HandlePullRequest` (one repo-match rule for both hook routes); `HandlePush`.
Call sites: `registry` 11, `githubapp` 10, `cigate` (janitor + prhook).
Calls: ProxyService, `infra/registry`, `infra/githubapp`, EnvironmentService (create ephemeral env, layout, teardown), PlanService (`RunEnv`, `Run`, `Preview`, `ApplyResolved`, `EnqueueApply`), DeployService (`Enqueue`, `Park`), SchedulerService, NotificationService. Called by TileService (connector check), PlanService (`State.OrgConnectors`), CIGate.

- [A] Web `DeleteRegistry` (`web/settings/handler.go:657`) has no load and no managed guard; API 409s (`api/registry.go:334-338`). A POST deletes the managed row; boot recreates it with a fresh password (`infra/registry/registry.go:41-55`), breaking every org's derived system credential until `EnsureSystemCredential` (`token.go:285-309`).
- [B] API `patchRegistry` (`api/registry.go:298-324`) skips `registry.EnsureManaged` that web runs (`web/settings/handler.go:616-622`); traefik routes to an alias the service may not carry. API also patches any registry incl. username/password. External registry rows have no consumer anywhere in `infra/`.
- [A] Create org credential: API 503 without a managed registry (`api/registry.go:36-42`); web mints without one (`web/org/registry.go:165-188`).
- [A] Delete image tag: name validation and in-use matcher differ (`web/org/registry.go:223-226,152` vs `api/registry.go:147-166,209-211,252`).
- [A] Set tile connector: four copies of the org check; planner allows when `len(OrgConnectors)==0` (`plan.go:885`), engine silently nils (`githubapp.go:326-329`).
- [A] PR env settings: no `editGate`/`rejectManaged` (`web/project/handler.go:2647`, `api/lifecycle.go:397`) though the file overwrites `comment`/`status` at PR open (`web/prhook/handler.go:488-500`); `pr_envs.enabled` never persisted (`:476`).
- [A] Stack-scoped hook (`web/prhook/handler.go:110`) skips `stackTracksRepo` and `updatePlanComment` that the connector route runs (`:157-161`).
- Delete connector has no reference check (`web/settings/handler.go:498-512`; `cigate.go:69-73`, `stackconf/runner.go:47-49` fail later).
- Round 2: [A, SEC] org registry credential create/delete and tag delete are owner on web (`web/org/registry.go:166,193,218`) and member on api (`api/registry.go:26-34,101,200`) (g-access). `registries.password` plaintext (`sqlite/infra.go:31-51`) beside `DBPassword`, `BasicAuthPassword`, `WebhookToken`, `ProtectPassword`; `secrets.Encrypt` is applied only in `store/repo/sqlite`, so which service owns each plaintext field is an open question (g-config-small). Boot `EnsureManaged` goroutine and the handler call are unguarded against each other (`main.go:285`, `settings/handler.go:620`); nightly `registry.GarbageCollect` runs only from `main.go:376-402` and could be a method the maintenance page calls (g-boot-workers). `PREnvService.Update` should be sparse: the CLI read-merge-writes because PUT replaces `enabled` (`internal/cli/cmd/lifecycle.go:625-647`, `api/lifecycle.go:408`) (g-cli).

### 2.9 VolumeService + StorageService + VolumeOwnership

Rows: 1.2. 9 flagged rows (5 A, 3 B, 1 C).

Methods, `VolumeService`: `Create(ctx, stack, env, VolumeSpec{Name, AttachTo, MountPath, VolumeName, MaxSizeMB}, mode)` (one target-kind rule, path, name grammar, slug, stage vs direct); `Attach(ctx, vol, target, mountPath, mode)` (redeploys old and new target); `SetLimits`; `Delete(ctx, vol, mode)` (attached guard, teardown, redeploy former target); `AttachedTo(ctx, app)` (the one filter). `StorageService`: `Create(ctx, StorageSpec)` (one `ServerID` rule, probe-on-create); `Probe`; `Delete` (one consumer scan for `slug/path` and `${{ org.storage }}` lines, `DropOrgShareVolumes`, managed-org check); `DeclarePath`; `RemovePath`; `Attach(ctx, tile, line, mode)` (service-only rule, `ValidateAttach`, redeploy decision); `Detach`; `Consumers`. `VolumeOwnership` (class C prerequisite): `VolumesOf(ctx, tile)`, `OwnerOf(ctx, name) (kind, ownerID, ok)`, `NodeOf(ctx, tile)`. `VolumeMoveService` (exists, `infra/volmove`): add `Targets(ctx, tile)` from `web/server/nodes.go:422`.
Call sites: `storagetiles` 6; 13 ownership implementations (1.2 note); `volmove` from web only.
Calls: DeployService, StagingService, ProxyService (`DropTile`), SliceService (detach on delete), NodeService (raw volumes), OrgService (`ConfigManaged`), cluster.

- [B] Re-attach from A to B: web enqueues both (`web/app/handler.go:794-801`); config deploys only the new target (`apply:1393-1399`). Stale bind on A.
- [B] `DELETE /apps/:id` with a volume id runs `teardownTile` and never enqueues the attached service (`api/apps.go:463`, `requireTile` no kind check), unlike `deleteVolume` (`api/volumes.go:104-107`); web refuses deleting an attached volume (`:1616`), neither API path does.
- [B] Web `DeleteStorage` (`web/server/storage.go:105-125`) has no `OrgID` branch: skips `DropOrgShareVolumes` and the managed-org check, cannot see `${{ org.storage.NAME }}` refs.
- [A] Create volume: target rule three ways (`web/project/handler.go:537` service && !managed; `api/volumes.go:44` any non-managed non-volume app, cron allowed; `apply:1383` any tile). Web can create detached, API cannot. API takes `volume_name`/`max_size_mb`, web does not; no patch key or `TileConf` field for either.
- [A] API `createStorage` hard-codes `ServerID:"local"` (`api/storage.go:100`); web uses `:id` (`web/server/storage.go:80`). CLI pools always probe/mount on the manager. Config org shares never probed (`orgconf/storage.go:213-217`).
- [A] Storage attach: config service-only (`stackconf.go:1064-1066`); API writes for any kind (`api/apps.go:297-321`, before the `service` block `:332`); web no kind check (`web/app/storage.go:71`); web gate `RU` reject-only where sibling edits use `editGate`.
- [A] Sub-path name: web slugify-then-empty (`web/server/storage.go:150-152`), API empty-then-slugify (`api/storage.go:185,189`).
- [C] Raw docker volumes on a node (`web/server/handler.go:158,260,277`) have no row and no ownership check; `DeleteVolume` confirms against `docker volume ls` only (`:292-301`). Needs `VolumeOwnership.OwnerOf`.
- Round 2: [A] boot `ConvertLegacyMounts` (`envops.go:642-700`) is a fifth volume-tile writer: `uniqueSlug`, no managed gate, no target redeploy; it becomes a `VolumeService.Create` caller (g-boot-workers). CLI defaults the volume name from the mount path (`tile.go:644-650`), nowhere else (g-cli). [B] org shares: api probes on create (`api/storage.go:117-135`), orgconf inserts `Status:"unknown"` and is the only update path (`orgconf/storage.go:213-233`) (g-config-small). `storage CHECK` one-owner and `server_id` default `local` live in SQL and store (`sql:460`, `sqlite/storage.go:24-26`) (g-store). `VolumeMoveService` consumer is the fifth "Deploy then status" copy (`volmove.go:675-680`).

### 2.10 BackupScheduleService + BackupDestinationService (+ BackupRunService exists)

Rows: 1.7. 5 flagged rows (3 A, 2 B).

Methods, `BackupScheduleService`: `Create(ctx, tile, ScheduleSpec{destRef, kind, mode, cron, tz, keep, enabled})` (dest resolution `authz.go:19,77,114`, kind derivation, `VolumeFor`, mode-on-dump rule, `Validate`, write, reload); `Update(ctx, b, spec)`; `Delete`; `DropForTile(ctx, tileID)` (what every tile-delete path calls); `ListByTile`; `Reload`. Panel self-backup is kind `stackr` on the same service (`Get`, `Save`, `Delete`, `Run`). `BackupDestinationService`: `ListFor(ctx, viewer)` (one visibility rule replacing `web/settings/handler.go:175`, `web/backups/handler.go:125`, `api/backups.go:37-43`); `Create` (trim, required, admin-for-global, probe); `Delete` (then `Scheduler.ReloadBackups`); `SetShared` (one `destinationUsers`); `Resolve(ctx, orgID, ref)`. `BackupRunService` = `infra/backup.Service` (`Start`, `StartRestore`, `LatestRestore`).
Call sites: `backup` 9; `backup.LoadSchedules` 10 (api 3, web 5, config 1, boot 1).
Calls: SchedulerService, TileService (`VolumeFor`, org of tile), TileService.Gate (file-owned `backup:`), workqueue, cluster, managedtiles, S3.

- [B] Destination delete cascades every schedule (`001_initial.up.sql:52`); neither `web/settings/handler.go:336-357` nor `api/backups.go:84-104` reloads. Tile delete cascades rows from six sites with no backup reload. Orphaned entries fire `Start` on missing rows every tick (`backup.go:100-102`, `work.go:109-112`).
- [A] Create schedule: kind (web `web/backups/handler.go:217-219`, API `api/backups.go:181-187` accepts dump on non-managed, config `stackconf/backups.go:39`); `VolumeFor` guard missing on config (`:217-230`); `backup.Validate` not called by config (second impl `stackconf.go:934-970`, tz rule differs); keep default 7 web vs 0 elsewhere; no `editGate`/`rejectManaged` on web or API for a file-owned key; stale comment `web/backups/handler.go:314-317`.
- [A] Update schedule: web clears cron/tz from the form (`:262-263`), API/CLI cannot clear tz (`api/backups.go:266-271`); config plans and applies `cur[0]` only (`backups.go:119,232`), extra panel schedules invisible and deleted on block removal.
- [A] Create destination: web trims (`web/settings/handler.go:304-308`), API raw (`api/backups.go:58-61`); web org route has setup gate, API `requireOrgWrite` has none.
- Duplication: `destinationUsers` verbatim twice; web `Run` nil-derefs `h.svc` (`web/backups/handler.go:299`).
- Round 2: [B] `quiesce` (`backup.go:404-417`) and restore recovery `stopFor` (`backup/work.go:165-187`) scale a service without writing `tiles.status`; the reconciler flips it twice per backup; `BackupRunService` should call `TileLifecycleService.Stop/Start` (g-boot-workers). [A] `keep_latest DEFAULT 7` dead (`sql:57`, `sqlite/backups.go:71-75`) (g-store). `UpdateBackupDestination` keep-secret-on-blank is dormant, both callers pass the decrypted row (`bk:46-62`, `web/settings/handler.go:682-697`, `api/backups.go:441`). CLI required-flag checks duplicate server rules (`backup.go:116,321,435`), removable (g-cli).

### 2.11 SettingsService + NodeService + AdminService (settings)

Rows: 1.10. 5 flagged rows (4 A, 1 D), plus the node rules.

Methods, `SettingsService`: `Get(ctx, level, id)`; `Set(ctx, level, id, patch, actor)` (load, `Merge`, `Check`, one config-managed policy per level, store, `Proxy.Resync`; refuse server-only knobs below server); `SetEnvColor`; `SetUIEditsDefault`. `NodeService` (grows `infra/nodes.Service`): `Drain` (pinned refusal `web/server/nodes.go:279`), `Remove` (manager guard, drain, swarm remove, burn keys, delete row `:335-363`), `SetGroup` (`:382`), `Activate`, `AddNode` + `agent.Ensure` (`:71`), `JoinScript`, `RawVolumeCreate/Delete` (via VolumeOwnership). `AdminService` (exists): add `SetCleanup`, `SetImageWatchInterval`; drop the swarm import (class D). `ProxyConfig*` methods live on ProxyService (2.6). `ConfigExport` folds into PlanService.Export / `orgconf.ExportYAML`. SearchService: read-only aggregate, lowest priority.
Call sites: settings web 4 + api 1×4 + config 2; proxy admin web 6, api 4, cli 5; nodes web only, 10 sites in `nodes.go`.
Calls: ProxyService, OrgService/StackService (managed flag), `infra/runtime`, `infra/agent`, `infra/placement`.

- [A] Org defaults gate: web owner + 409 managed (`web/org/registry.go:262`), API member, no managed check (`api/settings.go:47-57`).
- [A] Server defaults: web renames the server unvalidated (`web/server/handler.go:322`), any `:id`; API fixed to `"local"` (`api/settings.go:42`), never sets name.
- [A] Org plan role split (2.5).
- Stack/env defaults: neither web nor API checks `ConfigManaged` while config owns `defaults:` and replaces the blob (`apply:932-937,955-964`). Consistent with each other, inconsistent with org level.
- Node lifecycle rules (five) live only in `web/server/nodes.go`, not in `infra/nodes.Service`; any node API re-derives them.
- [D] `service/admin.go:16`.
- Parity doc stale: `ui_edits` default marked web/API settable, only `orgconf.go:676-677` writes it. Proxy escape-hatch helpers byte-identical twice; API drops the `EnsureTraefik` error (`api/proxycfg.go:93`).
- Round 2: [A] agent ensure has two entry policies: boot only with 2+ nodes, 10 retries (`main.go:463-484`); `AddNode` once, unconditional (`web/server/nodes.go:71`): `NodeService.EnsureAgent` with one retry policy (g-boot-workers). `nodes.Sync` writes `servers` rows from a GET (`nodes.go:32`, `nodes.go:135,216`); the ticker should own the write. [A] the panel service spec lives only in the installer (`internal/installer/steps.go:128-169`, copy `scripts/dev/deploy-test.sh:118-131`); `AdminService.Upgrade` sets `STACKR_IMAGE` only (`service/admin.go:156`) (g-leftovers, 2.15 InstallerSpec). [A] settings managed gate only on web org (`web/org/registry.go:262-265`); api all levels and web stack/env have none (`api/settings.go:44-75`, `web/project/handler.go:1022,2540`) (g-config-small). `ProtectPassword` plaintext at every level (`settings.go:28`). Nightly prune is a 24h sleep loop with no "run now" (`main.go:376-402`). Raw containers (2.15 ContainerService): stop guards any system container, remove only a running one, start and terminal none (`web/container/handler.go:202,212,224-235`, `terminal.go:36`) (g-leftovers).

### 2.12 SchedulerService (cross-cutting)

`ReloadCron(ctx) error` → `jobs.LoadSchedules`; `ReloadBackups(ctx) error` → `backup.LoadSchedules`. One nil guard, one error policy (today: `api/apps.go:163` returns 500, 14 sites discard, config `warn`s). `jobs.LoadSchedules` called from 18 sites (7 api, 9 web incl. webhook, 1 config, 1 boot); `backup.LoadSchedules` from 10. `envops.Teardown/CloneTiles/TeardownStack` and `Applier.CopyTile` do not reload themselves, so every caller must; two forgot.
Called by: TileService, EnvironmentService, StackService, PREnvService, BackupScheduleService, BackupDestinationService, ApplyEngine.

- [B] Stack delete on both surfaces (`web/project/handler.go:460`, `api/stacks.go:64`) and web direct-path cron tile delete (`web/app/handler.go:1632-1642`) never reload cron; stale entries tick and fail at `jobs.go:340-342`.
- [B] Destination delete and every tile-delete path never reload backups (2.10).
- Recorded, not decided: reloading inside `Teardown/CloneTiles/TeardownStack/CopyTile` would remove nine of the eighteen sites and close both rows.
- Round 2: [A] restart closes a cron run `error`/`interrupted` (`jobs.go:227`), a user stop closes it `stopped` (`:249-252`); recovery semantics per kind (`workqueue.go:254`, `stackconf/job.go:47`, `backup/work.go:150-187`, `volmove.go:390`) belong here with the durable queue (g-boot-workers). Image-check history rides `cron_runs` under `watch:<tile>` (`watcher.go:218`) and is pruned only from `jobs.finishRun` (`jobs.go:291`). Work items claim atomically, deployments do not (g-store).

### 2.13 NotificationService (cross-cutting, relocate out of `handlers/`)

`Push` called from 7 sites, all `infra/` (inverse layering); ws nudges from ~40 sites (web canvas/status/prhook, `api/forward.go` the only API caller, engine/jobs/metrics). Methods: `TileStatusChanged(ctx, tile)` (`Project` + `Containers`; replaces `web/app/handler.go:392`, `web/db/handler.go:305`, `engine.go:529-530`); `StackChanged`; `OrgChanged`; `ContainersChanged`; `DeployFinished(ctx, tile, d)` (one title/body builder for `engine.go:534` and `cigate.go:117`); `CronRunFailed`; `ImageUpdated`; `ImageCheckFailed`; `ListForUser`, `MarkAllRead`, `ClearAll`, `Unread`, `SetPrefs`.
Called by: TileLifecycleService, ManagedInstanceService, DeployService, PlanService, StagingService, VariableService, SliceService, canvas handlers, forward, jobs, imagewatch, metrics.

- [B] API `stopApp`/`restartApp`/`toggleCron` (`api/lifecycle.go:30,48,76`), `createDB` deploy (`api/databases.go:249-259`), `setProvisionPublic` (`api/lifecycle.go:274`), `putStackVars`/`putEnvVars` (`api/variables.go:245,296`) fire no nudge where web does.
- [A] `KindDeployFailed` fired with two payload shapes (`engine.go:533-534` "Deploy failed" vs `cigate.go:117` "Deploy blocked"); `KindImageUpdate` reused for check failures (`watcher.go:204`), so opting out of updates drops failure alerts.
- Notification centre GET writes (`web/notification/handler.go:265`), errors dropped.
- Round 2: [B] `watcher.go:152-162` (managed auto-update) and `backup/work.go:165-187` (restore recovery) change runtime state with no nudge (g-boot-workers). Inbox view, mark read, clear, badge (`web/notification/handler.go:27,41,49,57`) join the fan-out here (g-leftovers). [SEC low] `/ws` is registered before `auth.Load()` (`web/server.go:128` vs `:172`), the hub has no subject func and joins any named room (`cmd/stackrd/main.go:297-311`); payload is the event kind only (`notify.go:141`), so activity timing leaks, not content; room membership goes through AccessService (g-access).

### 2.14 MailService (cross-cutting)

`SendInvite(ctx, org, invite, link) (failed bool)` with the template from `web/org/members.go:291-295`. Only writer today: `web/org/members.go:290`. `*API` has no Mailer (`v1.go:57`). Needs a base URL source that is not `echo.Context` (`inviteURL` `members.go:289`) before the API can call it. Mailer is env-only (`infra/mail/mail.go:37`), no settings surface. `inv.MailFailed` set on the in-memory row only (`:297-298`); persistence not checked.
Called by: MemberService.

- [B] API invite/add-member never mails (2.5).

### 2.15 Round 2 candidates

- `ImageWatchService` (g-boot-workers W3-W8, F5-F7): owns `image_digest`/`latest_digest` baselining for the three writers (`main.go:420-424`, `managedtiles.go:497-500`, `watcher.go:118-127,183-187`), the check-history rows (`watcher.go:218`), the interval setting (`web/settings/handler.go:124`, `watcher.go:230-240`), and routes the auto branch through DeployService (inherits `UpperEnv`) and ManagedInstanceService (writes status). Today the rule is split across `main.go`, `managedtiles` and `imagewatch`.
- Tile status writer note (g-boot-workers W1, F9): 14 writers (`web/app/handler.go:338,361,367`, `web/db/handler.go:252-294`, `api/lifecycle.go:39,61,66`, `api/databases.go:250,255`, `engine.go:476,509,521`, `volmove.go:569,675,680`, `deploy/waiting.go:77,98`, `apply:605-607,1228-1231,1474-1484`, `orgconf.go:977-980`) and one observer (`reconcile.go:24-68`). Not a new service: `TileLifecycleService` (2.3) becomes the single owner of `UpdateTileStatus`, with `Observe(tile, containerState)` for the reconciler and `Stop`/`Start` for `quiesce`/`stopFor`; the kind rule (`runpolicy.KeepAlive`) is applied once instead of only in the observer.
- `AuditService` note (g-store): `audit.Record` (`store/audit/audit.go:185-190`) is a caller duty; every web/api secret path records, config apply (`apply:1802-1816`, `orgconf.go:723-727`), env clone and teardown (`envops.go:219-231,274-281,548-553`) and managed publication (`managedtiles.go:383-389`, `provision_s3.go:177-178`) do not. `VariableService.Set/Unset` records on every path; `AuditService` stays a thin leaf (record, `PanelViews`, `ServeValue`).
- `InstallerSpec` (g-leftovers F3, F4; a shared package, not a service, it runs outside the server): the panel service spec (env names, mounts, network `stkr`, hostname, constraints from `internal/installer/steps.go:128-169`) and the root-domain grammar (`answers.go:518-549`) defined once and imported by the installer, `AdminService.Upgrade` (`service/admin.go:156`) and `seed.go`; retires `scripts/dev/deploy-test.sh:118-131`. DomainResourceService (2.7) takes the grammar so panel and installer refuse the same hosts. Relay port 15000 (`cmd/proxyrelay/main.go:107`, `runtime.go:83`) is the same kind of shared constant.
- `ContainerService` (g-leftovers F1; or a `NodeService` method set, 2.11): node routing, the system-container guard as one rule for stop and remove, the running filter, the inspect error taxonomy (`web/container/handler.go:38-43,76-90,179-191,212,224-235`, `terminal.go:36`). Calls cluster only; web-only today, an API twin needs it first.
- `MetricsService` (g-boot-workers W2, B20): already one impl (`StoreHostSample` `metrics.go:200`, agent samples `handlers/api/server.go:100` → `nodes.go:288`); needs only a home outside `infra/metrics` that does not import `managedtiles`.
- Boot one-shots (`Migrate`, `secrets.Load`, `ConvertLegacyMounts`, `BackfillVariables`, `EnsureOverlay`, `SweepForwardState`, `SweepStaleRuns`, `SweepMoveReceivers`) stay in `main.go` or a `boot` package as callers: `ConvertLegacyMounts` → VolumeService.Create, `BackfillVariables` → VariableService, `SweepStaleRuns` → DeployService.RecoverInterrupted with one terminal vocabulary shared with `Cancel`. `SweepStaleRuns` and `workqueue.recover` run before `srv.Start` (`main.go:315,549`); `agent.Ensure`, `EnsureManaged`, `EnsureTraefik+Resync`, `CheckUpgrade` run in goroutines (`main.go:285,361,463,488`) and can overlap the first requests.

### 2.16 Composition graph (edges, caller → callee)

- TileService → ProxyService, StagingService, SchedulerService, DeployService, SliceService, VariableService, BackupScheduleService, VolumeService, ConnectorService, NotificationService
- TileLifecycleService → SchedulerService, NotificationService, DeployService
- ManagedInstanceService → ProxyService, DeployService, VariableService, SchedulerService, NotificationService, SliceService
- SliceService → ManagedInstanceService, VariableService, DeployService, NotificationService
- EnvironmentService → ProxyService, SchedulerService, PlanService, SliceService, DeployService, DomainService, ManagedInstanceService
- VariableService → PlanService, DeployService, StagingService, AuditService
- ShareLinkService → VariableService; EnvCompareService → PlanService, ApplyEngine, SchedulerService
- StackService → EnvironmentService, PlanService, StagingService, ConnectorService, SchedulerService, DeployService
- DeployService → NotificationService, ApplyQueue (park/release via engine)
- ReleaseService → DeployService, PlanService, ApplyQueue
- PlanService → ApplyEngine, ApplyQueue, NotificationService; StagingService → ApplyEngine
- ApplyEngine → TileService, DomainService, DomainResourceService, EnvironmentService, VariableService, BackupScheduleService, SliceService, ManagedInstanceService, StackService, ProxyService, DeployService, SchedulerService
- OrgPlanService → ApplyQueue, OrgService, StackService, DomainResourceService, VariableService, StorageService, ManagedInstanceService
- OrgService → ProxyService, DomainResourceService, OrgRegistryService, MemberService
- MemberService → MailService; AuthService → (store); APIKeyService → (store); AccessService → (store)
- DomainService → ProxyService, DomainResourceService, StagingService, SettingsService
- DomainResourceService → ProxyService, OrgService, StackService
- VolumeService → DeployService, StagingService, ProxyService, SliceService, VolumeOwnership
- StorageService → DeployService, OrgService, NodeService, cluster
- BackupScheduleService → SchedulerService, TileService, BackupDestinationService; BackupDestinationService → SchedulerService
- RegistryAdminService → ProxyService; ConnectorService → githubapp; PREnvService → EnvironmentService, PlanService, DeployService, ConnectorService, SchedulerService, NotificationService
- SettingsService → ProxyService, OrgService, StackService; NodeService → runtime, agent, placement, VolumeOwnership
- ImageWatchService → DeployService, ManagedInstanceService, TileService (digests), SettingsService, NotificationService
- ContainerService → NodeService, cluster; MetricsService → NodeService (samples), SettingsService (retention)
- AccessService: consulted by every handler on both surfaces and by NotificationService (ws rooms); reacts to MemberService (remove, demote), AuthService (deactivate), StackService (move)
- Boot → VolumeService (legacy mounts), VariableService (backfill), DeployService (RecoverInterrupted), RegistryAdminService (EnsureManaged), ProxyService (SetTrustedProxies, Ensure, Resync), NodeService (EnsureAgent, Sync), SchedulerService, AdminService (CheckUpgrade), DomainResourceService (seed)
- InstallerSpec: leaf package imported by the installer, AdminService, seed, DomainResourceService
- ProxyService, SchedulerService, NotificationService, MailService, AuditService, VolumeOwnership: leaves (store + infra only)

## 4. Round 2 gap sweep

Six shards for the surfaces round 1 kept as a column or skipped: `shards/g-cli.md`, `g-boot-workers.md`, `g-store.md`, `g-access.md`, `g-config-small.md`, `g-leftovers.md`. The rows (1.1 to 1.12) and the "Round 2" bullets in section 2 carry the detail; this section is the headline per shard.

CLI (`g-cli`, `internal/cli/cmd`): which rules the client applies before or instead of the API.
- Only surface that strips `schedule`/`command`/`timeout` off a non-cron tile (`tile.go:126-129`); the api stores them on any kind (`apps.go:150-153`).
- `--cpu` alone zeroes memory and `--dockerfile` alone clears the build context because the api patch takes each nested object whole (`tile.go:247-263`, `apps.go:248-253`).
- `pickRelease` (`promote.go:131-196`) is the only ref resolver and ambiguity check for promote; three force policies across web (`true`), api (body), cli (`--force`).
- `-y` doubles as `force` on env rm and reset (`promote.go:239-240,274`); `infra rm` always sends `force=true` (`infra.go:1559`), so the api held-slices refusal is unreachable.
- Metrics range validated client-side only, coerced server-side (`lifecycle.go:191-195` vs `:137-142`); `--role admin` is coerced to `member` by the api with no CLI check (`members.go:358` vs `api/members.go:232-237`).
- Four name resolvers scan `/apps` and `/dbs` (`helpers.go:132-223`, `backup.go:16-35`) beside the server's `slugpath.go:94`; org plan approve confirms blind because there is no `GET` one route (`v1.go:242-246`).

Boot and workers (`g-boot-workers`, `cmd/stackrd/main.go`, tickers and consumers under `infra/`): what runs outside a request.
- `tiles.status` has 14 writers and one observer (`reconcile.go:24-68`); a cron stopped from web stays `stopped` for ever (`:26`) and still ticks (`jobs.go:311`).
- Image auto-update bypasses the upper-env gate (`watcher.go:175`); managed auto-update writes no status (`:152-162`); `image_digest` has three writers (`main.go:420-424`, `managedtiles.go:497-500`, `watcher.go:118-127,183-187`).
- Boot never renders the registry route (`main.go:286`, `proxy.go:577-624,715`); trusted-proxy CIDRs are validated two ways (`seed.go:54-57` vs `web/settings/proxy.go:74-77`); agent ensure has two entry policies (`main.go:463-484` vs `web/server/nodes.go:71`).
- Two vocabularies for "did not finish": `SweepStaleRuns` `error` vs cancel `cancelled` (`sqlite/deployments.go:55-69`, `engine.go:313`); cron run `error`/`interrupted` vs `stopped` (`jobs.go:227,249-252`).
- `quiesce` and restore recovery scale services with no status write (`backup.go:404-417`, `backup/work.go:165-187`); `ConvertLegacyMounts` is a fifth volume-tile writer with its own rules (`envops.go:642-700`); `BackfillVariables` skips `ClearWaiting` (`backfill.go:23-45`).
- Boot-only steps that are services in disguise: `netpool.Backfill/Sweep`, `nodes.Sync` (also writes from a GET), `registry.EnsureManaged`, `seedInstall`; the deploy engine never touches the proxy.

Store (`g-store`, `store/db/migrations`, `store/repo/sqlite`): rules that live in SQL or the repo layer. Foreign keys are on (hamr `sqlite.go:78,139-140`); the comment at `variables.go:395-396` is stale.
- Tile delete: the FK cascade covers domains, deployments, backups (`sql:103,91,51`); `DeleteTile` drops variables and bindings (`tiles.go:269-282`); `provisions` has no FK and api plus orgconf leave rows behind.
- Env delete leaves `staged_changes` and env `node_positions` on every surface (`sql:271-281`, `stacks.go:378-381`); stack delete cleans canvases (`:276-282`) but not staged rows, variables, secret links, domain resources.
- Dead SQL defaults: tile columns (`sql:288-316`), `keep_latest 7` (`:57`), domain `https` (`:107,114`); each writer re-supplies them differently.
- `UNIQUE` pre-checks: env slug `stack` and stack `(org_id, slug)` surface as raw 500 on web (`sql:126,269`); no layer maps constraint errors.
- `users.email` UNIQUE is case-sensitive and the store never folds (`sql:341`, `users.go:559-578`); `repo.Slugify` lives in the store, the store never applies it (30 handler sites, `orgconf.go:963` raw).
- Unaudited secret writes from config and infra (`apply:1802-1816`, `orgconf.go:723-727`, `envops.go:219-231,274-281,548-553`, `managedtiles.go:383-389`, `provision_s3.go:177-178`); the store already owns encryption, `UpdateTile` field exclusion, home env creation, plan superseding and atomic claims, and a service must not re-implement them.

Access (`g-access`, authorization on purpose): the gate vocabulary per surface and where the two picked different levels for one verb.
- Deactivating a user closes nothing: sessions and API keys keep working (`web/settings/handler.go:563-580`, `service/auth.go:117`, `web/server.go:162-164`, `api/auth.go:163-166`).
- Org registry credentials and tag delete: owner on web, member on api (`web/org/registry.go:166,193,218` vs `api/registry.go:26-34,101,200`).
- API key write scopes are granted against the active cookie org and keys have no org column (`web/account/handler.go:201-205,260`, `web/cli/handler.go:96`, `orgctx.go:148-154`); only the per-request `requireOrgWrite` holds, and `cancelDeployment` forgot it (`apps.go:575`).
- `/ws` is unauthenticated and rooms are joinable by name (`web/server.go:128` vs `:172`, `cmd/stackrd/main.go:297-311`); activity timing leaks, not content.
- Plan-preview registered `config:read`, gated write (`v1.go:244,281`, `config.go:39-52`); wizard steps mix owner and member (`setup.go:93,171,416` vs `settings.go:529`, `settings/handler.go:441`); orphan deployment rows lose the tenancy check on web (`web/deployment/handler.go:44-52`).
- Duplication: owner check ×3, write check ×3, tenancy ×2, admin ×2, scope grant ×2, managed lock ×3 plus about 20 inline `ConfigManaged()`; web answers 404 where api answers 403.

Small config packages (`g-config-small`, `orgconf`, `settings`, `secrets`, `sharelink`, `envcompare`, `runpolicy`, `varref`, `envutil`, `envcolor`): orgconf is a second hand-built copy of eight handler operations; the rest are pure helpers.
- Org rename skips anti-squat (`orgconf.go:424-441,660-673` vs `web/org/members.go:101-116`); org domains skip squat and never `EnsureTraefik` on an acme change (`:461-482,811-823,826-833`, note `:477`).
- Org `vars:` keys are never validated (`:160-241,706-712`); the settings managed gate exists only on web org (`web/org/registry.go:262-265`; `api/settings.go:44-75`, `web/project/handler.go:1022,2540` have none).
- Org shares are inserted unprobed and orgconf is the only update path (`orgconf/storage.go:213-233`); a stack binding from the file skips the connector check, repo normalisation and staged drop (`:895-902,621-630`).
- Shared instances: raw slug, no reserved or collision check, delete with no provisions gate (`:962-970,985-995`); `WebhookToken` `uuid` vs `RandomHex(24)` (`:966`, `api/databases.go:237`, `web/project/handler.go:576`).
- Web create tile bypasses `runpolicy` (`web/project/handler.go:492-503,570`); the service-only knob rule is spelled inline in api and config (`apps.go:332-361`, `stackconf.go:1021`).
- Plaintext at rest: `registries.password`, `DBPassword`, `BasicAuthPassword`, `WebhookToken`, `ProtectPassword`; `secrets.Encrypt` only in `store/repo/sqlite`. Three inline KEY=VALUE parsers beside `envutil.Parse` (`plan.go:1126-1132`, `engine.go:1298`, `api/helpers.go:145-159`).

Leftovers (`g-leftovers`, raw containers, CLI login, notification centre, graph, installer, relay): surfaces round 1 did not assign.
- Raw container guard asymmetry: stop refuses any system container, remove only a running one, start and terminal nothing (`web/container/handler.go:202,212,224-235`, `terminal.go:36`); the rule is handler code and `cluster.StopContainer` accepts anything.
- Root domain has three grammars (installer `answers.go:518-549`, seed `seed.go:33-46`, `envops.go:287-299`) plus `proxy.routablePanel` (`proxy.go:797-802`); `localhost` is refused by the installer and accepted by the panel.
- The panel service spec is owned by the installer alone (`steps.go:128-169`, copy `deploy-test.sh:118-131`); `Upgrade` sets `STACKR_IMAGE` only (`admin.go:156`); the CLI wrapper execs `/app/stackr`, absent from `Dockerfile.runtime:6-10`.
- CLI `Exchange` is a third API-key minting site (`web/cli/handler.go:153-161`); the scope filter is copied and ignores `CanGrantWrite` (`:96-104` vs `web/account/handler.go:201-209,258-262`).
- Relay port 15000 is defined twice (`cmd/proxyrelay/main.go:107`, `runtime.go:83`); the pending-moves filter twice (`web/project/handler.go:978-989`, `apply:1929-1943`).
- Graph builders and `annotate` carry no rule and import nothing below handlers: they stay in `handlers/web`.

### Security-relevant (round 2)

Split by evidence. **Confirmed** = the refs were read and the check is
genuinely absent. **Unverified** = the read is right but the exploitability
is a hypothesis nobody exercised; do not triage these as live until someone
does. The count in section 5 is the confirmed list.

#### Confirmed

- [SEC] Deactivated user keeps sessions and API keys: `web/settings/handler.go:563-580`, `service/auth.go:117`, `web/server.go:162-164`, `api/auth.go:163-166` (2.5).
- [SEC] Members mint and delete org registry credentials and tags via API where web is owner-only: `api/registry.go:26-34,101,200` vs `web/org/registry.go:166,193,218` (2.8).
- [SEC] Cancel deployment on a read gate, the one live case of scopes minted against the wrong org: `api/apps.go:570-579`, `web/account/handler.go:201` (2.4, 2.5).
- [SEC low] `/ws` hub joinable unauthenticated, rooms by name: `web/server.go:128` vs `:172`, `cmd/stackrd/main.go:297-311`, `notify.go:141` (2.13).
- Image auto-update bypasses the upper-env deploy gate: `infra/imagewatch/watcher.go:175` vs `web/deployment/handler.go:81`, `api/apps.go:485` (2.4, 2.15).
- Plan-preview registered `config:read` but gated write: `v1.go:244,281`, `config.go:130,39-52`, `orgconfig.go:55`; stricter than declared, the scope catalogue lies (2.4).
- API key write scopes bounded by the active cookie org while keys are user-scoped: `web/account/handler.go:260`, `web/cli/handler.go:96`, `orgctx.go:148-154`; demotion leaves scopes in place (2.5).
- Wizard `/setup/domain` and `/setup/connector` are member where the rest of the wizard is owner: `web/org/settings.go:529`, `web/settings/handler.go:441`, `setupgate.go:403-405` (2.5).
- Share links outlive their minter and an org move; the token is a bearer by design: `config/sharelink/sharelink.go:164-201`, `web/org/handler.go:148` (2.2, recorded).
- Plaintext credentials at rest: `registries.password` (`sqlite/infra.go:31-51`), `Tile.DBPassword`, `BasicAuthPassword`, `WebhookToken`, `Settings.ProtectPassword` (`settings.go:28`) (2.8, 2.11).

#### Unverified

Read correctly, consequence not exercised. Each needs one runtime check
before it earns a [SEC] label.

- Orphan deployment rows skip tenancy on web (`web/deployment/handler.go:44-52`). Deployments cascade with tiles (`001_initial.up.sql:91`), so the branch is probably unreachable. Check: can a deployment row outlive its tile at all (2.4)?
- Profile email save is not lowercased (`web/account/handler.go:78`) while `GetUserByEmail` is exact (`sqlite/users.go:29`) and `users.email` UNIQUE is case-sensitive (`001_initial.up.sql:341`). Lockout is inferred, never reproduced. Check: save a profile with capitals, then log in (2.5).

## 5. Totals

Operation table: 267 rows (246 in round 1 incl. one "see stacks" pointer row; 21 added in round 2: 1 in 1.1, 1 in 1.8, 16 in 1.11, 3 in 1.12). Flagged 117 (round 1: 95; round 2: 10 new rows flagged, 8 A and 2 B, plus 11 existing rows newly flagged and 3 existing rows gaining a second letter; review pass: "Rename tile" in 1.1 resolved from "not checked" to B). A 91 (76 A-only incl. one A+D, 15 A+B), B 38 (23 B-only, 15 A+B), C 3, D 1 (now A+D). Unflagged 150 (4 of them marked `svc`). Round 1 alone: flagged 95, A 71 (59 A-only, 12 A+B), B 32 (20 B-only, 12 A+B), C 3, D 1.

Call sites into infra/config from handlers and `config/stackconf` (lens 2): `managedtiles` 38, `infra/deploy` 31, `stackconf`/`staging`/`envops`/`orgconf` 84 (api 25, web 47, prhook 12), proxy 53 (44 writes), `jobs` 19 (`LoadSchedules` 18), `envnet` 11, `backup.LoadSchedules` 10, `workqueue` 8, `cluster`/`runtime`/`placement`/`netpool` direct 9, `deploy.ClearWaiting*` 9, notifier nudges ~40, `Push` 7 (all infra), `mail.Send` 1. `internal/stackrd/service/` calls none of them. Round 2 adds the non-handler callers: boot `main.go` 23 steps (B1-B23), 21 ticker and consumer sites (W1-W21), `UpdateTileStatus` 14 writers + 1 observer, `image_digest` 3 writers, `agent.Ensure` 2, `EnsureManaged` 2, `WriteRegistry` 2, `netaddr.ParseTrusted` 3, `secrets.RandomHex` 13, and 40 CLI rows of client-side rules (`shards/g-cli.md` §1).

Services in the inventory: 14 groups, about 40 named services (2.1 to 2.14), plus the round 2 candidates in 2.15 (ImageWatchService, ContainerService, MetricsService, the InstallerSpec package, AuditService and tile-status notes) and the composition graph in 2.16. AccessService (2.5) now carries a full brief.

Security-relevant findings, split by evidence. **Confirmed** = the refs were
read and the check is genuinely absent. **Unverified** = the read is right,
the consequence is a hypothesis nobody exercised. Round 1 contributes 7
confirmed and 1 unverified; round 2 adds 10 confirmed and 2 unverified under
"Security-relevant (round 2)" in section 4 (the orphan-deployment entry is
round 2's own). 17 confirmed, 3 unverified. Triage the confirmed list.

Confirmed (round 1):
- Cross-tenant domain flip or delete by id: `web/app/handler.go:1810`, `:1908` (2.7). Re-read: `ToggleDomainHTTPS` takes `:domainID` with no `d.TileID == a.ID`; `DeleteDomain` checks ownership on the staged branch `:1892` and not on the live branch `:1908`.
- Any org member can plan, approve and apply an org plan from the API/CLI; web is owner-only: `api/orgconfig.go:28,111,55,86` (2.5).
- Read-only member with a deploy-scoped key can cancel a deployment: `api/apps.go:570-579` (2.4). Re-read: `loadDeployment` calls `requireTile(c, d.TileID, false)`.
- Web delete-registry has no managed guard; a POST removes the managed registry and boot rotates its password: `web/settings/handler.go:657` (2.8). Re-read: the handler is `DeleteRegistry(c.Param("id"))` with no load in between.
- Password strength lives in three handlers with two rules, none in `service/auth.go` (2.5).
- Web `SetDomainCert` has no managed gate: `web/app/handler.go:1846` (2.7).
- `KeyAuth` does not check `user.Active` (`api/auth.go:163-166`); round 2 confirmed nothing else rejects an inactive user (section 4, first confirmed entry). Re-read: `KeyAuth` loads the user by id and checks only that the row exists.

Unverified (round 1): profile email save is not lowercased and the login
lookup is exact, so a save with capitals may lock the user out
(`web/account/handler.go:78`, 2.5). The reads are right; the lockout was
never reproduced. One runtime check settles it.

Shard files: `shards/` (20: 14 round 1, 6 round 2 `g-*`). Calibration corrections and class D at the top of this file.
