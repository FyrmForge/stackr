# Cloudflare connector

Status: parked 2026-09-13; nothing built.

The proposal gives each organization a Cloudflare tunnel and optional shield
so origin traffic reaches Traefik without exposing the host. Token custody,
hostname ownership, failure behavior and whether this belongs before or after
beta remain undecided.
