# Surface parity

Status: all eight groups shipped 2026-09-13.

Web, API/CLI and config now share scoped var and secret references, releases
and promotion, environment lifecycle, registry namespaces, backup settings,
resource export and declarative moves. The verified matrix is
`docs/surface-parity.md`; feature contracts live under `docs/features` and
the API contract is `docs/openapi.json`.
