# Light theme

Status: shipped and browser-verified 2026-09-03.

The UI gained a light palette across the shared `rw-*` tokens, component
states, graphs, terminals and charts while retaining the dark theme. Theme
tokens live in `frontend/css/input.css`; shared selection and layout behavior
lives in the web components.
